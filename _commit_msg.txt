release(v1.0.0): ★★ 里程碑 — 前后端看板建成；版本号改为「只改一处」(根 VERSION) + 修三处版本打架

【用户指令】「好了，现在有前端了，版本就定为 1.0.0 吧」

【★★★★ 建版本单一来源（与"端口只改一处"同思路）】
新增仓库根 `VERSION` 文件（内容 1.0.0）：
  ├── README.md 顶部「当前版本」= 它
  ├── change_log.md 最新 [x.y.z] 条目 = 它
  ├── dashboard/api/app/settings.py **读**它（看板 /api/meta 的 version 由此来）
  └── dashboard/web/package.json version = 它（看板页脚显示"项目版本 v1.0.0"）

【★★★★ 顺带修掉真实事故：版本号三处打架】
  git tag 最新（真实）      : v0.21.1
  change_log.md 最新条目    : 0.20.2   ⚠ 落后 9 版
  README.md 顶部声明        : v0.20.0  ⚠ 落后 11 版（且与自身版本表格矛盾）
  README 版本表格           : 只到 v0.20.4，缺 v0.20.5~v0.21.1 共 7 版
⇒ 补记：change_log 新增 [1.0.0] 条目（含**补记 v0.20.3~v0.21.1 这 9 版的明细**，并说明"此前漏记"）
⇒ README 顶部改为 v1.0.0，版本表格补齐 v0.21.1 / v0.21.0 / v0.20.8 / v0.20.7 / v0.20.6 / v0.20.5 等行

【★★ 新增永久防线 tools/_test_version_sync.py】
检查 VERSION / README / change_log / settings.py(不许硬编码) / package.json 是否一致；
git tag 只警告（因 tag 在 commit 之后打）。
★ 已做**负向验证**：把 VERSION 改成 9.9.9 ⇒ 报 4 处不一致 + exit 1（★ 连 BOM 都抓到）✓；撤销 ⇒ 通过 ✓
★ 另加 BOM 容错（utf-8-sig）—— 真实场景编辑器加 BOM 会导致版本变 '\ufeff1.0.0' ✗

【dashboard 文档】README 增「〇、版本也只改一个地方」一节

【验证】Python 编译 110 全过 · 引号干净 · **13/13 测试全过**（含新增版本测试）· tsc --noEmit 0 错
  /api/meta 实测返回 version=1.0.0 + versionSource=D:\loop_code\VERSION ✓

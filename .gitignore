# Python
__pycache__/
*.py[cod]

# 大二进制数据（*.h5/*.pkl 可由 build_*.py 从 E:\rq 重建）
*.h5
*.npy
*.pkl

# 运行产物 / 图形 / 日志
*.png
*.log
loop*C.log
loop*C_err.log

# 引擎滚动状态（运行中会变）
engine/loop_state.pkl

# standard_test 运行产物（带时间戳的报告；只跟踪 standard_test.py 本身）
standard/*_report.txt
standard/*_report.png

# all04_f11_idx 运行输出（stdout/stderr 日志，可随时重跑）
strategies/all04/f11_*.txt
# Loop 每代 L2 明细流水（docs/ 下逐代累积的 CSV 档案，运行产物不上 git）
docs/loop_archive.csv
# gen16 前旧快照已归 docs/history/（停更，已 git 跟踪为归档，此处不再 ignore）

# IDE / 系统
.idea/
.vscode/
*.swp
Thumbs.db

# IDE 会话/项目数据（CodeBuddy 记忆等，本地专用）
.codebuddy/

# 临时诊断脚本/产物（可随时删）
ai_test/

# 本地密钥文件（不上传；DeepSeek API key，桌面 1.txt 删除后由此兜底）
.deepseek_key
engine/.deepseek_key

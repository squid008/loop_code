# Python
__pycache__/
*.py[cod]

# 大二进制数据（*.h5/*.pkl 可由 build_*.py 从 E:\rq 重建）
*.h5
*.npy
*.pkl

# 运行产物 / 图形 / 日志
*.png
*.log
loop*C.log
loop*C_err.log

# 引擎滚动状态（运行中会变）
engine/loop_state.pkl

# standard_test 运行产物（带时间戳的报告；只跟踪 standard_test.py 本身）
standard/*_report.txt
standard/*_report.png
# pool_tags.py / obs_analysis.py 的生成报告（每次重跑覆盖）
standard/pool_tags_report.md
standard/obs_analysis_report.md

# all04_f11_idx 运行输出（stdout/stderr 日志，可随时重跑）
strategies/all04/f11_*.txt
# Loop 每代 L2 明细流水（docs/ 下逐代累积的 CSV 档案，运行产物不上 git）
# ★ 用通配 `*` 而非精确名(2026-09-12 修): --mine_pool 会产出带池后缀的变体
#   (loop_archive_300.csv 等), 精确名匹配不到 -> 会变成"未跟踪"甚至被误提交。
docs/loop_archive*.csv
# 风格暴露观测（--style_obs 的逐代累积产物：每代每个被求值候选一行）
docs/loop_style_obs*.csv
# 剥风格明细（--strip_style 的逐代累积产物: 每个 L2 候选的剥风格前后指标）
docs/loop_strip_style*.csv
# 池内指标（--pool_obs 的逐代累积产物: 长表, 每候选 x 每池一行）
docs/loop_pool_obs*.csv
# 池标签导出（pool_tags.py 从上面两个文件派生, 可随时重算 -> 喂 PG 用）
docs/pool_tags*.csv
# gen16 前旧快照已归 history/（停更，已 git 跟踪为归档，此处不再 ignore）

# IDE / 系统
.idea/
.vscode/
*.swp
Thumbs.db

# IDE 会话/项目数据（CodeBuddy 记忆等，本地专用）
.codebuddy/

# ai_test/ = 草稿区 + 运行产物（一次性诊断脚本、日志、pkl/csv 等，可随时删）
# ⚠ 2026-09-14（v0.5.2）：**生产管线已迁至 tools/**。
#   原因：ai_test/ 长期被整个忽略，但收尾管线（run_tracks/build_facs/cross_pool_review ...）
#   住在里面 ⇒ `git checkout v0.x` 回退代码时**整条收尾管线会消失**（回退点不完整）。
#   ⇒ 工具入 git、产物继续忽略：tools/ 正常跟踪；ai_test/ 只留 3 个兼容垫片。
#
# ⚠ 必须写 `ai_test/*`（带斜杠 = **锚定仓库根**）而**不是** `ai_test/`：
#   git 规则「父目录被排除时无法再包含其中的文件」⇒ 写 `ai_test/` 会让下面的 `!` 例外**全部失效**。
ai_test/*
# ★ 2026-09-15：原先这里还有 3 条 `!ai_test/{run_tracks,build_facs,cross_pool_review}.py`
#   **兼容垫片例外** —— 已验证那 3 个文件**根本不存在**（真实版本在 `tools/`）⇒
#   例外是**失效残留**，已删除 ✓（不删会让读者误以为 ai_test 里还有生产脚本）
# ⚠ 但**同名目录出现在别处**（history 下的归档快照）要继续整体忽略 —— 与迁移前行为一致。
#   原规则是 `ai_test/`（无斜杠 ⇒ 匹配**任意层级**的同名目录），换成锚定的 `ai_test/*` 后
#   会**误放开** history/**/ai_test/ 里的 101 个归档文件（实测：001311 下 88 个、001515 下 13 个）
#   ⇒ 显式补回这条。
history/**/ai_test/
# ★ 2026-09-15：`ai_test/` 整体归档 → `history/ai_test_20260915/`（591 个一次性脚本/产物）
#   ⚠ 上面那条 `history/**/ai_test/` **匹配不到** `ai_test_20260915`（名字不同）⇒ 必须显式补 ✓
history/ai_test_20260915/
# ★ 2026-09-15 归档的一次性件（上证50 池产物 / standard 已归档件）—— 同样不进版本
history/20260915_cleanup/

# 外部参考项目（只读参考，非本项目代码；28MB/237 文件，勿混入版本）
# 来源：D:\工作\QuantaAlpha-main.zip → 解压于此供查阅（roadmap §8.8）
QuantaAlpha-main/

# 本地密钥文件（不上传；DeepSeek API key，桌面 1.txt 删除后由此兜底）
.deepseek_key
engine/.deepseek_key

# ★ 看板 dashboard（2026-09-15，原 frontend/）：只跟踪源码/配置，**不跟踪依赖与构建产物**
dashboard/web/node_modules/
dashboard/web/dist/
dashboard/web/.vite/
dashboard/**/__pycache__/
dashboard/**/*.pyc

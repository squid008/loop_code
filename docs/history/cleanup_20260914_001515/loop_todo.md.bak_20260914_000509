# loop_todo.md — 因子挖掘项目的「等待清单 / 工作队列」

> **这个文件是本项目跨会话的唯一任务真相源。**
> 我（AI）在对话中会退出，而自动化跑的是**全新会话**（不继承任何对话上下文）
> ⇒ 所以：**任何"待办"都必须落到这个文件**，任何新会话**先读这里**再动手。
>
> 关联文档（按需读，别全读）：
> - 设计决策全文：`docs/factor_roadmap.md` §8.19 ~ §8.23（**改动前必读对应小节**）
> - 当日细节与踩坑：`.codebuddy/memory/2026-09-12.md`
> - 跨会话长期事实：`.codebuddy/memory/MEMORY.md`
> - **进度速查：`python ai_test/status.py`**

---

## 🔴 待改清单（**新会话先看这里**；下面每条都写清了"何时能改/怎么验证"）

### 待改 #1 ★★★ B角 的「诊断→决策」链条断裂 —— 等轨道跑完必修（**证据已备齐**）

- **状态**：`[~]` **阻塞中** —— 1000/300/500 三池轨道正在跑（`ai_test/run_tracks.py`），
  **中途改 `engine/loop_critic.py` 会让 B角 行为在代与代之间突变**，实验不可比。
- **何时能改**：**轨道跑完后（或主动停掉轨道）立刻做**。它只影响"下一代怎么调参"，**不影响已入库因子**。
- **★ 一键复现证据**：`python ai_test/critic_sensor_report.py --pools=all,1000,300,500`
  ⇒ 生成 `ai_test/_critic_sensor.md`（逐代并列 **B角建议 vs LLM建议 vs 实际产出**）。
  ⇒ **改动前先跑它**，下面所有数字都出自它。

#### 问题 ①：规则5 从来不是「自适应」，而是**固定动作**
`fail_calmar` 触发条件（`loop_critic.py:186`：`> 0.55`）**在全部 70 代上 100% 成立**：

| 统计（全A 轨道 70 代） | 值 |
|---|---|
| `fail_calmar` = 1.000 | **52 代（74%）** |
| 取值分布 | 0.846 ~ 1.000（**最低 0.846**） |
| ⇒ 规则5 触发比例 | **70/70 = 100%** |

⇒ `depth` 被**永久**推到 `[3,4,4]`、`mix` 交叉被推到上限 0.4。
⇒ **缺"饱和检测 / 触发记忆"**：一个"条件触发"的规则若条件恒真，就退化成**固定偏移**，
   而且会在多代间**累积**直到撞边界。

#### 问题 ②：判据与实际生效门槛**脱钩**
`engine/loop_critic.py:94`
```python
d['fail_calmar'] = float((fail['calmar'] <= 0.5).mean())   # ← 硬编码 0.5，且取全A 口径
```
而池内挖掘的**判定**是**池口径**（引擎启动时打印：
`入库门槛: 任一池 Calmar > 0.15　或　全A 口径(calmar>0.00 & sharpe>0.50 & 分段)`）。
⇒ 它**回答不了"离池门槛差多远"** —— **而那才是池内模式的真实卡点**。
⇒ 实测自相矛盾：`pool=1000 gen1` 报 `fail_calmar=1.000`，
   而同代**确有 2 个候选 Calmar 0.661 / 0.561 通过并入库**。

#### 问题 ③ ★★★ 最根本：LLM 审查「**只写日志、不参与决策**」
`loop_critic.ai_review(...)` 的返回值只用于打印，**从不回写 `sug`**
⇒ **59 次独立反驳全部被浪费**。LLM 在不同池、不同代上给出**高度一致的同一判断**：
> gen??：深度加深**有害**，当前已是 `ts_rank套ts_std套ts_mean` 的深链，再深只会更过拟合、更脆
> gen75：交叉+15%**对症但过头**…**深度加深会加剧过拟合与 negyear（已 0.85）**
> 1000 gen1：深度加深与 `min_stab=0.3` **冲突**：弱信号下深树更易过拟合，`neg_yr=3~5` 已示警

⇒ **方向完全相反**：LLM 每次建议 `depth=[2,3,3]`（**降**深度），B角 每次设 `[3,4,4]`（**升**深度）。
⇒ 59 次独立、一致、跨池稳定的反驳 ⇒ **不是幻觉，是结构性问题**。
⇒ **正确用法**：**规则负责稳定，LLM 负责纠偏**；现在 LLM 没有否决权 ⇒ 白白浪费。

#### 修法（按代价从小到大，建议至少做 1+3）
1. **`fail_calmar` 与实际门槛对账**：硬编码 `0.5` → 用生效的 `--min_calmar`；
   并**并行**输出 `calmar <= 0` 占比（**真·信号弱**，不随配置漂移）。
2. **池口径诊断**：池内模式下 `fail_*` 改按**池口径**统计（需把 `pool_rec` 的判定结果
   传进 L2 的 DataFrame，`loop_critic` 才能读到）。
3. **★ 动作记忆 / 饱和检测**：同一规则动作**连续 N 代**触发 ⇒ 视为已饱和，**不再重复施加**
   （否则 `mix`/`depth` 被单调推到边界）。
4. **★ 给 LLM 审查「否决/降权」通道**：当 LLM 对**同一条规则动作**连续多代反对 ⇒
   跳过该动作或降权。这是"规则 + LLM"双引擎的正确用法。

#### 怎么验证
改完跑 1 代，检查：① `fail_calmar` 与实际门槛一致；② B角 不再每代都加交叉/升深度；
③ `n_pass` 不下降；④ journal 里 LLM 反驳次数下降（说明纠偏生效）。

### 待做 #2 轨道跑完后**再跑一次**池因子库补录（兜底，30 秒的事）

- **为什么**：`_lib_sync` 的修复（自动建骨架，§8.44）是**改在文件里**的，而
  **gen3 进程加载的是旧代码**（我改文件时它已在跑）⇒ **gen3 入库的因子仍不会写进文档**。
  `gen4+` 是新进程 ⇒ 自动生效 ✓（`1000` 共 3 轮，`300/500` 各 3 轮 = 9 代）。
- **怎么做**：轨道跑完后执行
  ```
  python ai_test/backfill_library_pool.py --pools=300,500,1000      # 先 DRY-RUN 看
  python ai_test/backfill_library_pool.py --apply
  ```
  ⇒ 自动补 `docs/factor_library_{300,500,1000}.md`（**以 state.bank 为准**，不会多收）。
- **顺带体检**（增列后必做）：
  ```
  python ai_test/fix_csv_schema.py            # DRY-RUN 看有没有新的混合宽度
  ```
- **★ 判据**：补录后，各池 `factor_library_{pool}.md` 的「当前 N 个入库」应与
  `python ai_test/tracks_status.py` 里该池 bank 数**一致**。

---

## 0. 轮询者纪律（每次自动运行必须遵守）

1. **先读本文件的「当前占用」**：若已被占用且时间戳在 40 分钟内 ⇒ **立刻退出，什么都不做**
   （防两个自动化同时干活；超时/崩溃会留下过期占用，靠时间戳自动失效）。
2. 占用后，**本轮只做「待办表」里最靠前的、且阻塞已解除的**那**一件**事。
3. **有界**：单轮工作量控制在 **10 分钟内**。宁可做一半并把进度写回本文件，也不要长跑
   （自动运行**有超时**，历史任务 `loop-gen50` 就多次 `Run timed out`）。
4. **边做边落盘**：进度写进本文件「进度日志」；结论写进 `docs/factor_roadmap.md` 新小节。
5. **收尾必须清占用**（写「当前占用：空闲」），并把状态改为 `[x]` / `[~]`。
6. 改 `engine/loop_engine.py` 前，**先确认 Phase A 已结束**（见待办 0 的完成条件）；
   改完**必须跑 QA**（`python engine/qa_mine_pool.py`，加 `--heavy` 可选）。
7. **⚠ Token 纪律（直接决定成本，2026-09-12 实测）**：大文件**禁止整读**——
   `factor_roadmap.md` **201KB/2828行（≈70-100k tokens）**、`2026-09-12.md` **74KB**、
   `loop_engine.py` **121KB/2279行（≈35-45k）**、`MEMORY.md` **36KB**
   ⇒ 三者整读一次 **≈150-200k tokens**。取用方式：
   · 找 roadmap 小节：先 `search_content` 搜 `^### 8\.` 拿行号，再 `read_file` 带 `offset/limit` **只读那 1~2 节**；
   · 当日 memory：**只读尾部** 30~50 行；
   · 改引擎：先 `search_content`/LSP 定位函数，再定点读，**绝不整读**。
   **空闲轮目标 <8k tokens**（Phase A 没跑完 ⇒ 只看 `_pipeline.log` 尾部 + 追加一行 + 清占用，别读 roadmap/memory/引擎）。
   待办全部 `[x]` 后**每轮只追加一行日志**，不做别的。

---

## 当前占用

```
状态: 空闲
占用者: (无)
时间戳: -
```

---

## 待办表（按顺序；⚠ = 未解除阻塞）

| # | 状态 | 事项 | 阻塞 / 完成条件 | 需要改代码 |
|---|---|---|---|---|
| **0** | `[~]` 进行中 | **三池并行挖掘 Phase A**：`ai_test/night_pipeline.py --rounds=2 --pools=300,500,all`（**19:12 首发 → 19:31 崩 → 19:58 已修并重启**） | **完成条件（唯一可靠判据）**：`ai_test/_night/_pipeline.log` 里出现 **「流水线结束」**。<br>⚠ **不要**只看 `_night_report.md` 是否存在 —— 历史试跑会留下陈旧文件（2026-09-12 已删过一次）；务必同时核对 `_pipeline.log` 的末尾时间戳 **晚于** `_night_report.md` 的修改时间 | ❌ |
| **0.5** | `[ ]` | **判读 Phase B 报告**：读 `ai_test/_night_report.md`，核心判据 = **剥风格后 `strip_calmar>0` 的比例**（全A 基线 **3/30=10%**）与「300/500 两池皆无效」占比 | 阻塞：0 未完成 | ❌ |
| **1** | `[ ]` | **池内轨道试 `--score_mode=new`**（L1 排序去掉 `ic_ir`；已标定与 L2 Calmar 相关性 `old +0.167 → new +0.668`）<br>★ **已获第二个独立证据（§8.24）**：同等保留量(66~70)下 `ic_l1` 精率 **21%（<基线25%，提升0.86×⇒有害）**，而低换手代理 **37%（提升1.50×）** | 阻塞：0 未完成（**Phase A 期间不许改引擎**）。**只传参，不改代码** | ❌ |
| **1.5** | `[ ]` | **池轨道换 L2 门槛（§8.24-⑤）**：`--min_pool_calmar=0.10`(保守)/`0.15`(精率86%) 取代 `--min_calmar=0.5`；并把 `--min_ic` 降到 0（它作门槛**负向**，提升 0.86×<1） | 阻塞：0 未完成。**只传参**（机器已就位）；⚠ 阈值来自 n=105 的离线标定，**只能定方向与量级** | ❌ |
| **2** | `[ ]` | **质量门前移到「生成后、L1 前」**（QuantaAlpha `FactorQualityGate` 思路：复杂度/冗余不合格就不进 L1/L2，省预算） | 阻塞：0 未完成 | ✅ |
| **3** | `[ ]` | **补「与因子库的最长公共子树」Novelty 度量**（QuantaAlpha `duplicated_subtree_size`，其阈值 5；**本项目的阈值必须先离线标定**，勿照抄） | 阻塞：0 未完成 | ✅ |
| **4** | `[ ]` | **补 `top_percent_plus_random` 亲本选择策略**（top 30% 保底 + 余量随机；显式的探索/利用配比） | 阻塞：0 未完成；依赖 5 的"语义单元"才能完全生效 | ✅ |
| **5** | `[ ]` | **语义单元 + 方向维度 + 跨方向优先交叉**（架构级，**治本**）：`StrategyTrajectory` 式的单元 = `假设 + 描述 + 表达式 + 代码 + 回测 + 反馈(含新假设) + 血缘 parent_ids`；变异=同方向最新一条；交叉=**优先跨方向** | 阻塞：0 未完成。依据 §8.21-⑤：**我们缺的不是"交叉算子"，是"可交叉的语义单元"** | ✅ |

### 判断分支（0.5 做完就定路线）

- **300/500 的剥风格存活率明显高于全A 基线 10%** ⇒ **池内挖掘有效**
  ⇒ 先加轮数（`night_pipeline.py --rounds=3`），再按 1→2→3→4→5 推进。
- **三者差不多（都≈10%）** ⇒ **「池」不足以改变搜索行为**
  ⇒ **跳过 1、2**，直接做 **4（亲本选择）** 与 **5（语义单元+跨方向交叉）**——
  因为问题在"搜索结构"而不在"排序分/门前置"。

---

## 已知事实快照（省得每次重查）

| 项 | 值 |
|---|---|
| 挖掘池 | `--mine_pool=all\|300\|500`；`all`=现有无后缀文件（历史全A轨迹）；`300/500`=全新独立轨迹（文件加 `_300`/`_500`） |
| 池面板规模（实测） | 300 池 L1 列 **925**、当期 PIT 成分中位 **299**；500 池 **1776** / **499** |
| L1 实测速度 | 300 池 **约 0.42~0.57 s/候选**（全A 约 0.93）⇒ **20~40 min/轮/池** |
| 全A 基线（§8.13，30 个入库因子） | 剥风格后超额为正 **3/30=10%**；`csi_all_only` **10/30=33%**；**`lnamt`/`lntr` 为负 30/30**；十档单调性 ≥0.9 占 27/30 |
| ⚠ 池内 IC 更低是**正常的** | 截面样本少（300/500 只）+ 无小盘溢价 ⇒ **`--min_ic` 等门槛需重新标定，勿照搬全A 的 0.02** |
| 目标函数现状 | **L2 已是超额口径**（`--min_calmar=0.5` = 超额年化/最大回撤、`--seg_need=2` 分段超额>0）<br>**L1 仍是 IC 口径**（`--min_ic=0.02` + `--score_mode=old` 含 `ic_ir`） |
| ★ 新观察（2026-09-12 19:4x，300 池 L2 早期） | 候选 `[2]`：**池内 IC=+0.0609（高于历史全A 候选）但费后超额 −2.42% / Calmar −0.07**；另一候选 `[1]` IC=+0.0395 而超额 +0.89%。⇒ **"IC 高 ≠ 能赚钱"在池内同样成立**，再次支持 §8.19 的结论（IC 不是好排序量）；也提示**池内 IC 与池内超额的相关性值得在 §8.23 量化一下** |
| ★★ **池内门槛标定结果（§8.24，2026-09-12 22:3x，n=105）** | 真值 = `strip_calmar>0`（基线 **26/105=25%**）。**相关性**：`ic_l1`（L1 现用）**−0.249\*\*** / `cal_all` +0.314\*\* / **`cal_pool` +0.598\*\***（最强）。**提升(lift)**：`cal_pool≥0.15` ⇒ 留7/精率**86%**/提升**3.46×**；`cal_pool≥0.10` ⇒ 留9/78%/3.14×；`turn_low≥−0.05`(组合换手≤5%) ⇒ 留10/40%/1.62×。**⚠ `cal_all≥0.5` 的 4.04× 是"只留 2 个"的假象**（必须看**同等保留量**下的提升）。**★ 正面对比**：留 66~70 个时 `ic_l1` 精率 **21%**（低于基线、提升 **0.86×**）= **有害**，而 `turn_low≥−0.2` 精率 **37%**、**召回 100%、提升 1.50×**。⇒ **L1 的 IC 该换掉**（第二个独立证据）；落地 = `--score_mode=new`。 |

---

## 进度日志（append-only，最新在最后）

```
2026-09-12 19:12  Phase A 启动（night_pipeline.py --rounds=2 --pools=300,500,all）
2026-09-12 19:12  pool=300 gen=1 开始（全新轨迹，从 gen1 起）
2026-09-12 19:22  取消旧自动化 loop-gen50（PAUSED + validUntil=2026-09-11 已过期）
2026-09-12 19:2x  pool=300 gen=1 L1 560/800（318s，约 0.57s/候选），err 空
2026-09-12 19:23  建立本等待清单 + 新建「挖掘待办轮询者」自动化（每小时一次，id=automation）；
                  顺带删掉试跑遗留的陈旧 ai_test/_night_report.md（会让轮询者误判 Phase A 已完成）
2026-09-12 19:31  ⚠ 事故: pool=300 gen=1 崩于 UnboundLocalError(保存状态处, 整代末尾)。
                  根因: `n_tested=st.get(...) if os.path.exists(STATE)` 的条件在 `open(STATE,'wb')` 之后求值
                  ⇒ 文件已被创建 ⇒ 条件恒真 ⇒ 全新轨迹下 st 未绑定。详见 roadmap §8.23。
                  破坏: loop_state_300.pkl 被截断成 0B；观测 CSV 已落盘(数据保住)；journal 已写"第1代"
2026-09-12 ~20:0x 修: ①n_tested 基准提前到载入处 ②保存改**原子写**(tmp+os.replace, 根治 0B 坏状态)
                  验证: 用**一次性池 50** 冒烟(--mine_pool=50 --n=30 --l2=1) 走完"全新→保存"全路径 ✓
                  现场: 失败半成品挪 `ai_test/_failed_runs/`(留证), 0B state 删除, all 池文件未污染
2026-09-12 19:58  流水线重启(300→500→all × 2轮)。⚠ 期间**不要改 loop_engine.py**(每轮新起进程)
2026-09-12 20:17  300 gen1 END 退出码=0 19.2min err=0B ✓
2026-09-12 20:38  300 gen2 END 退出码=0 20.9min err=0B ✓  ← 300 两轮完成, state 115KB 正常落盘
2026-09-12 21:03  500 gen1 END 退出码=0 25.6min err=0B ✓
2026-09-12 21:37  500 gen2 END 退出码=0 33.5min err=0B ✓  ← 500 两轮完成
2026-09-12 21:37  all gen72 START (既有 71 代 -> 接第 72 代)
2026-09-12 22:1x  ★ 初步判读(300/500 数据完整; all 还在跑) —— 见下方「初步判读」
2026-09-12 22:20  all gen72 END 退出码=0 42.8min ✓ ; all gen73 START
2026-09-12 23:07  流水线结束(all gen73 也完成) + Phase B 自动出 _night_report.md ✓
2026-09-12 22:3x  ★★ 池内门槛标定完成(§8.24): ic 负相关 −0.249*、提升 0.86× = **有害**
2026-09-12 23:31  ★ 初步判读(300/500 完整): §8.24 结论已写入
2026-09-12 23:35  ★ 验收轮启动(接力脚本 chain_validation.py, detached):
                  --extra="--min_calmar=0.0 --min_ic=-1 --min_pool_calmar=0.10 --score_mode=new"
                  300 gen3 / 500 gen3 / all gen74 各 1 轮 ⇒ 预计 01:05 完成 ⇒ 自动出 _valid_report.md
2026-09-13 01:29  ★ 验收轮完成(114min, 退出码=0), 自动出 _valid_report.md ✓
2026-09-13 09:3x  ★ 验收判读(§8.25): **入库率没动(300: 2%→0%, 500: 2%→3%)**,
                  但**候选质量暴涨**(池内有效 300: 42%→77%, 500: 33%→63%; 500 剥风格 18%→47%)
                  ⇒ L1 改造有效, 瓶颈已转移到 **L2 门槛组合**
                  逐门诊断(diag_gate_chain.py)定位两个主卡点:
                    ①**`sharpe > 0.5`(硬编码不可配)** —— 300 砍掉 10/12(83%)、500 砍 12/17(71%)
                    ②`池内 Calmar ≥ 0.10` AND 上全A门槛后, 300 从 2 个砍到 **0**
                  ⚠ 根因是我在 §8.24 的方法论错误: **逐门标定 ≠ 多门组合**(AND 后保留率≈乘积 ⇒ 归零)
2026-09-13 09:3x  ⚠ **轮询者自动化整夜 5 次全部 `Run timed out`(每次跑满 90 分钟、零产出)**
                  ⇒ 已 **PAUSED**; 而 detached 的 chain_validation.py 全程正常
                  ⇒ **结论: detached 脚本链可靠, app 调度器自动化不可靠**(§8.25-⑦)
2026-09-13 09:4x  ★★ 组合标定 + 落地 OR 语义(§8.26):
                  · 深挖: 池内有效与全A 有效**基本不同源**(300 池"只在池内有效"31 个
                    vs "两者都有效"15 个) ⇒ 池门槛 AND 上去**逻辑自相矛盾**
                  · 组合标定(n=167): AND 最优只留 5/167(召回8%); **OR 可留 41/167、
                    精率59%、召回49%、提升2.00×** ⇒ 保留量约 **6 倍**
                  · 落地: 新增 `--min_sharpe`(默认0.5=原硬编码值, 行为不变)
                    + `--pool_gate_or_all`(默认关) ⇒ QA[2b] 7 项断言 + 冒烟 全过 ✓
                  · ⚠ **事故**: 重复跑一次性 `_cleanup_prerun.py` ⇒ 误挪池数据 + 删
                    `loop_state_{300,500}.pkl`。数据**已完整恢复**(_restore_pools.py),
                    **state 不可恢复** ⇒ 300/500 的 gen4 将以"空状态"起跑(无变异/交叉亲本)。
                    已给该脚本加**一票否决式保护**。
2026-09-13 09:44  ★ v2 验收启动: --min_calmar=0.0 --min_ic=-1 --score_mode=new
                  --min_pool_calmar=0.15 --pool_gate_or_all  (300 gen4 / 500 gen4 / all gen75)
                  预计 ~2 小时; 报告切分 `valid_report.py --new_from=4`
2026-09-13 10:0x  ★★ 无人值守闭环建成(§8.27) —— 用户要求「到时候直接判读并继续推进」:
                  ① `chain_validation.py` 跑完验收轮 -> `_valid_report.md` -> `interpret_valid.py`
                     -> **`ai_test/_next_action.md`（结论 + 下一步的确切命令）**
                  ② 另起 `chain_interpret.py`（detached）等**新一次**「接力结束」再判读（兜底）
                  ⚠ 踩坑: 判"有没有新的一次完成"不能写成"出现过就算完" ——
                    `_chain.log` 是 append-only、里面**已有** v1 的「接力结束」⇒ 会立刻误判。
                    (与 §8.26 的"空闲判定"**判据方向相反**, 别混。)
```

### ★★ 人读顺序（全部自动产出，不需要任何会话在场）

1. **`ai_test/_valid_report.md`** —— 按 gen 切分的对照表（基线 gen1-2 / v1 gen3 / **v2 gen≥4**）
2. **`ai_test/_next_action.md`** —— **结论 + 下一步的确切命令**（决策树已写死在 `interpret_valid.py` 里）

**判读决策树**（核心指标 = 入库率，基线 **2%**）：

| 分支 | 条件 | 结论 | 下一步 |
|---|---|---|---|
| **A** | 入库率 ≥10% | ✅ 门槛改造成功 | 加轮数（`--rounds=3`）+ 转 §8.21-⑥ **#2**（质量门前移到 L1 前） |
| **B** | 3%~10% | 🟡 部分有效 | 先试 `--min_pool_calmar` 0.15→**0.10**；仍不理想转 **#4**（`top_percent_plus_random`） |
| **C** | <3% | ❌ 门槛不是瓶颈 | 转 **#5**：**语义单元 + 方向维度 + 跨方向优先交叉**（治本） |
| 特例 | `all` 明显更好 | ⚠ 是 300/500 的**空状态**拖累（§8.26-⑥ state 被误删） | 先补跑 2~3 轮恢复演化，**再重判** |

### ★ 最新门槛取值（2026-09-13，§8.26 组合标定结论）

```
--min_calmar=0.0          # 原 0.5（池内候选的全A Calmar 天然低）
--min_ic=-1               # 关掉（§8.24: ic 与真信号负相关 −0.249*、提升 0.86×<1 = 有害）
--score_mode=new          # L1 排序去 ic_ir（以 stab=低换手代理为核心）
--min_pool_calmar=0.15    # 池内 Calmar 门槛（§8.26 组合标定）
--pool_gate_or_all        # ★ OR 语义: 全A 口径达标 **或** 池内达标（替代原 AND）
--min_sharpe=0.5          # 新参数, 默认值 = 原硬编码值（本轮回退到 0.5）
```

---

## ★ 下一步（2026-09-13，见 roadmap §8.25-⑤）

| 优先 | 动作 | 理由 | 需改代码 |
|---|---|---|---|
| **1** | **把 `sharpe > 0.5` 变成可配置 `--min_sharpe`**，并对池轨道标定 | **最大卡点**（砍 71~83%），而 §8.24 实测它提升仅 **1.01×（≈噪声）** | ✅ |
| **2** | **池轨道改 OR 语义**：`全A Calmar>0` **或** `池内 Calmar≥t`，而不是 AND | 对"只在池内有效"的因子，同时要求全A 达标**自相矛盾** | ✅ |
| **3** | **门槛做「组合标定」**（网格搜联合精确率/保留率），不要逐门标定后叠加 | §8.25-④ 的教训 | ❌（新脚本） |
| **4** | 深挖「池内有效」的那批候选（300 有 23/30、500 有 19/30） | **数据已在** `docs/loop_pool_obs_*.csv`，不必等下一轮 | ❌ |


### ⚠ 验收轮的一个方法论缺陷（必须一并说明）

gen3 除了**门槛变化**，还叠加了**引擎自适应参数变化**（B角 依据 gen2 诊断把 `depth` 从 `[2,3,4]`
调到 `[3,4,5]`、`mix` 也可能变）。⇒ 「gen1-2 vs gen3」的对比**是混合变量**，
**不能把入库率变化全部归因于门槛**。若差异显著，需再跑一轮"仅改门槛、冻结自适应"来隔离。

---

---

## 初步判读（2026-09-12 22:1x，300/500 两轮已跑完；all gen72 进行中）

**★ 核心判据：答"能"—— 但门槛需重标定。**

| 池 | L2 候选 | 入库 | 剥风格 Calmar>0 | **剥风格存活率** | **300内>0** | **500内>0** | 仅两池皆无效 |
|---|---|---|---|---|---|---|---|
| all(gen71) | 21 | — | 11 | 52% | **1 (5%)** | 1 | 19/21 (90%) |
| **300** | 50 | 1 | 15 | **30%** | **21 (42%)** | 11 | 25/50 (50%) |
| **500** | 49 | 1 | 9 | **18%** | 11 | **16 (33%)** | 32/46 (70%) |

1. **★★ 池内挖掘确实"转向"了**：300 池挖的因子在 **300 内有效 42%**（全A 历史仅 **5%**）、
   500 池挖的在 **500 内有效 33%**（全A 历史 5%）⇒ **提升 7~8 倍**。「仅两池皆无效」300 池最低(50%)。
2. **⚠ 但绝对水平仍低**：`Calmar 中位` = 300 **−0.073** / 500 **−0.013**（剥风格后同为负）⇒ **入库各仅 1 个**。
   原因：`--min_calmar=0.5` 是**为全A 标定**的，池内天然达不到（正是 §8.20-⑦ 的警告）
   ⇒ **门槛必须按池重新标定**，否则入库率≈0。
3. **⚠ 反向信号待解释**：**剥风格存活率反而低于全A**（300 30% / 500 18% vs all-gen71 52%）
   —— 池内挖掘把"池内有效性"提上去了，却没提升"剥掉 lncap+lnamt 后还剩多少"。
   猜测：池内挖出的因子更依赖**池内特有结构**；但也可能是 all 侧样本太小(21)。
   ⇒ **等 all gen72 跑完（会加 ~25 个样本）再判**。
4. **⚠ 报告口径提醒**：表中各行统计的是 **L2 候选**，而 §8.13 的 10% 基线是 **入库因子** ⇒ **不可直接比**。
   已在 `night_report.py` 输出里加了口径说明。
5. **✓ 顺手修掉一个静默 bug（与 §8.15 同一个坑）**：`loop_pool_obs*.csv` 的 `pool` 列写入是 `'300'`，
   **CSV 往返后 pandas 读成 int64** ⇒ 用 `'300'` 查表全部落空 ⇒ `300内>0/500内>0` **静默全为 0**。
   修法：读入即 `astype(str)`。**⇒ 跨文件键的类型必须显式统一（已在 MEMORY.md 记录过，这次又踩）。**

---

## 无人值守接力机制（2026-09-12 建立）

**问题**：AI 在对话结束后会退出，而自动化跑的是**全新会话**（不继承对话上下文）。
**方案**：①**本文件**作为跨会话任务真相源；②一个**轮询者自动化**按纪律逐轮推进。

| 部件 | 说明 |
|---|---|
| 任务真相源 | **本文件**（`docs/loop_todo.md`）—— 任何新会话**先读这里** |
| 轮询者 | 自动化 **`id=automation`（挖掘待办轮询者）**，`FREQ=HOURLY;INTERVAL=1`（**每小时一次**），ACTIVE |
| 长跑本体 | `ai_test/night_pipeline.py`（detached 进程，**不依赖任何会话**，自己会跑完并出报告） |
| 互斥 | 本文件「当前占用」+ 40 分钟时间戳（超时/崩溃留下的过期占用会自动失效） |
| 有界 | 每轮 ≤10 分钟；做不完就把进度写回本文件（自动运行**有超时**，长跑会丢进度） |

### ⚠ 自动化本身的踩坑（已实测，记录以免重犯）

1. **`automation_update` 没有 delete 模式**（只有 view / suggested create / suggested update）
   ⇒ 「取消」= `status=PAUSED` + **`validUntil` 设为过去日期**（= 已过期 ⇒ 永不执行），并把 name/prompt 改成「已取消」+写明原因。
2. **改 `scheduleType` 不生效**：`once` 的自动化改成 `recurring` 会**静默保留原 `once`**
   ⇒ 要换调度类型只能**新建 + 取消旧的**。
3. **`id` 由 name 派生（会随改名而变）** ⇒ 改名后用旧 id 会 `Automation not found`。
4. **多值 `BYMINUTE` 会被错误展开**：`FREQ=WEEKLY;...;BYHOUR=<24个值>;BYMINUTE=0,20,40`
   实测 `nextRunAt` 落到 **5 小时后（00:20）而不是 20 分钟后**（旧 `loop-gen50` 同症状：落到 00:00）
   ⇒ **只用文档里明确支持的最简形式**：`FREQ=HOURLY;INTERVAL=1`（实测 `nextRunAt` 正好 +1 小时 ✓）。
5. **存储位置**：`C:\Users\Administrator\AppData\Roaming\CodeBuddy CN\automations\automations.db`（SQLite，带 -wal/-shm）
   ⇒ **不要手动改它**（IDE 在用，并发写有损坏风险）。

**当前自动化清单**：`automation`（轮询者，ACTIVE） / `loop-gen50`（**已取消**：PAUSED + 已过期）。

---

## ★★★ 当前路线（2026-09-13 决定；详见 roadmap §8.33/§8.34）

**诊断**：「打转」的真身 **不是表达式重复，而是收益流重复**
—— 库内 30 个入库因子的**组合收益流两两相关中位 `0.967`**（`ai_test/test_combine.py`），
而结构层面极多样（骨架 1181 唯一、Top5 仅 1.9%，`analyze_diversity.py`）
⇒ **换叶子/换窗口/换外壳，赚的都是同一块钱**。
⇒ 这也解释了为什么**改门槛 / 改排序量 / 改池**都动不了结果 —— 它们全在**选择**阶段，问题在**生成**阶段。

**★ 核心 KPI = 「库的平均两两收益流相关」**（工具 `ai_test/library_kpi.py`，**秒级、不需面板**）
　现状 **~0.967** → 目标 **<0.7**。**这个数不降下来，加再多因子都没用。**

| 步 | 动作 | 状态 |
|---|---|---|
| **0** | 立 KPI（`library_kpi.py`） | ✅ 已建（**秒级，不需面板**） |
| **1** | 止血：`--dup_ex_corr`（收益流去重） | ✅ 已实现 + QA 全过（§8.34）；**降级为低优先** |
| **2** | ~~造血：§8.21-⑥ #5 语义单元~~ | **⛔ 暂缓**（库独立性没问题：`ex` 相关 0.435） |
| **★★ 新 1** | **多因子合成（组合层）** | ✅ **已完成**（§8.36~§8.39）：neu-B freq=5 超额 **+6.79%** / Calmar **1.193** / 剥风格后仍 **0.673**；6 个因子文件 + 配方 + 选股脚本 `combo_score.py` 全部就绪 |

### ★★★ 2026-09-13 18:30 更新：合成做完了，瓶颈已转移

**手上已有的东西**：neu-B 合成因子（freq=5）超额 **+6.79%**、Calmar **1.193**、夏普 1.484、
换手 17.8%、9 年 8 年正、十档**单调 +1.000**、**剥掉市值+成交额后仍有 Calmar 0.673**。

**⇒ 所以瓶颈不再是"引擎产不出 alpha"，而是这三条**：

| 瓶颈 | 证据 | 性质 |
|---|---|---|
| **A. 300/500 轨的库几乎空** | 只有 **1 / 0** 个因子 ⇒ **无法在 300 池内合成** ⇒ 这就是"300 增强做不了"的直接原因（合成因子在 300 内超额 −1.72%） | **时间/调度**，非架构 |
| **B. 合成因子仍偏小盘** | 选中股票市值分位仅 **15%**（raw 版 1.7%）⇒ 不加以约束不能产品化 | 组合构建 |
| **C. 绝对回撤 −33.7%** | 主要是**市场 β**（超额回撤只 −5.7%）⇒ 形态选择问题（增强 vs 中性） | 产品定位 |

**⇒ 修正后的优先顺序**

| # | 动作 | 类型 | 成本 |
|---|---|---|---|
| **1** | **让 300/500 池内轨道持续跑**（攒池内因子）—— 直接解 A；§8.31 已证池内挖掘有效（池内 Calmar>0 率 42%→72%）。⚠ 先跑 1~2 轮看**池内入库率**（池内 IC 尺度不同，§8.20-⑦ 提示门槛需重标定） | **调度，零人工** | 启动即可 |
| **2** | **组合构建约束**（市值/行业中性 + 权重上限 + 换手约束）—— 解 B | 代码（组合层） | 1-2 天 |
| **3** | **引擎改造 §8.21-⑥ #5**（语义单元 + 方向 + 跨方向交叉） | 架构 | 数天，**可延后** |
| **4** | ⚠ **决定产品定位**（全A宽基增强 / 中证500增强 / 中性绝对收益）—— 决定 1/2 的参数 | 决策 | 0 |

**★ 为什么 #5 可以延后**：它要解决的是"库重复/打转"，而**实测库独立性 0.435（没问题）**
⇒ **问题不存在**；真正缺的是**池内因子储备**（时间问题）⇒ **先跑起来攒量，比改架构见效快**。

### ⚠⚠ 2026-09-13 重要纠正（roadmap §8.35）—— 上面"诊断"里的 0.967 是**错的**

我早先用 **`tr`（组合绝对收益）** 算相关得 0.967，据此说"库只有一个因子" —— **方法论错误**：
`tr = ex + 基准`，**基准对所有因子相同** ⇒ 必然趋近 1，**那是市场 β 的共性，不是因子重复**。
**正确口径是 `ex`（超额）**：

| 口径 | 中位 | p90 | >0.9 |
|---|---|---|---|
| `tr`（含β）×错误 | 0.967 | 0.983 | — |
| **`ex`（超额）✅** | **0.435** | 0.660 | **3/820 (0%)** |

且 §8.33 拿"合成 vs **事后挑出的最好单因子**"比也是错的（**选择偏差**）。改成与**个体中位**比：

| | 年化超额 | **Calmar** | 夏普 |
|---|---|---|---|
| **等权合成(41)** | **+5.47%** | **0.897** | **1.323** |
| 个体中位 | +4.85% | 0.506 | 0.838 |
| 个体最好*（事后挑） | +14.30% | 1.162 | 1.405 |

⇒ **合成相对个体中位：Calmar `1.77×`、夏普 `1.58×`** ⇒ **合成明显有效**。

### ★★ 因此当前第一优先：**多因子合成（组合层）**

- **实测**：等权合成已是**产品级候选**（年化超额 +5.47% / Calmar 0.897 / 夏普 1.323）
- **成本**：~1-2 天，组合层工作，**不动引擎生成/选择逻辑**
- **待做**：去相关筛选 + 权重（等权/IC加权/风险平价）+ **低换手约束**（换手 corr 全A −0.242，值得顺手做）+ 实盘约束（涨跌停/流动性）
- **⚠ 说明**：现有 `evaluate_real` / 门槛 / 入库判据 / 池门槛 **全在单因子层面** ⇒
  **"一直逐个评估单因子、从没合起来"正是单因子弱却没发现库整体不错的原因**。

**⚠ 方法论教训**：①算"因子是否重复"必须用 `ex` 而非 `tr`；②评估组合策略别拿"事后最好的个体"当基准。

**⚠ 顺带已知的可优化项**：**换手**（corr(换手,费后超额) 全A Spearman **−0.242**；最低换手档
费后 +1.97%/Calmar 0.149 明显最好）。成本约 1.3%/年 ⇒ **不是主因，但值得在组合层顺手做**
（持仓缓冲 `n_drop`、拉长持有期）。


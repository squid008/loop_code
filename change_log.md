# Change Log / 变更日志

本文件**事无巨细**地记录每一次改动。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本 SemVer](https://semver.org/lang/zh-CN/)：
**MAJOR**（不兼容改动）· **MINOR**（向后兼容的功能新增）· **PATCH**（向后兼容的修复）。

> **改动 → 版本 → tag 的动作约定**（沿用 README「版本与回退」）：
> 每次改动 = **一次 commit**；每个版本 = **一个 annotated tag**（`git tag -a vX.Y.Z -F <说明>`）。

---

## 本文件与 `docs/factor_roadmap.md` 的分工

两份文档**相似但职责不同**，不要互相替代：

| | **`change_log.md`**（本文件） | **`docs/factor_roadmap.md`** |
|---|---|---|
| **定位** | 变更台账（审计/回退用） | 研发档案 / 开发手册（复盘/传承用） |
| **记什么** | **每一次改动**：新增/变更/修复/移除，含文件名 | **大改动、踩坑、经验、结论、事后纠正** |
| **粒度** | 事无巨细，逐提交 | 只记**值得复盘**的 |
| **小修小改** | ✅ 都写 | ❌ 不写 |
| **写完后** | 不回头改 | **可被后续章节推翻/纠正**（如 §8.35 推翻 §8.33） |
| **失败/负结果** | 只记"改了什么" | **重点记"为什么错、怎么发现的"** |
| **给谁看** | 想知道"这版动了什么、怎么回退"的人 | 想理解"为什么这样设计、踩过哪些坑"的人 |
| **章节号稳定性** | 版本号即索引 | ⚠ **`§8.x` 编号被 451 处引用**（代码注释/文档）⇒ **只增不改、不重编号** |

**实践口径**：一次改动先在 `change_log.md` 落一行；若它**改变了引擎行为 / 推翻了一个结论 / 踩了新坑**，
再在 `roadmap` 里写一节（带证据与复盘）。纯重命名、纯格式、纯脚本搬移 → 只写 changelog。

---

## [Unreleased]

（下一次改动写这里，发布时整段移到新版本号下）

---

## [0.16.0] — 2026-09-15

> 主题：**架构清扫 P0-1 —— 算子「单一事实源」**（并顺手修掉**两个正在漏水**的真 bug）
> 起因：用户问「**你经常出错又自己修正，在别的项目不这样 —— 是不是项目架构太乱/太零碎？
> 从软件工程角度打分，是不是该先清扫？**」

### ① 先量化（新增只读体检：`tools/_audit_codebase.py` / `_audit_coupling.py` / `_audit_deadcode.py`）

| 维度 | 实测 | 判定 |
|---|---|---|
| 规模 | **184 个 .py / 32,042 行** | 尚小 |
| ★ 单点巨人 | `engine/loop_engine.py` **3,134 行 / 177 KB**，其中 **`run()` 一个函数 1,291 行** | **P0** |
| ★ 副本 | **加 1 个算子要同步改 10 处**（4 个代码文件 + 6 个文档）；同一名单被抄 **4 份** | **P0** |
| 重复函数体 | **24 组**（`to_int_date` ×9、`market_temperature` ×5…） | P2（历史研究代码）|
| 同名常量各自定义 | `ENG` ×9 · `BARRA` ×8 · `SRC` ×7 · `OUT_CSV` ×7… | P2 |
| 文档体积 | `factor_roadmap.md` **4,510 行 / 312 KB**（~69% 是开发日志） | P1 |

**八维评分** ⇒ 结论：**不是"混乱"，是"高质量但未拆分的单体"**：

| 功能性 | 可测试性 | 工程纪律 | 性能 | 可读性 | 可移植性 | 文档 | **可维护性** |
|---|---|---|---|---|---|---|---|
| **8** | **8** | **9** | 6 | 6 | 5 | 5 | **4** ← 最低 |

**综合 ≈ 6.4 / 10** —— **纪律/测试是 A 级，结构是 C 级**。
> ★★★ 所以"出错多"**约 6 成归架构、4 成是我自己的操作失误** —— **不能全甩锅给项目** ✓

### ② 做了什么（只做**最高杠杆、最低风险**的 P0-1）

**新增 `engine/ops_registry.py` = 算子**唯一声明处**（含 `SLOW_OPS` 与 prompt 名单生成）**

```
engine/ops_registry.py    ← ★ 声明一次：56 单目 + 10 双目 + SLOW_OPS + prompt 名单
        ↓ 派生（**结构上不可能漂移**）
loop_engine.UNARY/BINARY  ·  loop_critic.SLOW_OPS  ·  loop_llm 的 prompt（**占位符注入**）
```

| 项 | 改造前 | 改造后 |
|---|---|---|
| 加 1 个算子要改**代码**文件 | **4 个** | **1 个**（`ops_registry.py`）|
| `loop_engine.py` 的算子表 | 13 个 helper + ~90 行 `UNARY` + `BINARY` 字面量 | **4 行接线**（`build_unary/build_binary`）|
| prompt 的算子/叶子名单 | **手写 ×2 份副本** | **占位符 `{{OPS_*}}`/`{{LEAF_*}}` 运行时注入** |

`loop_engine.py` **净减约 150 行**（206 行区域 → 55 行接线）✓

### ★★★ Fixed（**本次最有价值的产出**）：两个**正在损害效果**的老 bug

做第 3 步（把 prompt 也改成注入）时，升级后的 `_test_ops_sync [3]` 立刻报出：

```
engine/skills/gen_skill.md  ← _load_skill() 优先读它 ⇒ 【运行时真正生效】的那份
   缺 21 个算子：ema*(5) + ts_slope/ts_rsqr/ts_resi*(12) + ts_skew/kurt*(4)
   缺  7 个叶子：fa_pb/fa_accrual/fa_asset_turn/fa_gw/fa_inv_turn/fa_recv_turn/fa_sell_exp
                             ↑ 正是 v0.15.0「财报叶子 8→15」新增的那 7 个
_GEN_SYSTEM_FALLBACK        ← 只在 .md 缺失时才用（**没生效**），反而是全的
```

⇒ 而 prompt 又**硬性要求**「窗口必须是上述枚举值」「禁止出现叶子字段以外的名字」
⇒ ★★★★ **A角 LLM 即使想用，也会被自己的规则拒掉** ⇒
 **v0.13.0/v0.13.1 的 16 个算子 + v0.15.0 的 7 个财报叶子，对 A角 语义引导实际从未生效** ✗✗

> ★★★ **教训①**：**「文件存在就用它」的外置副本 = 最容易过期的副本** ——
> 改 prompt 时通常只改源码里那份，忘了 `.md`（v0.15.0 的 Notes 只改了
> `build_fa_pit.py`/`loop_fields.py`/`fa_pit.h5`，**完全没碰 `gen_skill.md`**）。
> ★★★ **教训②**：**旧测试查不出来** —— 它 grep 的是 `loop_llm.py` 的**源码**（= **回退份**），
> 恰好**不是生效的那份** ✗ ⇒ 已改为测 **渲染后的真实产物**，且**两份都测** ✓

### Changed

- `engine/ops_registry.py`（**新增**）· `engine/loop_engine.py`（表改派生 + 清死代码）·
  `engine/loop_critic.py`（`SLOW_OPS` 改派生）· `engine/loop_llm.py`（占位符渲染机制）·
  `engine/skills/gen_skill.md`（名单改占位符 + **补回缺失的 ema/回归/高阶矩说明行**）
- `tools/_test_ops_sync.py`：**[3] 改为测渲染后产物（两份都测）** · 新增 **[6] 叶子覆盖** ·
  `ALLOW` 更新为「`ops_registry.py` = 唯一声明处」（22 → **28** 项）
- `tools/_test_ops_registry.py`（**新增 16 项**）：★ 含 **"只改一处就够"的行为证明** ——
  临时插一条算子（`ts_mean137`）⇒ **引擎表 + A角 prompt 双双自动生效**，并在 `finally` 清理还原 ✓

### Removed

- `loop_engine.py` 里 **4 个死函数**（`ts_max_op`/`ts_min_op`/`ts_corr20_op`/`ts_corr60_op`）
  —— `_audit_deadcode.py` 实测**本文件内外均无引用**，且与 `UNARY` 里的同名算子重复
- `loop_engine.py` 里 **13 个 helper**（`_m/_s/_r/_mx/_mn/_sm/_cr/_e/_sl/_rq/_rs/_sk/_ku`）
  —— 注册表**直接绑定 `fastops`**，不再需要中间层
- `ai_test/_verify_ops_registry_equiv.py` · `ai_test/_apply_ops_registry_wiring.py`（一次性脚本，使命已完）

### 验证

- ★★ **先抓"金标准快照"再动刀**（`ai_test/_ops_oracle_before.json`）：`UNARY=56 / BINARY=10 / SLOW_OPS=9`
- ★★ **逐位等价**：注册表构建的算子 vs 改造前引擎算子 ⇒ **56+10 个全部 `allclose(atol=0, rtol=0)`**
  （**不是"看起来一样"**，是逐位相同，含 NaN 位置）✓
- **真机冒烟 4/4** · 全部 `loop_state*.pkl` **SHA256 未变** ✓
- 全量：编译 **83** 个文件全过 · 引号全绿 · **11 个回归测试全绿**（含新 16/16、升级后 28/28）

### ⚠ 诚实遗留（不粉饰）

- ⚠ **一处有意的顺序变更**：旧 `UNARY` dict 的**插入顺序**是历史产物（短窗先行、长窗后补），
  注册表改用**自然的族顺序** ⇒ 而 `list(UNARY.keys())` 被 `rand_expr`/`mutate`/`_mix_weights`
  当**随机选择池** ⇒ **同一 RNG 种子会选到不同算子** ⇒ **候选流与旧版不同**
  （**算子集合逐字相同**）⇒ 这**可接受**（版本升级本就变了算子集），但**不能声称"零行为变化"** ✗
- ⚠ **P0-2（拆 `run()` 1,291 行）尚未做** —— 它是"改主循环时上下文不足"的**直接根因**，
  收益比 P0-1 更大，但需"纯提取 + 回归兜底"，**单独一轮做更稳** ✓
- ⚠ P1（文档分家：`factor_roadmap.md` 4,510 行）与 P2（`research/`+`strategies/` 去重）未做

---

## [0.15.0] — 2026-09-15

> 主题：**财报叶子扩展：8 → 15 个**（用户问「财报数据不是有一堆吗？什么 ROE、PB、归母净利润同比」）

### ① 盘点结论：我们**用了 8 个 R 量纲比率**，而**源里有 ~394 个科目**
- 现况（`loop_fields.FA_LEAVES`）：`fa_np_yoy`（**归母净利润同比**）· `fa_rev_yoy` · `fa_op_yoy` ·
  `fa_ocf_yoy` · `fa_gm` · `fa_np_margin` · **`fa_roe`** · `fa_lev`
- **PB**：⚠ 原**没有** —— 角色由 BARRA `barra_book_to_price`（PB 倒数）承担 ⇒ **本次补上** ✓
- 源：`E:\rq\finance\pit\*.h5`（**5567 只股票各一个文件**，`fields` 组 ~394 科目）
- ★ `build_fa_pit.py` docstring 写明「**不注册绝对额/绝对股本等非 R 量纲**」是**刻意的**
  （绝对额与 `mktcap` 强相关 ⇒ 挖出规模因子）⇒ **本原则保留** ✓

### Added（+7 个叶子，总 15 个）
| 叶子 | 含义 | 与旧叶子的**截面** rank 相关（最高）|
|---|---|---|
| **`fa_pb`** | **PB**（归母权益 / 总市值，**按日 mktcap**）| **+0.063** ★★ **最独立** |
| `fa_accrual` | 盈利质量 `ocfTTM / npTTM` | +0.288 |
| `fa_asset_turn` | 资产周转 `revTTM / total_assets` | +0.265 |
| `fa_gw` | 商誉占比 `goodwill / total_assets` | +0.138 |
| `fa_inv_turn` | 存货周转 | +0.185 |
| `fa_recv_turn` | 应收周转 | +0.184 |
| `fa_sell_exp` | 销售费用率 | +0.538 |

### ⚠ Removed / 试过但**删掉**的 5 个（两类原因，都实测过）
**(a) 覆盖率不够**（判据：近 3 年 < 30%）
- ✗ `fa_int_cov` 利息保障 —— 有效 **0.01%**（`interest_expense` 源里多为 NaN/0）
- ✗ `fa_rnd` 研发/营收 —— 有效 **9.95%**（A 股研发披露不全）
- ✗ `fa_rnd_cap` 资本化研发比例 —— 有效 **2.29%**
**(b) 信息重复**（判据：**逐日截面** rank 相关 > 0.85）
- ✗ `fa_roe_wa` 加权平均 ROE —— 与 `fa_roe` 截面 **+0.855**；⚠ 且其口径是「**报告期累积**」
  不是 TTM（实测按月底截面中位 Q1 0.015 → 中报 0.018 → Q3 0.037 → 年报 0.053 呈阶梯）
  ⇒ **`ts_delta` 会读出"假变化"** ⚠；而 `fa_roe` 已是 TTM（无锯齿）⇒ 留它更干净
- ✗ `fa_roa` 总资产收益率 —— 与 `fa_np_margin` 截面 **+0.867**
  （且 `fa_roe_wa ~ fa_roa = 0.807` ⇒ roe/roe_wa/roa/np_margin 是**同一族**）

### ★★ Fixed：两处**会产生静默错误**的实现问题
1. 🔴 **科目缺失从「整只跳过」改为「容忍」**
   旧版 `if not all(have): continue` ⇒ **任一新科目缺失就整只股票被跳过** ⇒ 覆盖率暴跌 ✗
   现在：缺的科目置 NaN，其余照算 ✓
2. 🔴 **`fa_pb` 必须特殊处理**：其它叶子的分母都与时间无关（`snap` 常量），
   而 PB 的 **`mktcap` 是日变的** ⇒ 必须在**按日填充**时算，不能进 `_fact` 的标量返回 ✓

### ★★ 方法学：判重复必须用**逐日截面 rank 相关**，不能用 pooled
- pooled（全样本堆一起）会**虚高** —— 混进了"跨时间的水平变化"（如全市场 ROE 逐年下滑），
  而那部分**截面选股用不到**；引擎的 IC/组合都是**同日截面比较** ⇒ 必须用截面口径 ✓
- 自检：旧的那两对"已知偏高"测得 `fa_np_yoy~fa_op_yoy = +0.877`、`fa_np_margin~fa_roe = +0.762`，
  与独立算法一致 ✓

### 验证（四层）
| 层 | 结果 |
|---|---|
| **旧 8 个逐值不变** | ★★ **最大差 0.000e+00 · NaN 掩码差 0 个** ⇒ 重建未破坏既有数据 ✓ |
| 清单一致 | `FA_LEAVES` ↔ `fa_pit.h5` ↔ `LEAVES`（61 个）↔ `loop_critic.LEAVES` 全部对齐 ✓ |
| 引擎可读 | `base_fields()` 15/15 在面板里（末期截面有效 50~99.9%）· 6 个含新叶子表达式解析+求值 OK ✓ |
| 回归 | 编译 **78** 个全过 · 引号全绿 · **10 个测试全绿** · 真机冒烟 **4/4** ✓ |

### ⚠ 一个检查方法上的坑（已更正）
- 我写「B角认得这些叶子吗」时查的是 `loop_critic.py` **源码里的字面量** ⇒ **误报 15 个"不认得"** ✗
- 真因：`loop_critic` 的叶子表是 `from loop_fields import LEAVES`（**import 来的**）⇒ 源码里当然没有
- ⇒ 必须查**运行时对象** `loop_critic.LEAVES` ✓（与"`_ops_of` 改成派生"是同一类教训：
  **派生出来的东西别去源码里找**）

### Notes
- 文件：`engine/build_fa_pit.py`（重写）· `engine/loop_fields.py`（+7 叶子，
  ⚠ **追加在末尾** —— 该文件注释写明"顺序即 `loop_critic` 结构去重的替换优先级"）·
  `engine/fa_pit.h5`（重建）
- ⚠ 叶子总数 54 → **61** ⇒ **搜索空间扩大**（与"窗口档位原则"同理）⇒ 已按
  「覆盖率 ≥30% + 截面相关 <0.85」两条判据**砍到只剩 7 个** ✓
- 重跑：停掉旧的 47 轮（叶子变了 ⇒ 跨代不可比），重建后**从当前代继续**

---

## [0.14.2] — 2026-09-15

> ⚠ **本文件补记**：发布时**漏写**（只改了 `tools/run_tracks.py`）⇒ 2026-09-15 事后补 ✓
> 主题：**回退 `--min_mono=0.75`**（跑完 3 轮 9 代实测：既不省成本、也无证据改善选人）
> 用户拍板：「**选a吧**」

### Removed
- `tools/run_tracks.py` 的 `extra` 移除 `--min_mono=0.75`
  ★ 并把**完整实测证据写进注释**（含「我标定口径错了」的更正 +
  「若将来要重开，**必须先修标定口径：在 L1 全量上算，而不是 top-N 30 个上**」）✓

### 实测依据
**(a) 不省成本 —— 而且根因是标定时口径错了** ⚠
- L1 全量上确实拦掉 **72.3%**（如 `[形状门槛] 原 664 -> 剩 193`），
  **但「进 L2 的数」几乎没变**（300 池 gen12/13/14 = 30/30/30 仍顶格）
- ★ 根因：这道门在 **`head(TOPN)` 之前**，而 **L1 候选有 600~700 个（≫ `TOPN=30`）**
  ⇒ 筛完剩下的仍 ≥30 个 ⇒ **top-N 照样填满** ⇒ **L2 数量不变** ✗
- ⚠ **我的错**：标定时是在「**已进 top-N 的 30 个**」上算拦获（27~36%），
  而实际门作用在「**L1 全量 ~700 个**」上 ⇒ **口径不同，高估了省成本的效果** ✗
- ★ 正确说法：这道门是「**换人**」（改变谁进 L2），**不是「省成本」**

**(b) 选人是否变好 —— 无证据，信号偏负面**
- 全局：无 mono（21 代）入库 **0.67 个/代** → 有 mono（9 代）**0.11 个/代**
- ⚠ 但**不能定因果**：① 样本仅 9 代 ② **最公平的对照（1000 池）显示没降** ——
  无 mono 的 gen7/8/9 = 1/0/0（**已降到 0.33 个/代**）vs 有 mono 的 gen10/11 = 0/1（**0.50**）
  ③ ⇒ 300/500 池的"下降"更可能是**自然边际递减**（它们都已 11 代）

**(c) 决策原则**：**没有可证实的收益 + 有风险信号 ⇒ 不该开** ✓
⇒ 关掉后的代正好构成 **A/B 后半段**（有 mono 9 代 vs 无 mono）✓

### Notes
- ★ **+16 个新算子确认被用上**（16 个里 **7 个**出现在日志并进了 L1/L2：
  `ts_slope5/20/60`、`ts_rsqr5/20`、`ts_skew60`、`ts_kurt20`）✓

---

## [0.14.1] — 2026-09-15

> ⚠ **本文件补记**：发布时**漏写** ⇒ 2026-09-15 事后补 ✓
> 主题：**把 `--min_mono=0.75` 接进生产参数** + 修 `tracks_status.py` 的路径 bug

### Added
- `tools/run_tracks.py` 的 `extra` 追加 `--min_mono=0.75`
  （依据：引擎注释标定 1150 条「可拦 ~27% 判死候选且对 19 个入库因子零误杀」+
  我用独立 124 条复核「误杀 0/6 = 0.0% · 拦掉 42/118 = 35.6%」⇒ 两次独立标定互相印证）
  ⚠ **后被 v0.14.2 回退**（省成本口径错误，见上）

### Fixed
- 🔴 **`tools/tracks_status.py` 的 `LOGD` 与 `run_tracks.py` 分家了**：
  ```
  run_tracks.py   : LOGD = ROOT/ai_test/_tracks      ✓ 现址
  tracks_status.py: LOGD = HERE/_tracks (= tools/)   ✗ 旧址
  ```
  根因：`run_tracks.py` **后来迁入 `tools/`**，而本文件仍按旧位置推算
  ⇒ `[1] 驱动器日志`/`[4] 最近日志`/`[6] 在跑的代进度` **全空** ✗
  ⇒ 修法：按 `run_tracks.py` 定义取 + **保留历史目录回退**
  （`ai_test/_tracks` → `tools/_tracks` → `ai_test/_night`）✓
  > ★ **通用教训：同一份"约定路径"不要在两处各算一遍** —— 挪文件时必然分家

### Notes
- ★ **新增工程手法：长跑「衔接器」**（`ai_test/_tracks/_chain_to_47.ps1`）：
  `Wait-Process -Id <PID>` → 检查无并发 → `Start-Process` 新 driver
  ⇒ **不用在对话里干等**，且 `next_gen()` 从各池末代+1 起**不重跑** ✓
- ⚠ **坑**：**Windows PowerShell 5.1 在 `.ps1` 无 BOM 时按 ANSI(cp936) 读**
  ⇒ 中文字面量变乱码（功能不受影响但**日志不可读**）
  ⇒ **凡 `.ps1` 一律写纯 ASCII** ✓

---

## [0.14.0] — 2026-09-15

> 主题：**§1.3-B 核查 —— 「生成后、L1 前」那道门在本项目没有可用判据**（+ 发现 `--min_mono` 已标定却关着）
> 用户指令：「**接着做 §1.3-B**」

### ① 先拿 QuantaAlpha 的**精确语义**（不猜）
`factors/regulator/factor_regulator.py:80-85` + `configs/experiment.yaml:132-134`：
Novelty（最长公共子树 `>5`）· Free Args Ratio（**数值常数节点/总节点** `>=0.5` FAIL）·
Unique Vars Ratio（唯一变量/总节点 `>=0.5` FAIL）· Symbol Length（`len(expr)` `>200`）·
Base Features（唯一字段数 `>5`）

### ② ⚠⚠ 关键：我们表达式表示不同 ⇒ **阈值不能照抄**
```
QuantaAlpha: TS_MEAN($close, 20)   <= 20 是 NumberNode
我们:        ts_mean20(close)      <= 窗口在算子名里
=> ts_mean20(close): nodes=2, unique_vars=1 => ratio = 0.50 => QuantaAlpha 门 FAIL **最正常的因子** ✗✗
```
实测：照抄 `unique_vars_ratio>=0.5` ⇒ **误杀 21.7% 的已入库因子** ✗

### ③ ★★★ 标定结论：**「生成后、L1 前」无可用判据**（数据）

**样本**：正 = `bank`（已入库）· 负 = `last_l1` 中未入库
（⚠ 初版拿 `rand_expr` 纯随机当负样本 —— **不代表真实生成流** ⇒ 结论全错，已更正）

| 指标 | 正 p50/p90/max | 负 p50/p90/max | "正 max"作阈值能拦多少负样本 |
|---|---|---|---|
| `length` | 76.5/138.5/**189** | 72/112/172 | **0.0%** ✗ |
| `n_nodes` | 7/12.5/**15** | 8/13/15 | **0.0%** ✗ |
| `n_fields` | 2.5/4/**4** | 3/4/6 | **5.1%** ✗ |
| 结构 Novelty（留一）| 1/7/9 | 1/2/8 | **中位重合 ⇒ 无区分度** ✗ |

**根因**：① **复杂度**由生成端 `cfg['depth']` 已管住 ⇒ 再加长度/节点上界是**重复的**
（QuantaAlpha 需要它是因为候选由 **LLM 写**，LLM 天然倾向写复杂式）
② **结构 Novelty** 正负中位重合 ⇒ 无区分度

> ⚠⚠ **更正**：早先一次性脚本报过"Novelty 正 p50=**7.0**（方向反了）"，那是**实现 bug**
> （`zip(bank_all, zoo)` 而 `zoo` 只含可解析项 ⇒ **两列表错位**）⇒ 修好后 **正 p50 = 1.0 = 负样本**
> ⇒ 真相是「**无区分度**」，不是「方向反了」✓

### ④ ★★★★★ 但「省预算」成立，只是**位置错了**：`n_l1=28 → n_l2=28 → n_pass=2`（L2 命中率 4.8%）
**L2 = 全量面板费后回测（最贵的一环）** ⇒ 门该在「**L1 后、L2 前**」
**而清点代码发现那里已有一整套门**：`--min_ic`/`--min_stab`（L1957）· **`--min_mono`（L1963）** ·
相似度门（L1975）· `score`-topN（L2037）· `--fam_quota`（L2084）· LLM 审查 KILL（L2125）

### ⑤ ★★★★★ 真正的发现：**`--min_mono` 建好了、标定了，却关着**
引擎 L1959 注释（**标定 1150 条**）：`mono >= 0.75 可拦下 ~27% 判死候选且对 19 个入库因子零误杀`
**我用独立数据（124 条）复核 ⇒ 同向且更强**：
```
python tools/calib_gate.py --target=l1_l2 --eval "mono>=0.75"
  ★ 误杀正样本 0/6 = 0.0%       ✓ 安全
  ★ 拦掉负样本 42/118 = 35.6%   ✓ 有效
```
★ **两次独立标定互相印证** ⇒ `mono >= 0.75` 可信 ✓
⚠ 但引擎默认 `--min_mono=0.0`（关）+ `run_tracks.py` 生产参数也没传 ⇒ **这道门一直没生效**

### Added
- **`tools/calib_gate.py`** —— **可复用的门标定器**（加门前先跑它）：
  `--target=pre_l1`（结构判据）· `--target=l1_l2`（L1 指标）· `--eval "mono>=0.75"`（复核具体规则）
  判据：**误杀 ≈ 0 且拦获 ≥ 20%** 才值得开 ✓

### ★★★ 通用教训：**并行列表绝不要靠"两个同条件推导式"对齐**
只要有**一条**被过滤掉就错位 —— 实例：`zip(bank_all, zoo)`（`zoo` 只含可解析项）、
`zip([e for e in P if parse(e)], zoo)` ⇒ **要成对构造**（`pairs.append((e, key))`）✓
> ★ 这个坑我**在同一次任务里犯了两次** ⇒ 已写进工具注释与 roadmap §8.50

### ⚠ 待用户拍板
**是否把 `--min_mono=0.75` 接进 `tools/run_tracks.py` 的生产参数** ——
（**不改引擎默认值**，保持「默认关 = 行为不变」铁律；只驱动脚本带上它）
⚠ 阈值是**样本内**标定（正样本仅 6 个）⇒ 建议后续做**样本外复核**

### Notes
- **未改任何引擎行为**（`engine/` 只读核查）⇒ 回归全绿：
  10 个回归测试全过（33/11/32/15/17/22/**51**/22/20/34）· 编译 208 个文件全过 · 真机冒烟 4/4
- ⚠ **顺带发现（非本次引入）**：`ai_test/` 里有 9 个**既有探针脚本**共 **12 处**「ASCII 引号紧邻中文」
  （`append_mem*.py` / `combo_*.py` / `diag_*.py`）—— 它们在项目常规质检范围（`tools`/`engine`/`standard`）**之外**，
  **未擅自修改** ⇒ 如需要可另做一次清理 ✓
- 清理了一批一次性标定脚本（`_calib_quality_gate.py` / `_calib_novelty.py` 等），
  统一到 `tools/calib_gate.py`（**避免留一个错的在那里误导**）✓

---

## [0.13.1] — 2026-09-15

> 主题：**回归族补 `w=10` + 定下「窗口档位四原则」**（治 `loop_todo §1.26` 追加）
> 用户指令：「**还有个问题，为啥算子只做 `ts_slope5/20/60`，还有 10，15，30 呢？**」
> ⚠ 本版本发布时**漏写本文件**（只改了 roadmap/README/loop_todo）⇒ 2026-09-15 事后补记 ✓

### Added
- `ts_slope10` / `ts_rsqr10` / `ts_resi10` ⇒ **`UNARY` 53 → 56**

### ★★★ 窗口档位**四原则**（已固定成测试 `_test_ops_math.py [8]`）
先查数据（扫本项目 `UNARY` 全部窗口档位）：`w=20` 被 **13** 个算子用（事实默认档）·
`w=60` 被 **10** 个 · `w=5` 被 **6** 个 · `w=10` 只被 `ts_mean` 用 ·
★★★ **`w=15` / `w=30` 全项目从未出现**

| # | 原则 | 依据 |
|---|---|---|
| **①** | **近似等比**（短端密、长端疏）| 短端的**相对差异才是信息**（`5` vs `10` 差 2×；`60` vs `65` 没意义）⇒ 回归族 `5/10/20/60` 间距 **2×/2×/3×** ✓ |
| **②** | **优先复用已有档位**，别引入新档 | `15`/`30` 从未出现 ⇒ 加了就是引入新档 ⇒ **不加** ✓ |
| **③** | **别加冗余档** | 趋势类在**相邻窗口高度相关**（`slope10` ≈ `slope5`/`slope20` 混合），**不像 `ts_mean` 那样正交** ⇒ 间距不能太密 ✓（`ts_mean` 有 8 档是因为要做**比值/叠加**；趋势类用绝对量，不需那么多档）|
| **④** | **短窗对高阶矩无意义** | 偏度/峰度需**足够样本**（5 点 ≈ 噪声）⇒ `ts_skew`/`ts_kurt` **只给 20/60，不给 5** ✓ |

★★ **自我修正**：按原则 ①，`5/20/60` **短端太疏**（4×/3×）⇒ **补 `w=10`** ✓
> ★ **本质**：**窗口档位不是"越多越好"，而是「尺度分得开 + 不引入新档」** ✓
> 且快慢趋势差（类 MACD）用 `sub(ts_slope5(close), ts_slope60(close))` **已能组合**，
> **不需要**真存在 `w=10/15/30` 三档 ✓

### 验证
- ★ **真面板**：首有效期严格 = `w-1`（**4 / 9 / 19 / 59**）⇒ 满窗语义 ✓
- `_test_ops_math.py` **51/51**（新增 `[8]` 窗口原则 4 条断言）· 全量编译 77 + 10 测试全绿
- ⚠ 记一个**我自己引入的结构错**：`t_window_policy` 被**插进 `t_engine` 中间**，把后半段挤了进去
  ⇒ 运行时报 `NameError`（**编译通过但运行炸，`py_compile` 抓不到**）
  ⇒ **教训：插入函数定义时，锚点要选在"上一个函数完全结束"之后；且光"编译通过"不够，必须真跑一遍** ✓

---

## [0.13.0] — 2026-09-15

> 主题：**算子族扩展 ②③：回归三件套 + 偏度/峰度**（治 `loop_todo §1.26`）
> 用户指令：「**除了 slope 还有线性回归拟合度 R方也很重要，R方高线性度好，说明涨得稳，你觉得呢？
> 然后除了成本高的，其他都做了吧**」

### ① 回答「你觉得呢」—— 用户判断**方向对**，补三点

| | 内容 |
|---|---|
| **用户说** | 「R² 高线性度好，说明**涨得稳**」 |
| **判断** | ✅ **方向正确** —— R² 度量「价格路径有多接近直线」= 趋势干净、少噪音 |
| ★★ **补充①** | **R² 不含方向**：「涨得稳」与「**跌得稳**」R² **完全相同**（已固定成测试：纯涨/纯跌 R² 都 = 1.000）⇒ 单用 `ts_rsqr` **丢方向** ⇒ 必须配合 `ts_slope`（`mul(ts_rsqr20(close), ts_slope20(close))` 实测 +0.70/−0.70 可区分 ✓）|
| ★ **补充②** | 价格**几乎不动**时 `SS_tot → 0` ⇒ `R²` 是 **0/0** ⇒ 实现用 `EPS` 兜底、**输出 NaN**（不臆造、**也不给假 1.0**）✓ |
| ★ **补充③** | **R² 尺度无关** ⇒ 对"波动大小"不敏感 ⇒ 捕捉的是「**路径有没有单边趋势**」，**不是**「涨得多不多」|

### Added（16 个算子，`UNARY` 40 → **56**）

| 族 | 算子 | 窗口 |
|---|---|---|
| **回归（用户点名）** | `ts_slope*` / `ts_rsqr*` / `ts_resi*` | **5/10/20/60** |
| **分布形状** | `ts_skew*` / `ts_kurt*` | 20/60 |

#### ★★★ 窗口档位**原则**（用户追问「为啥不做 10/15/30」后定下来的）

> 用户原话：「**为啥算子只做 `ts_slope5/20/60`，还有 10，15，30 呢？**」

**实测本项目全部窗口档位**：`w=20` 被 **13** 个算子用 · `w=60` 被 **10** 个 · `w=5` 被 **6** 个；
而 **`w=15` / `w=30` 全项目从未出现**（`w=10` 只有 `ts_mean` 用）。

**⇒ 四条原则**（已**固定成测试** `_test_ops_math.py [8]`）：

| # | 原则 | 依据 |
|---|---|---|
| **①** | **近似等比**（短端密、长端疏）| 短端的**相对差异才是信息**（`5` vs `10` 差 2×；`60` vs `65` 没意义）⇒ `5/10/20/60` 间距 **2×/2×/3×** ✓ |
| **②** | **优先复用已有档位** | `15`/`30` 从未出现 ⇒ 加了就是**引入新档** ⇒ 不加 ✓ |
| **③** | **别加冗余档** | 趋势类在**相邻窗口高度相关**（`slope10` ≈ `slope5`/`slope20` 混合），**不像 `ts_mean` 正交** ⇒ 间距不能太密 ✓（`ts_mean` 有 8 档是因为要做**比值/叠加**；趋势类用绝对量，不需那么多档）|
| **④** | **短窗对高阶矩无意义** | 偏度/峰度需**足够样本**（5 点 ≈ 噪声）⇒ `ts_skew`/`ts_kurt` **只给 20/60，不给 5** ✓ |

★★ **自我修正**：按原则 ①，我原来的 `5/20/60` **短端太疏**（4×/3×）⇒ **补 `w=10`**
⇒ `5/10/20/60`（2×/2×/3×，与 `ts_delta{5,20,60,120}`、qlib `{5,10,20,30,60}` 同族规律）✓ **53 → 56**

> ★★ **本质**：**窗口档位不是"越多越好"，而是「尺度分得开 + 不引入新档」** ✓
> 且**快慢趋势差（类 MACD）用 `sub(ts_slope5(close), ts_slope60(close))` 已能组合**，
> **不需要**真存在 `w=10/15/30` 这三档 ✓

- `engine/fastops.py`：`_linreg(x, w)`（**一次出三件套**，共用 3 个前缀和）· `_cum_w()`（**满窗**窗口和）·
  `ts_slope` / `ts_rsqr` / `ts_resi` / `ts_skew` / `ts_kurt`
- `engine/loop_engine.py`：13 个算子进 `UNARY` + 5 个 helper
- `engine/loop_critic.py`：`SLOW_OPS` 加 `ts_slope60`/`ts_rsqr60`
- `engine/loop_llm.py`：A角 prompt 加回归族/分布形状说明（含"**R² 不含方向、建议与 slope 组合**"）
- `tools/_test_ops_math.py`（**44 项**）

### ★★ 「成本高的」用**实测**定义（不是感觉）

`ai_test/_bench_newops.py`（3309×2000）：

| 类 | 代表 | 耗时 | 相对 | 做? |
|---|---|---|---|---|
| **前缀和型** | `slope`/`rsqr`/`resi`/`skew`/`kurt` | **0.42 s** | 1× | ✅ |
| **排序型** | `rolling.quantile` | **4.21 s** | **10×** | ❌ |
| **位置型** | `rolling.apply(argmax)` | 17.2s（仅 1/4 面板）⇒ **69 s** | **164×** | ❌ |

★ 且实测印证：**`scipy.maximum_filter1d` 0.85s 但只给「值」不给「位置」**
⇒ **「位置」（距最高点几天）极贵，而「幅度差」（`sub(ts_max100(x), x)`）便宜且现有算子能组合** ✓
⇒ `hhvbars`/`llvbars`/`barslast` **不必做**（有等价替代）✓

### ⚠ 没做什么 + **理由**（同样是产出）

| 没做 | 理由 |
|---|---|
| `ts_median`/`ts_quantile` | **10× 慢**；且 `median` 与 `mean` 高共线 |
| `argmax`/`hhvbars`/`barslast`/`count`/`cross`/`filter` | **164× 慢**；「位置」可用「幅度差」替代 |
| `sqrt`/`pow`/`exp`/`int` | ★★ **对秩类因子无效** —— 全程按**截面秩**算 IC ⇒ **单调变换不改变结果** ✗ |
| `if`/`between`/`and`/`or` | 需给表达式树加**布尔类型** ⇒ 成本最高 |

### ⚠⚠⚠ Fixed（事故）：**并行跑两个负向验证 ⇒ 把源码改坏了**

**现象**：`_test_ops_math.py` 先 **44/44** 通过，跑完两个负向验证后变 **37/44**，
`rsqr` 算出 **0.000**（正是我人为打进去的错公式）。

**根因（两层）**：
1. **我的操作失误**：同一轮**并行**发两个负向验证（都改 `engine/fastops.py`）⇒
   进程② 读到的"原始快照"已含 ① 的错公式 ⇒ ② 的"还原"**把错公式写回去** ✗
2. ★★ **工具缺防护、且会谎报**：旧版 `finally: write(src)` 后**直接宣布"已还原"** ——
   `src` 被污染时写回去还是错的，**却报成功** ✗

**修法（两条硬防护，`tools/verify_test_catches.py`）**：
- **防护 A：并发守卫** —— `.verifybak` 存在即**拒绝启动**（当互斥锁）+ 提示"绝不要并行跑" ✓
- **防护 B：还原后逐字节校验** —— 写回**读回来比对**，不一致 ⇒ **用备份强制恢复** + **大声报错** ✓

> ★★★ **通用教训**：**凡「临时改写文件、跑完再还原」的工具，都必须**
> （i）**有并发互斥**（ii）**还原后验证**而不是假设。
> 而且——**工具会改文件时，别并行调用它**（哪怕看起来是两个独立的验证）。

### 验证
- **对拍外部实现**：`ts_slope`/`ts_rsqr`/`ts_resi` 与 **`np.polyfit` 逐位一致**（相对差 ~1e-8 = float32 精度）；
  `ts_skew`/`ts_kurt` 与 **`scipy.stats` 逐位一致** ✓
- **解析解**：纯线性 ⇒ slope=斜率 / R²=1 / resi=0 ✓；完全不动 ⇒ R² = **NaN** ✓
- ★★ **「R² 不含方向」已固定成测试** ✓
- **纪律**：5 个算子全过「**改 t≥120 不影响 t<120**」= **无未来信息** ✓ · 满窗 ✓ · 中段 NaN 不臆造 ✓
- **两个负向验证**（**串行**跑）都成立 ✓
- **真机冒烟 5/5** · **真面板验证**：**首有效期 = 19/19/59/59**（满窗语义证据）· 覆盖率 69~71% ·
  `ts_rsqr60(close)` 值域 **[0, 1]** ✓
- 全量：编译 **77** 个文件全过 · **10 个回归测试全绿**

### ⚠ 诚实遗留
- 新算子比 `ts_mean` 贵（真面板 4~7s/表达式）；**算子数 40→53 会扩大搜索空间**
  ⇒ 仍需 §1.3-B 的**复杂度门**配套，否则候选更冗长

---

## [0.12.0] — 2026-09-15

> 主题：**加 `ema` 算子** + 顺手修掉一处**已存在漂移**（`loop_todo §1.25/§1.26`）
> 用户指令：「**ema 加上**，然后他们的 20+（偏度/峰度/分位/回归/argmax…），这个我们没有吧？
> 还有比如通达信里的 slope、barssince、count、barslast、BARSCOUNT、CROSS、FILTER 之类的也没有吧？」

### Added
- **`fastops.ts_ema(x, w)`** —— 指数移动平均，衰减因子 **`α = 2/(w+1)`**（对齐 QuantaAlpha/通达信）
- **`loop_engine.UNARY` 加 5 档**：`ema5` / **`ema12`** / `ema20` / **`ema26`** / `ema60`
  - ★ 为什么是这 5 档：**12/26 是 MACD 标准参数** ⇒ `sub(ema12(x), ema26(x))` **就是 MACD**
    ⇒ **不必再单独加 `MACD` 算子** ✓
  - ★ **为什么只加这一个**：`ts_mean` 是**等权**、EMA 是**指数加权** ⇒ **不同算子、组合不出来**
    ⇒ 这是盘点发现的**唯一"补不回来"的缺口** ✓（RSI/BOLL/ATR 都能组合 ⇒ 不加）
- `loop_critic.SLOW_OPS` 加 `ema60`（指数平滑天然低换手/高稳定）
- `loop_llm` 的 A角 prompt 加 EMA 说明 + MACD 组合示例

### ★★★★ Fixed（本次真正的收获）：抓到一处**已存在的漂移**

`loop_critic._ops_of()` **硬编码 28 个算子**，而引擎实际有 **45 个** ⇒ **缺 15 个**：
`ts_mean60/100/120/150/200` · `ts_std100/150/200` · `ts_rank100/200` ·
`ts_max100` · `ts_min100` · `ts_delta60/120` · `ts_sum100` · `corr100/200`
⇒ ★ **长窗口算子一直被 B角 的结构诊断忽视** ✗
> 这与项目史上「`loop_critic` 硬编码旧 12 字段」（`loop_fields.py` 头注）**是同一类漂移** ——
> **同一个文件、同一种病，第二次犯** ✗

**修法：不补名单，改成派生 + 用测试锁**：
```python
_OP_CACHE = []
def _op_names():
    if not _OP_CACHE:                      # 惰性 import：loop_engine 会 import loop_critic
        import loop_engine as LE           # ⇒ 避免循环导入
        _OP_CACHE.append(tuple(LE.UNARY.keys()) + tuple(LE.BINARY.keys()))
    return _OP_CACHE[0]
```
### 实现要点（`ts_ema`）
- ★★ **`min_periods = w`（满窗）**，不是其它 `ts_*` 的 `max(2, w//2)`：
  EMA 早期值**严重依赖起点**（w 期后仍有 `(1-2/(w+1))^w ≈ 13.5%` 残留）⇒ 必须满窗
  ⇒ 前 `w-1` 期为 NaN（真面板验证：`ema12` 首有效期 = **11** ✓）；代价可控（3309 日里 w=60 占 1.8%）
- ★ **NaN 处「跳过」**（保持上一个 EMA 值、计数不增加）—— 通达信/qlib 口径 ✓
- ★ **不用 `scipy.signal.lfilter`**：它把 NaN **传播**成整段 NaN ⇒ 不符合口径 ✗（也不引依赖）
- ★★★ **性能实测推翻了我的估算**：真面板 `ema12(close)` **0.58s** vs `ts_mean20(close)` **1.17s**
  ⇒ ★ **`ema` 反而快约一倍**（`ts_mean` 要两次 cumsum + 计数）
  > **教训：小面板估算 ≠ 真面板** —— 我原先写"ema 更慢"，是错的 ✓

### Notes
- `tools/_test_ops_sync.py`（**22 项**）**四层锁**：
  ① `_op_names() == UNARY+BINARY` + **`_ops_of` 体内不许有算子名字面量**（防写回去）
  ② **穷尽搜**「列表字面量里 ≥4 个算子名 = 疑似全量清单副本」
  ③ **A角 prompt 必须列出全部 50 个算子**（含**斜杠简写展开** —— 初版没展开 ⇒ 误报 20 个"缺失"）
  ④ **`ts_ema` 数值**：手算递归一致 / 满窗 / **无未来信息** / NaN 跳过 / 组合出 MACD
- ★ **负向验证**：把 `_ops_of` 改回硬编码 ⇒ **确实 FAIL** ✓
- ★ **真面板验证**（`ai_test/_smoke_ema_realpanel.py`）：覆盖率 72.5% · MACD 值域 p1..p99 = −2.4..+3.1
  （close 量级 9.5）⇒ 量纲合理 ✓ · **真机冒烟 5/5** ✓
- 全量：编译 **76** 个文件全过 · 9 个回归测试全绿
- `docs/loop_todo.md §1.26` —— **第二个问题的答案**：统计类/通达信函数**都没有** ✓
  归纳成 **4 类缺口**（事件位置 / 高阶矩分位 / **回归类** / 数学逻辑）+ 按价值成本排序的建议

### ★★ 通用教训
> **凡"白名单副本"必然漂移** —— 要么**改成派生**（首选），要么**用回归测试锁住副本**。
> 只靠"记得同步"是**必然失败**的（本次就是第 2 次犯）。

---

## [0.11.3] — 2026-09-15

> 主题：**特征/算子盘点** —— 回答「有四价一量吗？MA/MACD/EMA/HHV/LLV 有吗？」
> （对照对象：**本地 `QuantaAlpha-main` 源码**，不凭记忆）

### 结论（`docs/loop_todo.md §1.25`）

| | 我们 | QuantaAlpha（qlib Alpha158）|
|---|---|---|
| **四价一量** | ✅ **`close`/`open`/`high`/`low`/`volume`**（**基础 7 叶子，直接可用**）| ✅（要从 OHLC 构造函数）|
| 派生价量 | ✅ **12 个** | ✅（K 线 9 + 归一化 4）|
| **MA** | ✅ `ts_mean` × **8 档** | ✅ `Mean`/`MA*` × 5 档 |
| **HHV/LLV** | ✅ `ts_max`/`ts_min`（**只 2 档**）| ✅ + **`HIGHDAY`/`LOWDAY`** |
| **EMA** | ❌ **没有** | ✅ `EMA`/`SMA`/`WMA`/`DECAYLINEAR` |
| **MACD** | ❌ **没有** | ✅ `MACD` |
| RSI | 🟡 **间接有**（`ts_rank` ≡ qlib 的 `RSV`）| ✅ |
| BOLL / ATR | 🟡 **可组合**（原料已有）| ✅ 预计算列 |
| K 线形态 | ✅ **等价物齐**（`intraday`↔`KMID` · `amplitude`↔`KLEN` · 影线↔`KUP/KLOW`）| ✅ |
| **资金流 / BARRA / 财报 PIT** | ✅ **16 / 11 / 8**（**他们都没有**）| ❌ |
| 统计算子 | 🟡 8 个 | ✅ **20+**（偏度/峰度/分位/回归/argmax…）|

### ★★ 关键结论：**"缺"的只有一类 —— 加权均线族**

- ✅ **四价一量**：**有**（且是**基础叶子**，比"从 OHLC 构造函数"更灵活）
- ✅ **MA / HHV / LLV**：**有**（= `ts_mean` / `ts_max` / `ts_min`）
- ❌ **EMA / MACD**：**真缺** —— ★ 因为 `ts_mean` 是**等权**、EMA 是**指数加权**
  ⇒ **是不同算子，组合不出来** ✗（**唯一补不回来的**）
- 🟡 **RSI / BOLL / ATR**：**能用现有算子组合出来**（`ts_rank` / `ts_mean`±`ts_std` / `ts_mean(true_range)`）✓
- ★★★ 而且这正是项目文档里的**既定"三期"项**（`roadmap` 文末：
  `三期：技术指标（EMA/MACD/RSI 等预计算列）与分钟频聚合`）⇒ **已知缺口，非遗漏** ✓

### 建议（若补）
- ★★ **只加 `ema` 一个算子** ⇒ 加了就能组合出 MACD（`sub(ema(x,12), ema(x,26))`），
  **不必专门加 `MACD`** ✓
- ★ **RSI / BOLL / ATR 不用加**（都能组合）✓
- 可选按价值排序：`wma` · `highday`/`lowday` · `skew`/`kurt` · `regbeta`
- ⚠ **风险**：加算子会**扩大搜索空间** ⇒ 同预算下有效候选密度下降；
  且候选更冗长 ⇒ **必须配套**复杂度门（§1.3-B 的 `Base Features ≤ 5`）
  ⇒ 但**方向对**：**EMA 是"新信号源"（不同平滑方式）**，正对 §1.20 铁律「瓶颈是**信号源多样性**」✓
- ⚠ **实现坑**：EMA 是**递归量** ⇒ ① 起点依赖需 warm-up；② 现有 `ts_*` 的
  `min_periods=max(2, w//2)` **语义不同**，得单独定义；③ `pd.ewm` 与 `rolling` 的
  **NaN 传播规则不同**，要与 `rank_rows` 对齐

### Notes
- **纯盘点 + 记待办**（`loop_todo §1.25`）—— **未改代码**
- 做法上特意**读本地 `QuantaAlpha-main` 源码**（`backtest/factor_loader.py` 的
  `ALPHA158_FACTORS`/`ALPHA158_20_FACTORS` + `factors/coder/function_lib.py` 的 **83 个算子**）
  ⇒ **不凭记忆**

---

## [0.11.2] — 2026-09-15

> 主题：**更正 v0.11.1 的建议 —— 不要扩到 11 个 BARRA 风格，应扩到 4 个**
> 用户一问引出：「剥离清单从 2 个风格扩到 11，这个要不要**等每个风格都有 1-2 个因子**再去自动剥离？」

### ⚠ 为什么这是一次**更正**（不是新增）

v0.11.1 里我写了「**建议扩到全部 11 个 BARRA 风格**」—— **那是错的** ✗
（当时只是从"外部给的 11 个暴露"顺推出来的，**没核实这 11 个在我们系统里是什么**）

### ★★★★ 核实结论

**(1) 先纠正用户问题里的前提：不用等**

剥风格用的是**风格暴露值**，与"库里有没有该风格的因子"**无关**：
`style_features()`（`loop_engine.py:350`）**从行情现算** 4 个
（`lncap=log(市值)` / `lnamt=log(20日均成交额)` / `lntr=log(20日均换手率)` / `lnpx=log(收盘价)`）✓

**(2) 但**两套「风格」是完全不同的东西** —— 这是关键区分**

| 类别 | 来源 | 数量 | 是什么 | 该不该剥 |
|---|---|---|---|---|
| **BARRA 风格叶子** | `barra.h5`（外部 BARRA 模型）| **11** | ★ **因子的构造原料** | ❌ **不能剥** |
| **`style_features` 4 项** | `base_fields` **现算** | **4** | ★ **系统性暴露** | ✅ **该剥**（现只剥 2）|

**证据（实测）**：
- 全库 **56 个因子里 28 个（50%）用 BARRA 叶子构造**
- **`barra_residual_volatility` 一个被 22 个因子当原料**（`non_linear_size` 15 · `liquidity` 8 · `leverage` 5）
- **精选池 7 个里 5 个**含 BARRA 叶子（`F35`/`F25`/`F07_1000`/`F33`/`F21`，各 2~3 个）
- ⇒ **剥掉 `barra_residual_volatility` = 把 22 个因子掏空**（甚至退化成常数）✗
  ⇒ 这是**自相矛盾**的：用某个叶子构造因子、又要把它剥掉

**旁证**：11 个 BARRA 风格本身**高度共线**（标准 BARRA 特点）⇒ 全剥会**过度**；
且 `barra_size`/`barra_non_linear_size`/`barra_liquidity` 与 `lncap`/`lnamt` **有重叠**
⇒ 剥 `lncap`/`lnamt` **已经间接剥掉一部分**（解释 `F35` 剥后 1.077→0.604 的降幅）✓

**(3) 正确的动作：`2 → 4`（加 `lntr` + `lnpx`），且**零边际成本**

```python
# loop_engine.py:1694（现状）
for _k in STYLE_KEYS:                                   # STYLE_KEYS = 4 个
    if _strip_style and _k in ('lncap', 'lnamt'):       # ★ 这行把 lntr/lnpx 过滤掉了
        STYLE_FULL[_k] = _sf[_k]                        # 全面板(供 L2 剥除)
```
⇒ `lntr`/`lnpx` 的值**同一个 `style_features` 就算出来了**，只是被这行 `if` 丢掉
⇒ **改一个开关即可** ✓
- 代价：全面板内存 **142MB → 284MB**（各 ~71MB；只在 `--strip_style` 时保留）
- 收益：**正好覆盖外部指出的 `F07 lntr −0.51` 漏网** ✓

### ★★ 由此确立的**分工原则**（值得跨项目复用）

| 用途 | 覆盖范围 | 为什么 |
|---|---|---|
| **诊断暴露** | **越全越好**（11 BARRA + 4 系统性 = 15 个都可看）| 只是"看"，不影响因子本身 |
| **剥离清单** | **只剥"系统性暴露"**（4 个）| ★ **BARRA 叶子在因子内部是"被人为组合过的信号"**，不是"需要中性的背景暴露" |

> ★ **通用教训**：
> **「诊断用的维度集合」与「中性化用的维度集合」不是同一件事** ——
> 前者要**全**（看得清），后者要**只含背景暴露**（剥得准）。
> 把诊断维度直接当剥离清单用，会**把因子自身的信号当成风格剥掉** ✗

### Notes
- 本次是**纯更正**（改 `docs/factor_roadmap.md §8.47-④/⑥` + `docs/loop_todo.md §1.24-①/①b` + README）
  —— **未改代码**；`2 → 4` 待用户拍板后再动
- ★ 教训（对我自己）：**"顺着外部给的维度往下推"之前，先核实那个维度在本系统里是什么**
  —— 这次差点把"因子的构造原料"当成"要剥的风格" ✗

---

## [0.11.1] — 2026-09-15

> 主题：**核对「外部独立审查」的 7 个精选因子** + 补「下游使用须知」
> 用户：「你看看精选池我让一哥们儿查的，你看看对不对」

### 核对结论：**他的判断基本正确**，引擎值 **14/14 复现** ✓

逐条用我们自己的落地数据核过：

| 他的结论 | 核对结果 |
|---|---|
| 引擎值 14/14 复现（原 Cal + 剥 Cal/超额）| ✅ 我们的 `loop_strip_style_bank.csv` **7/7 完全一致**（0.820/1.228/+5.3% …）|
| 「剥风格」= 模板【6】的「剥两者（市值+成交额）」| ✅ **正确** —— 我们的定义就是「对 `lncap`+`lnamt` 秩中性化后重跑」（`loop_engine.py:1627`）|
| **6/7 需取反**（只有 `F07_1000` 正向）| ✅ **正确** —— 精选池的 `sign` 列 **7/7** 与他的方向一致 |
| 300 内全员失效 / 500 仅 `F07` 勉强 | ✅ **交叉验证成立**（与 §1.3-A 的独立结论一致）|
| `F33` 日频回撤被口径低估 7.5pp | ✅ **正确且重要** —— 正是我们 §1.19 修的问题 |

### Added（补了一个**真实的落地缺口**）
- **`factor_pool_selected.md` 顶部加「⚠ 下游使用须知」** —— 起因：核对时发现
  **`facs/` 存的是原始因子值，而本表的「剥风格」是评估口径** ⇒ 下游直接用 h5
  **拿到的是未剥风格版本、绩效与回撤都对不上** ✗（尤其 `F33`：未剥日频回撤 ≈ −19%，剥后仅 −10%）
  - ⇒ 须知里写明四件事：**必须乘 `sign`** · **必须自己实现剥风格**（对 lncap+lnamt 秩中性化）·
    **不能用 `values_q.h5`**（uint8 丢了原始值 ⇒ 做不了中性化）· **池内未测、不要搬进成分内**
- `docs/loop_todo.md §1.24` —— 核对时发现的 5 条待办（见下）

### ⚠ 核对时发现的问题（已记 `loop_todo §1.24`）
1. ★★★ **「剥风格」只覆盖 2 个风格，而暴露是 11 个 BARRA 风格** ⇒ 「A 独立有效」**有边界**。
   外部给的「主暴露」显示 **`F07` 是 `lntr` −0.51**（换手率），而**我们只剥 lncap+lnamt** ⇒
   `F07` 号称"最独立"但其实**仍暴露于换手率风格**。
   > ★ 注意：我们**早就知道** lntr/lnpx 也是风格（`--style_obs` 记 4 个：
   > `loop_critic.py:708` 的 `st_l*_lncap/lnamt/lntr/lnpx`）⇒ **但剥风格只剥 2 个** ⇒ 不一致 ⚠
   > **建议（需拍板）**：扩到全部 11 个 BARRA 风格（或至少加 `lntr`）⇒ 档位会重新洗牌
2. **日频回撤与外部实现差 0.2~1.7pp**（7/7 系统性，方向不定）⇒ **不影响本次任何档位判定**
   （最紧的 `F05_500` 剥后 Cal 0.349 vs 他 0.339，离门槛 0.30 还有 0.039）✓
   ⚠ 但 A 档的 `strip_dd_d > −0.20` 若压在边界上可能翻转 ⇒ 建议留余量（本次余量都 ≥ +6.4pp ✓）
3. **精选池缺「池内」结论**：实测这 7 个在 `loop_pool_obs_*.csv` 里**只有 2 个命中**
   ⇒ 其余 **5 个从未在池内测过**（`--pool_obs` 是 09-12 才加的，它们更早入库）
   ⇒ **外部同事的【3】表是真正的新信息** ✓
4. **库文件里早期因子的「单期换手」是旧口径**：`F07` 库里写 4.9%（gen11 时代、
   "本档建立前未归档"）vs 外部 11.9% ⇒ **同一列不同代可能不同口径** ⇒ 统计时要标注
   （精选池用的是新口径 ✓）

### Notes
- 他的本地复现值**系统性偏低 1.3%~5.4%**（7/7）⇒ 是他本地实现的口径细节，**不是引擎错**
  （我们 CSV 与他的【0】表**完全一致**）✓
- **Tier 建议我的意见**：同意 **`F25`/`F07` 入 · `F33`/`F05_500` 淘汰**；
  `F35` 降配的理由可以更强（§1.3-E 实测 `corr(换手, 费后超额)` **−0.242**、
  最低换手档最好 ⇒ **换手 20.2% 在我们成本口径下天然吃亏**）；
  `F21`/`F07_1000` 备用 + 正交（`F21` 的 `lnamt −0.32` ⇒ 与低成交额族共线）。
  ⚠ 但 **`F33` 要剔出精选池需要改规则**（它是 A 档 ⇒ 自动入选）⇒ **需拍板**

---

## [0.11.0] — 2026-09-14

> 主题：**「动作有效性」+「参数棘轮」**（治 `loop_todo §1.15` + `§1.1` 遗留）
> 用户指令：「§1.15 + §1.1 … 同族 ⇒ 建议一起做一起验，**接着做这一族吧**」
> （**注**：§1.1 曾记「用户决定：有风险，就先不做吧」；本次用户明确改主意要做，
>  并采用 §1.1 自己写的**小步做法**：只在**永久停用时回退一次** + 写进 journal 让人可复核）

### ★ 为什么是同族：都需要**给动作加"施加前快照"**

| | 快照什么 | 用来干什么 |
|---|---|---|
| **§1.15 有效性** | 动作的**目标指标**（如 `leaf_conc`）| 一段闭合后**结算**：没改善 ⇒ 判无效 ⇒ 冷却翻倍 ⇒ 累计 N 次**永久停用** |
| **§1.1 棘轮** | 动作改的**参数旧值** | 永久停用时 ⇒ **保守回退**（只回退**被证伪**动作的影响）|

### Added
- **`engine/loop_critic.py`**：
  - `ACTION_METRIC`（7 个动作 → 目标指标 + 期望方向）· `METRIC_EPS=0.02`（噪声容差）·
    `INEFF_MUTE_N=2` · `INVALID_COOL_MULT=2`
  - 纯函数 `_metric_of` / `_improved` / `_rollback_plan` / `_get_param` / `_set_param` / `_fmtv`
  - `suggest()` 新增 `_base`（施加前快照）/ `_ineff`（判无效史）/ `_cool_mul`（冷却倍率），随 state 持久化
  - ★ **唯一写入口 `_set()` + 动作上下文 `_ctx`**：`_guard` 通过时 `_begin(aid)`，
    此后 `_set` **自动**把"本动作名下该参数的首个旧值"记进快照
    —— 因为**同代多个动作会改同一参数**（`r2`/`r4` 改 `min_stab`；`r5`/`r7` 改 `depth`），
      让每个调用点手写旧值既易错、又必然漏
- `tools/_test_action_efficacy.py`（**33 项**）

### ★★★ 本次最有价值的坑：**回退顺序**导致"停在半路"

多动作改同一参数**且都判无效**时，应该**一路撤到最初值**。实测 `r5`/`r7` 都改 `depth`：
```
【r5】↩ 恢复 mix；跳过 depth（当前还是 r7 写的 [3,4,5] ≠ r5 写入的 [3,4,4]）
【r7】↩ 恢复 depth=[3,4,4]
最终 depth = [3,4,4]   ← ✗ 停在半路（两者都无效，应回到最初 [2,3,4]）
```
**根因**：`_guard` 的动作检查顺序固定 ⇒ `r5` 先处理时 `depth` 还是 `r7` 写的 ⇒ 判据不匹配 ⇒ 跳过；
等 `r7` 撤完，`r5` 已不再检查 ⇒ **永远追不上**。

**修法（优雅，不需给动作排序）**：**被永久停用的动作每次被拦时都重试一次回退** ⇒ **自然收敛** ✓
> ⚠ 为此还修了一个**孪生 bug**：初版 `if done: pop('params')` —— **把跳过的记录一起丢了** ⇒
> 重试无从谈起 ⇒ 永远停在半路 ✗ ⇒ 改为**只清已成功恢复的**。

★★ **通用教训**：**"部分成功"的操作不能整体清账**，要**按项记账**
（与 v0.9.x 的「`_merge_csv` 不能整体覆盖」是**同一个道理**）。

### Fixed
- ★ **有效性结算的时机**：首版写成"即将重新施加就结算" ⇒ ① 指标只过 1 代、**样本太短**；
  ② 让「连续 `SAT_N` 代」这条路径**根本走不到** ⇒ 破坏 §1.1 既有语义（**被既有 32 项回归当场抓到**）
  ⇒ 加 `due` 标记，**只在 `_saturated` 分支置位**（= 一段**真正闭合**）⇒ 解禁后才结算 ✓
  （与 §1.15 原文「**冷却解禁时**比对」一致）
- ★ **`guard_mix` 在动作之后改 `mix`** ⇒ 必须**同步快照的「新值」**，否则回退判据永远匹配不上
  ⇒ **棘轮对 `mix` 静默失效** ✗（不查数据根本看不出来的坑）

### Notes
- ⚠ **指标取不到时一律不判定**（不臆造，§8.45 铁律）；池口径缺失时退回全A 口径
  （`fail_pool_calmar` → `fail_calmar`）
- **验证**：`_test_action_efficacy` **33/33** · 既有 `_test_critic_sensor` **32/32 全过**（不破坏旧语义）
  · **真机冒烟**（`tools/smoke_gen_only.py`）**5/5**（引擎带新状态键正常运行、不写状态）
  · 编译 **75** 个文件全过 · 引号全绿
- ★★ **两个负向验证**（`ai_test/_verify_efficacy_test.py`）：
  **绕过 `_set` 直接赋值** ⇒ FAIL（`实得 ['min_stab']`）·
  **回退清理退回旧逻辑** ⇒ FAIL（`实得 [3,4,4]`，应 `[2,3,4]`）✓
- `docs/factor_roadmap.md` **§8.46**（大改动 + 踩坑 = 研发档案该记的）

---

## [0.10.0] — 2026-09-14

> 主题：**`sign`（**方向**）全链路补齐**（治 `loop_todo §1.22/§1.23`）
> 用户拍板：「**加列应该还是要做**，咱们下游目前没有按列解析吧？」

### 先回答用户的问题：**下游确实没有按列解析**（已逐个实测）

| 读取者 | 解析方式 |
|---|---|
| `build_crosspool_view.py:parse_library()` | `re.split(r'\n### (F\d+) · ')` 切**明细节** + 取**代码块** |
| `build_facs.py:parse_library()` / `check_strip_style_pool.py` | 同上 |
| `pool_grade_summary.py:lib_gen_map()` | `re.finditer(r'^\|\s*(F\d+)\s*\|\s*gen(\d+)\s*\|')` ⇒ **只依赖前两列内容** |
| `ai_test/find_lost_bank2.py` | `re.finditer(r'^###\s*(F\d\d)\b')` |

⇒ **一处都不是"按列位置"** ✓ —— **但**实测发现**两套规则**：

### ★★ 为什么 `factor_library*.md` **不能加列**

它是 **append-only**：总览表头**只写一次**（历史 41 行已固定 5 列）
⇒ **加列会让历史行全部错位**（与 §8.30 的 CSV 同一个坑）。
> ★ 这个坑**项目里踩过** —— `loop_engine._lib_sync`（L879）注释原文：
> 「⚠ **不加表格列**（2026-09-13 实录）…⇒ 池标签只写进**明细块**。」
> ⇒ 本次 `sign` 沿用同一约定。

| 报告 | 生成方式 | 加列 |
|---|---|---|
| `factor_library*.md`（5 个）| append-only | ❌ ⇒ **写进明细块** |
| `factor_library_crosspool.md` | 全文重写（派生视图）| ✅ **已加列** |
| `factor_pool_selected.md` | 全文重写 | ✅ 已做（v0.9.1）|

### Added
- **`engine/loop_engine.py` `_lib_sync`**：**新入库**因子的明细块加一行
  `- **符号 \`sign\`：\`-1\`**（★ 因子值须乘它才是"越大越好"的方向；不乘 ⇒ 反向选股）`
  （**不加表格列** ✓）
- **`tools/backfill_library_sign.py`（新增）** —— 给**历史**明细块回填 `sign`
  - sign 来源 = `facs/<xx>/<name>/values.h5` 的 attrs，按**表达式精确匹配**
  - ⚠ 匹配不到 ⇒ 写「**未记录**」+ 注明「未在 `facs/` 落地」⇒ **绝不臆造**（§8.45 铁律）
  - 幂等 + `--dry` 默认；实测 **59 块 · 58 匹配 + 1 未落地 · 0 重复**
  - ★ diff **只有新增行**；明细块数/总览表行数**不变**（有契约检查）
- **`tools/build_crosspool_view.py`**：表A/表B 加 **`**\`sign\`**` 列**
  （实测 `+1 x4 · -1 x11 · 未记录 x2`）+ 说明段声明「缺失不臆造」

### Fixed
- ⚠ **回填工具的幂等检测前缀太严**（差点造成重复插入）：
  初版标记 `- **符号 \`sign\`：`（带粗体），而"未落地"那条写的是 `- 符号 \`sign\`：**未记录**`
  （**开头不带粗体**）⇒ 检测不到 ⇒ 第二次运行会**再插一行** ✗
  ⇒ 改宽松标记 `符号 \`sign\`：`（两种形态都覆盖）✓
  > 幸好第一次跑的是 **DRY-RUN** —— 这是"默认 dry-run"这个设计**当场救了一次** ✓

### Notes
- `tools/_test_reports_expr.py` **27 → 34 项**：新增第 [5] 段
  「所有 `factor_library*.md` 的**每个明细块**必须含 `sign` 且不重复」+
  crosspool 表头含 `sign` 列 + `_lib_sync` 会给新因子写 sign 行
- ★ **负向验证**（`ai_test/_verify_sign_check.py`）：删掉一条 `sign` 行 ⇒ 测试**确实 FAIL**
  （精确报 `2/3`）⇒ 还原后 PASS ✓ —— **又一次证明测试有效**

---

## [0.9.1] — 2026-09-14

> 主题：**修 `factor_pool_selected.md` 的表达式被截断**（用户反馈）
> 用户原话：「几个精选因子的表达式好像都被截断了嘛，是因为要对齐还是啥原因？
> 那这个不行啊，我肯定要完整表达式啊，**不然我还得费劲去找对应的因子库去翻表达式**」

### Fixed
- 🔴 **`tools/cross_pool_review.py`：精选清单的表达式被硬截断到 58 字符** ✗
  根因：`meta[n]['expr'][:58]`，而 **`factor_pool_selected.md` 是"只有一个表格、
  没有明细段"的文件** —— 对比 `factor_library*.md` 是「**总览（截断 + `…`）+ 明细（全文）**」两层，
  所以它**截了就真没了** ⇒ 用户被迫去翻别的文件 ✗
  ⇒ **表格不再截断**（实测最长 **58 → 189**）✓
- ⚠ 顺带修一个**静默隐患**：Markdown 表格里若出现 `|` 会**切断单元格**
  ⇒ 新增 `_cell()` 统一转义 ✓

### Added
- **`factor_pool_selected.md` 新增「精选因子明细（可直接复制使用）」段** —— 每个精选因子一节：
  - **完整表达式**（代码块，可直接复制）
  - ★ **`sign`（方向，必须乘）** ← **这条原来根本没有！**
    引擎求值时对 `sign<0` 的因子**取负**；下游直接排序选股不乘它 ⇒ **方向反了、组合反向选股** ✗
    （本次 7 个里 **6 个 `sign=-1`** ⇒ 中招概率很高）
  - **因子值 h5 路径** + 快查副本（uint8）路径 —— 下游不用再找 ✓
  - 形状 / 来源 / 建于 / 剥风格判定 / 剥风格与原口径的 Calmar·超额·IC
- `tools/_test_reports_expr.py`（**27 项**）—— 报告表达式完整性回归测试

### ★ 测试编码的规则（区分"可以省略"与"不可以省略"）

| 文件类型 | 允许省略? | 理由 |
|---|---|---|
| 有**明细段**的（`factor_library*.md`）| ✅ 总览可省略 | 全文在明细段，**查得到** |
| **没有明细段**的（`factor_pool_selected.md`）| ❌ **绝对不许** | 截了就**无处可查** |

四层断言：① **反引号包裹的表达式括号必须配平**（合法表达式必配平 ⇒ 不配平 = 被切了，
比"按长度猜"可靠）② 代码块里的也必须配平 ③ **没有明细段的文件 0 个 `…` 省略号**
+ 必须给 `sign`/h5 路径 ④ **生成器源码里不许有 `[:N]`**（防手滑加回去）
> ⚠ 已知盲区（已写在 docstring）：**不加反引号**的截断（`factor_library*.md` 总览表里的 `…`）
> 抓不到 —— 那是有意的（总览是导航，全文在明细段）。

### Notes
- ★★ **做了负向验证**（`ai_test/_verify_test_catches.py`）：
  **把 `[:58]` 打回去** → 重新生成 → 跑测试 ⇒ **确实 FAIL**（且精确指出"最长 58"+"实得 ['58']"）
  → 还原 → 再跑 ⇒ PASS。
  > **一个"永远通过"的测试没有价值** —— 必须证明它真的抓得到它要防的那个 bug ✓
- ⚠ **新发现的同族缺口**（已记 `loop_todo §1.22`，**需用户拍板**）：
  `sign` 在**其它 5 个报告里全缺**（`factor_library_crosspool.md` + 4 个 `factor_library*.md`）
  ⇒ 同样是"不乘就方向反"的风险。**没直接改**：它们是 **append-only**、加列会动既有表格结构，
  若下游有按列解析会受影响。低风险做法已写在 §1.22。

---

## [0.9.0] — 2026-09-14

> 主题：**第二批改造 ② 亲本选择策略 `top_percent_plus_random`**（治 `loop_todo §1.3-C`）
> 用户指令：「**接着做 §1.3-C**」

### Added
- **`engine/loop_engine.py`: `--parent_sel {uniform,best,top_percent_plus_random}`**
  （**默认 `uniform` = 行为不变**）+ `--parent_top_pct`（默认 `0.30`）
- 纯函数 **`pick_parent(rng, seeds, mode, thr)`** + 模式表 `PARENT_SEL_MODES`
- 两处亲本抽取（**变异的第一亲本** + **交叉的第二亲本**）都改走 `pick_parent`
- `tools/_test_parent_sel.py`（**20 项**）

### Why（为什么值得做）

原来两处抽取都是 `rng.choice(seeds)`（**全池均匀随机**）
⇒ ★★ **L1 里第 1 名和第 30 名被选中的概率完全一样 —— 排名信息一点没用上**。
我们只有「**堵**」的手段（`--fam_quota` 配额 / `fam_block_thr` 黑名单 / `--decorr`），
**没有「疏」**（显式的探索/利用配比）—— 正是 roadmap §8.21-⑥ #5 指出的缺口。

依据：QuantaAlpha `configs/experiment.yaml:83-92` + `pipeline/evolution/crossover.py:423-437`。
它是**批量选名额**（top 30%% 保底 + 从 rest 随机补齐）；
**我们是逐个抽亲本** ⇒ 等价实现 = **以 `thr` 概率取 top 段、否则从全池随机**。

### ★★★ 头号坑：一个"写错就静默失效"的实现细节

「否则」分支**必须是「从全池随机」**，**不能**写成「只从 rest 随机」：
```python
# ✗ 错：rng.choice(seeds[:n_top] if rng.random() < thr else seeds[n_top:])
#   n=20, n_top=6：P(某个 top)=0.3/6=0.05, P(某个 rest)=0.7/14=0.05 **完全相同** ⇒ 退化成 uniform
# ✓ 对：rng.choice(seeds[:n_top]) if rng.random() < thr else rng.choice(seeds)
#   P(某个 top)=0.3/6+0.7/20=0.085, P(某个 rest)=0.7/20=0.035 ⇒ **2.43×**
```
> QuantaAlpha 的 `rest_candidates` 只是"**补剩余名额**"的来源，**不是唯一来源**。
> ⇒ 已写成回归测试 **统计检验**（`比值 ≈1.0 会 FAIL`）✓ ⇒ 实测 **2.416**（理论 2.429）✓

### Changed
- 新增**可审计**输出（这是 §1.1/§1.15 的教训：**动作必须可见，否则无法归因**）：
  - `本代参数:` 旁 → `本代亲本策略: parent_sel=... 种子池=N个(按score降序)`
  - 生成段 → `[亲本] 策略=...（top 30%% 保底 + 余量随机；种子池 N 个 ⇒ top 段 M 个）`
  - **A角 LLM 上下文**里也写（让它知道亲本怎么挑的，好补上策略造成的多样性缺口）
  - ⚠ **不改 journal 格式**（会打断下游解析）
- `tools/run_tracks.py` 文档补：用 `--extra=--parent_sel=top_percent_plus_random` 透传
  （`--extra=` 机制已存在，**无需改代码**；`透传引擎参数:` 那行已把它记进 driver 日志 ✓）

### ⚠ Notes：效果**尚未测量**

本次交付 = **实现 + 测试 + 接线**；**"换策略后产出是否变好"没有 A/B 实测** ⚠
（§1.1/§1.15 的教训正是「**动作施加了但完全无效**」⇒ 不能假设"抄了对的东西就有效"）。
A/B 方法已写进 `loop_todo §1.3-C ⑤`。

### 验证
- `--help` 13,067 字符、**无 Traceback**（⚠ 本次特意全程用 `%%` —— v0.8.0 刚被裸 `%` 坑过）
- ★ **真机冒烟**（用 `--gen_only`：`只跑候选生成段、不跑 L1/L2、**不写状态**`）：
  ```
  本代亲本策略: parent_sel=top_percent_plus_random(top_pct=0.30)  种子池=20个(按score降序)
  [亲本] 策略=top_percent_plus_random（种子池 20 个 ⇒ top 段 6 个）
  退出码=0 · 无 NameError/Traceback · loop_state.pkl SHA256 未变 ✓
  ```
- 回归 **20/20**：★ `uniform` 与旧写法 `rng.choice(seeds)` **逐位计数完全相同**（⇒ 默认行为真没变）·
  `top_percent_plus_random` **比值 2.416**（理论 2.429）· 边界（空池/单元素/未知模式/`thr=1.0`）·
  静态断言（两处接线 + `pick_parent` 外无遗留直抽）
- 其余回归：`_test_inject_pools` 17/17 · `_test_daily_dd` 15/15 · `_test_ok_gate` 22/22 ·
  `_test_critic_sensor` 32/32 · `_test_build_facs_merge` 11/11 · 引号检查全绿

---

## [0.8.0] — 2026-09-14

> 主题：**第二批改造 · ① 给全A 轨道注入池库对照集**（治 `loop_todo §1.8`）
> 用户指令：「**先做 §1.8**」

### Added
- **`engine/loop_engine.py`: `--inject_pools=300,500,1000`** —— 把**其它池的库只读注入**成对照集。
  - 新增 `bank_ext`（供 `--decorr` 的因子值相关性对照）+ `bank_ex_ext`（供 `--dup_ex_corr` 的收益流对照）
  - 新增纯函数 `_cmp_lib(bank_ex, bank_ex_ext)`：合并语义 = **自己的优先**；`ext` 为空时**返回原对象**
    （零拷贝 ⇒ 零开销）
  - **`--decorr`** 的对照集改为 `bank + bank_ext`
  - **`ex_max_corr` 的两处调用**（记录 `max_ex_corr` + 入库拦截）都改走 `_cmp_lib(...)`
  - **默认空 ⇒ 行为完全不变**
- **`tools/run_tracks.py`: `--inject_pools`** —— 默认**自动**给 `all` 轨道注入"本次要跑的其它池"；
  `--inject_pools=none` 可关；`--dry` 的 PLAN 里**显式打印** `[注入对照集] 300,500,1000` + 为什么
- `tools/_test_inject_pools.py`（**17 项**）

### ★★★ 设计要点：**为什么必须"只读注入"，不能并进 `bank`**

`bank` / `bank_ex` 是**本轨道的产出** —— 它们会被**持久化进 `loop_state*.pkl`**
并被 `_lib_sync` **写进 `docs/factor_library*.md`**；而本循环还会往 `bank_ex[expr] = _ex` 写
（入库即进对照集）。

⇒ 若把外部池的几十条**直接并进** `bank_ex`，它们会被**当成"本轨道自己入库的因子"
写进 state 和因子库** ⇒ **因子库凭空多出一批不是它挖的因子** ✗

**语义**：外部池库只能"**告诉我这些已经挖过了**"，**不能算我的产出**。

### ★★ 为什么**只给 `all` 注入、池轨道不注入**（不对称是刻意的）

| 轨道 | 痛点 | 注入? |
|---|---|---|
| `all` | **重挖池库**（实测池因子 vs 全A 库 收益流最大 \|相关\| **中位 0.767**、>0.7 占 **82%**，而 `--dup_ex_corr=0.90` 只挡得住 18%）| ✅ **必须** |
| `300/500/1000` | ★ **产出不是"新因子"，而是"有效域标签"** —— 同一个式子（或已被全A 库高相关因子覆盖的式子）**全A 库可能早就有了**，池轨道真正的新增信息是「它在各池分别有没有效」| ❌ **不能** |

⇒ 若给池轨道也注入全A 库，那些"已存在但没打过池标签"的式子会被**对照集挡在门外**
⇒ **池轨道就没产出了** ✗（把它的唯一价值掐掉）

### ⚠ Fixed（实施中踩到的坑）

- 🔴 **argparse 的 `help` 字符串里任何字面 `%` 必须写 `%%`** —— 否则 **`--help` 与 `parse_args`
  双双崩溃**（`ValueError: unsupported format character`）⇒ **引擎完全起不来** ✗
  > 实录：`--inject_pools` 的 help 里写了裸 `82%`。**被"每次改动都跑一次 `--help`"的冒烟测试抓到** ✓

### Notes
- **验证**：`--help` 11,964 字符无 Traceback · `--pools=300,500,1000,all --dry` 显示
  **只有 `all` 那行**带 `[注入对照集]` · `--inject_pools=none` 正确关闭 ·
  **穷尽检查**：`bank` 的全部赋值点只有 `[]` / `st.get('bank')` / `bank.append(nd)`，
  **没有一处来自 `bank_ext`** ✓
- **回归**：`_test_inject_pools` **17/17** · `_test_daily_dd` 15/15 · `_test_ok_gate` 22/22 ·
  `_test_critic_sensor` 32/32 · `_test_build_facs_merge` 11/11 · 编译 **69 个文件全过** · 引号检查全绿

---

## [0.7.1] — 2026-09-14

> **PATCH / 纯文档** —— 落下**第二批改造清单**（`loop_todo §1.21`）

### Added
- `docs/loop_todo.md §1.21` —— **第二批·引擎行为族改造**（用户已授权「趁没跑都改造了」，分批执行）：

  | # | 事项 | 改动点 | 风险 |
  |---|---|---|---|
  | **§1.15** | 规则1 的「**动作有效性**」检测（现在只记"施加次数"，不记"施加后指标变好没"）| `loop_critic.py` | 中 |
  | **§1.1 遗留** | **参数棘轮**（只停手不回退）| `loop_critic.py` | 中 |
  | **§1.8** | 给**全A 轨道注入池库对照集**（否则重挖 82%）| `loop_engine` state 加载处 | **低** |
  | **§1.3-C** | 补 `top_percent_plus_random` 亲本选择策略 | 生成端 | 低 |

  **建议顺序**：`§1.8`（低风险高收益）→ `§1.3-C`（小）→ `§1.15 + §1.1`（同族，一起做一起验）
- **分批理由**（写进文档）：**一次改 5 处，出问题无法归因** ⇒ 第一批（判据与口径族）已完成于 v0.7.0。

---

## [0.7.0] — 2026-09-14

> **第一批改造｜判据与口径族**（用户拍板「**① 取 0.30（日频）+ ② 加 dd_d > -0.20**，采纳，
> 趁没跑都改造了」）。本批 = **`§1.18-B + §1.19-③ + §1.5`** —— 三项强耦合，一起改一起验。

### Changed（★ 引擎行为：**变**）

- **`loop_pools.strip_grade()` 口径改为「日频」+ 新增日频回撤判据**（§1.19-③）
  - 签名：`strip_grade(calmar, ann_ex, dd_d=None)` —— **参数名去掉 `strip_` 前缀**，
    正是提醒「**传什么口径由调用方负责**」；docstring 写明**必须传日频**。
  - **A 档 = `calmar >= 0.30`（日频）且 `dd_d > -0.20`**（新增）
    ★ 回撤超限**只降到 B、不判 C** —— "回撤大"与"是纯风格"是两种不同的病，混在一起会误导。
  - `dd_d=None` ⇒ **不启用**回撤判据（**向后兼容**：老调用方行为不变）
  - 新增常数 `TAG_STRIP_DD_MIN = -0.20`
- **`loop_pools.TAG_POOL_FLOOR_CAL = 0.15` 新增**（§1.18-B）—— 修**判据不对称**：
  原来「全A 要 `calmar>=0.30`、**池内只要超额>0**」⇒ `all3`（"所有池都通过 = 真 alpha"）名不副实
- **`loop_engine.py` 的 `_okp` / `_oka` 改用日频 Calmar**（缺失**回退期频**，不炸不误杀）+
  `_okp` 加 `>= TAG_POOL_FLOOR_CAL`
- **L2 三处回测全部开 `with_daily=True`**（主口径 / 剥风格 / 池内）⇒ 引擎每代多花
  ~4s/候选 + ~12s/候选（池内 3 池）—— 因为**判据已吃日频**，必须算
- **`loop_pool_obs*.csv` 新增 `pools_scope` 列**（§1.5）—— 记录**当次 `--pools` 集合**，
  否则"只在两池测过"会被误读成"池内无效"（跨批次比标签会错）
- **`loop_archive*.csv` 新增 `dd_d` / `calmar_d`** —— 供**离线重算池标签**用
- **因子库文档的「剥风格」行补日频**：`原 Calmar → 剥后 Calmar；…；**日频** 剥后 Calmar，
  日频回撤 X%（劣于上限则标注"只给 B"）`
- 离线派生脚本同步：`standard/pool_tags.py`（新增 `_cal_d()` 日频优先回退）+ 池内卡 Calmar ·
  `tools/build_crosspool_view.py` · `tools/backfill_library_pool.py` · `tools/build_facs.py`
  （`LE_pools_grade(calmar_d, ann_ex, dd_d, calmar_p)` 日频优先）

### ★★ 端到端验证（§1.18 修复的实证）

**`F10_1000`（gen7）的池标签：`all3` → `csi1000_only`** ✓✓
```
calmar_300   0.0589   ← < TAG_POOL_FLOOR_CAL(0.15)  ✗ 不通过
calmar_500   0.0233   ← < 0.15                       ✗ 不通过
calmar_1000  0.3834   ← ≥ 0.15                       ✓ 通过   ⇒ n_pool_pass=1
```
**旧判据（只要超额>0）**：300 +2.39% / 500 +0.45% / 1000 +4.99% ⇒ 全通过 ⇒ **`all3`** ✗
⇒ ★★ **与外部独立审查「300/500 内不可用」的结论完全闭环** ✓

**重算后 `all3` 从一批变成 0 个** ⇒ **"真 alpha"这个语义承诺现在才成立** ✓
（`pool_tags.py` 重算：`{'none':131, 'csi1000_only':56, 'csi1000_500':15, 'csi500_only':11,
'csi300_only':7, 'csi1000_300_500':6, 'csi1000_300':3, 'csi300_500':2}`）

### Fixed
- `tools/_test_build_facs_merge.py` 修复自身缺陷：`COLS` **不再硬编码**（本次加列把它炸了）
  ⇒ 改为运行时从真实 CSV 取列，避免"被测系统加列、测试自己先炸"

### Notes
- **第二批（未做，引擎行为族，互相独立）**：`§1.15`（规则1 的「动作**有效性**」检测）·
  `§1.1 遗留`（**参数棘轮**：只停手不回退）· `§1.8`（给全A 轨道**注入池库对照集**，否则重挖 82%）·
  `§1.3-C`（`top_percent_plus_random` 亲本选择策略）

---

## [0.6.1] — 2026-09-14

> **PATCH** —— 全库日频实测后，**更正 v0.6.0 里的一处过度陈述** + 新增标定工具

### Fixed（文档更正）
- 🔴 **「期频回撤被**系统性**低估 ~3.6pp」用词错了** —— 那是 `F10_1000` 的**极端个案**，
  **不是普遍规律**。全库 50 个因子实测（`tools/calib_daily_threshold.py`）：

  | 口径 | 折算比 `strip_calmar_d / strip_calmar` |
  |---|---|
  | 全部（N=50）| 中位 **0.977** · 四分位 0.913~0.991 |
  | **仅正 calmar（A/B 档，N=31）** | 中位 **0.928** · 四分位 0.891~0.974 · 区间 **0.741~0.992** |

  ⇒ **回撤平均只放大 ~1.08×，不是 1.46×** ✗
  ⇒ **§1.19 的正确结论**：✓ **方向对**（日频更准，值得采用）；✗ **幅度逐因子差异极大**（0.74~0.99）
  ⇒ ★ **日频口径的价值在于「揪出个别高回撤因子」，不是「整体平移」**。

### Added
- `tools/calib_daily_threshold.py` —— 期频 vs 日频对照 + **档位阈值标定**（3 段输出）：
  ① 折算比分布（**只对正 calmar 统计**，因为负超额因子折比 ≈1 无信息量）
  ② 逐因子对照（A/B 档）
  ③ ★★ **阈值敏感度表** ⇒ 让"新阈值定多严"看着数字决定

### Notes（★ 两个新结论）

**① §1.19-③ 的净收益不是「改阈值」，而是「换更准的口径」**

| **日频**阈值 | A 档 | B 档 | 相对"期频 0.30"的保留率 |
|---|---|---|---|
| 0.15 | 21 | 10 | 124% |
| 0.20 | 20 | 11 | 118% |
| **0.25** | **17** | 14 | **100%** ← 与现状**完全相同** |
| 0.30 | 15 | 16 | 88% |

（现状 = **期频** 0.30 ⇒ A 档 **17** 个）
⇒ 因为折算比中位 0.928（不是 0.6~0.7），**日频阈值 0.28~0.30 就与现状等效** ✓

**② ★ 新洞察：A 档判据应加「日频回撤上限」**

有一批因子 **calmar > 0（现行判据判"可用"）但日频回撤高达 −21%~−35%**：
`F03_1000` **−35.3%** · `F11` **−23.1%** · `F40` **−21.2%** · `F04` −17.8% · `F01` −16.3%
⇒ **建议加** `strip_dd_d > -0.20`；对**真中性增强（B 路线）**尤其重要（回撤是产品端硬约束）。
**阈值定多少需用户拍板**（直接决定可用因子数）。

---

## [0.6.0] — 2026-09-14

> 主题：**新增「日频 mark-to-market」风险口径**（治 `loop_todo §1.19`）
> —— 用户拍板「**2 选 ③：重标档位阈值到日频**」
> ⚠ 同时记录用户的两条决策：**§1.18 选 B**（池内判据加 Calmar 下限 + 重算历史标签）·
> **§1.20 铁律**（不要用放宽门槛凑数量）

### Added
- `engine/factor_miner.evaluate_real(..., **with_daily=False**)` —— 新增**日频 mark-to-market**
  风险指标 `dd_d` / `calmar_d` / `sharpe_d`（`with_ex=True` 时另给 `ex_d` 日度超额序列）。
  **默认 False ⇒ 返回结构与行为完全不变**（老调用方零影响）。

  **为什么需要**（§1.19）：现状 `nav=(1+ex).cumprod()` 而 `ex` 是**每换仓期**一条
  ⇒ 净值**只在期末打点** ⇒ **漏掉持有期内的日内回撤** ⇒ 回撤**系统性低估 ~3.6pp**、Calmar **高估 ~1.4×**。

  ★★★ **实现要点（两个坑，都写进 docstring 了）**：
  1. **不要用 `set_fwd(1)` 得日频** —— 那会变成「**每天调仓**」（换手 ×5、成本 ×5）= **另一个策略** ✗
     正确做法：**保持 `FWD` 调仓，在持有期内逐日 mark**（复用同一个 `keep_top`/`keep_all` 持仓，
     **不重新选股**）⇒ 每因子只多 **~4 秒** ✓
  2. **必须把每期的日超额「锚定」到期频值** —— 即等比缩放使 `prod(1+seg) == 1+ex_e`。
     否则两条净值路径**不是子采样关系**（逐日算术和 ≠ 期收益的复利）⇒ `dd` 不可比、
     `calmar_d ≠ ann_ex/|dd_d|` ✗
     **锚定后**：期频净值 = 日频净值在每个期末的取值 ⇒ **`dd_d <= dd_e` 严格成立**（数学恒等式）
     ⇒ 可被单元测试**钉死** ✓

- `tools/_test_daily_dd.py` —— **15 项**回归测试。核心是三条**不变量**（不是"和某个数字对得上"）：
  · `dd_d <= dd_e`（子采样只能看到更少的极值）✓
  · `calmar_d == ann_ex/|dd_d|` 严格成立（只有回撤换了口径）✓
  · **逐点抽查 418/418 个期频点与日频曲线重合**（锚定的直接验证）✓
  另含"默认 `with_daily` 不产生任何新键 / `dd` 逐位相同"的行为兼容性检查 ✓

- `tools/build_facs.py` 新增**日频列**：`dd_d` / `calmar_d` / `strip_dd_d` / `strip_calmar_d` /
  `strip_sharpe_d`（`docs/loop_strip_style_bank.csv` 列定义同步更新）。
  ⚠ 加列**不会**让历史行错位（`_merge_csv` 用 `DictWriter` 按 key 写 + `setdefault` 补空；
  §8.30 那个坑只发生在"裸行写入"的场景）。

### Notes（与外部独立审查的对拍）

| 指标 | 引擎（期频）| 引擎（日频）| 外部审查（日频）|
|---|---|---|---|
| `F10_1000` dd | −7.77% | **−10.26%** | −11.42% |
| `F10_1000` calmar | 0.8911 | **0.6748** | 0.627 |
| `F10_1000` sharpe | 1.0592 | 1.1190 | 1.139 |

⇒ **方向与量级完全一致**（回撤放大 **1.32×**、Calmar 高估 ~1.3×），但 dd 有 **~1.16pp** 差异
⇒ 差异应来自**基准腿口径 / 停牌处理**的细节（外部用 `standard_test.py`，参数与 `evaluate_real` 未必全同）
⇒ **不影响结论**（"期频系统性低估回撤"成立）✓ 但**报告时要注明口径**。

### Notes（待办，已拍板）

- **§1.18 选 B**：`loop_engine.py` 的 `_okp` 加 Calmar 下限 + `loop_pools.TAG_POOL_FLOOR_CAL`
  + **重算历史标签**（数据都在 `loop_pool_obs_*.csv` ⇒ 离线 re-derive，不必重跑引擎）
- **§1.19 选 ③**：本版已产出日频列 ⇒ **下一步按日频分布重标** `TAG_STRIP_CAL_MIN` / `TAG_CAL_MIN`
- **§1.20 铁律**：**不要用放宽门槛凑因子数量**（瓶颈是**信号源多样性**，不是条数）

---

## [0.5.5] — 2026-09-14

> **PATCH / 纯文档** —— 外部独立审查触发，落两条**口径层面的结构性问题**（待拍板）+ 重跑结果

### Notes（★ 外部 AI 独立审查 `F10_1000` 的结果：7 项指标 8/8 全部复现 ✓）

独立审查确认**引擎自报值可信**，但指出两个问题 —— **我复核后两条都成立**：

🔴 **新增 `loop_todo §1.18`：`all3` 标签的判据比入库门槛松得多**
```python
engine/loop_engine.py:2001-2006
_okp = {q['pool']: (ann_ex > TAG_POOL_FLOOR)}      # = 0.0  ← 池内**只要超额>0**
_oka = (ann_ex > 0 and calmar >= TAG_CAL_MIN)      # = 0.30 ← 全A **要卡 calmar**
```
**判据不对称** ⇒ `derive_tag` 的 `all3`（"全A 且所有池都通过 = **真 alpha**"）
实际只要求"池内超额 > 0"，**完全不卡 Calmar** ✗

实测 `F10_1000`：300 池 calmar **0.0589**、500 池 **0.0233**（**均 < `--min_pool_calmar=0.15`**，
入库门槛判**不通过**），但标签判据判通过 ⇒ **库文档标成 `all3`**；
而独立审查的日频复核：300 池**日频回撤 −41.28%**、日频 Calmar 0.06、500 池超额仅 +0.41%
⇒ **大中盘段无效** ⇒ 下游按 `all3` 筛"真 alpha"会**误选**。

🔴 **新增 `loop_todo §1.19`：引擎侧风险指标是「期频打点」口径**
`factor_miner.py: FWD=5`（**无人调用 `set_fwd`** ⇒ 全局周频）+ `nav=(1+ex).cumprod()`
只在**每期期末**打点 ⇒ **回撤系统性低估 ~3.6pp、Calmar 高估 ~1.4×**。
实测折算比（F10）：期频 0.891 → 日频 **0.627**；剥后 0.587 → **0.366** ⇒ **日频 ≈ 0.6~0.7 × 期频**。
**影响面**（系统性）：L2 `calmar/dd` · `loop_strip_style_bank.csv` · `loop_pool_obs_*.csv` ·
journal `fail_calmar` **全是期频** ⇒ 档位阈值 `TAG_STRIP_CAL_MIN=0.30` 与池门槛 `0.15` **都偏松**。
⇒ ★★ **挖掘侧（期频）与组合侧（`combo_constrain` 逐日净值 = 日频）Calmar 不可直接比** ——
这**解释了 §1.4 里"挖掘侧 0.5~0.9 vs 组合侧 S0 2.515"的量级差异**。
**日频能力已现成**（`standard/standard_test.py --freq=1`）⇒ **不必改引擎**。

### Notes（重跑 1000 池 gen7/8/9 完成）

- **gen7** 56.1min 入库 1 · **gen8** 64.6min +0 · **gen9** 55.9min +0 ⇒ **bank 3 → 4**
- ★★ **收尾① 从 23.7 min 降到 2 秒** —— **`--only-new` 终于生效（提速 ~700×）**
  （上次没生效：那个 `run_tracks.py` 进程的内存里是改动前的代码）
- ★★ **§1.17 端到端验证通过**：`gen>=7` 入库的 1 个（`F10_1000`）**是 A 档**，**C 档零新增**
  ⇒ 对比修复前 8 代出 6 个 C 档（60%）· 修复后 **0%** ✓
- 全库 56 因子：**A17 B14 C25** · L3 精选 **7 个**
- ⚠ **产出率极低**（3 代只出 1 个）⇒ 印证 §1.4 的警告「B 真中性路线要 20~50 个因子，按此速度差很远」；
  且**候选数持续萎缩**（L2 `30 → 19 → 15`，冻结骨架 4 个、失败库 384 条）

---

## [0.5.4] — 2026-09-14

> **PATCH**（新增工具，不改引擎行为）—— 补上 §1.17 验证清单第 ④ 条：**防回归断言**

### Added
- `tools/pool_grade_summary.py` — **各池入库因子的剥风格档位汇总 + 防回归告警**。
  **动机**：§1.17 那个 bug 能潜伏一天多，唯一原因是**没人在看"入库因子的档位分布"**
  （要人工偶然跑 `build_facs.py` 才碰得到）⇒ 做成**随时可跑的一行命令**。
  - `--since-gen=N` —— 只看第 N 代及以后入库的（**验证修复**用）
  - `--warn-c=0.30` —— C 档占比超阈即报 `[!]` 并**返回退出码 1**（便于将来接进收尾做硬断言）
  - 保留**历史记录**（不隐藏已剔除的 C 档）—— 审计需要看到"曾经混进来过"

### ★★ 端到端验证通过（§1.17 修复的实证）

**只数 `gen>=7`（即修复后）入库的**：

```
## pool=1000   （1 个）
  [A]   F10_1000     原Calmar=0.89107  剥后Calmar=0.5867438   cs_demean(mul(add(turn_ratio, ts_mean60(barr
  档位: A×1   ⇒ C 档占比 **0%**
  [OK] C 档占比未超阈（0% ≤ 30%）
```

对比：**全部 10 个（含修复前的 6 个 C 档，历史保留）** ⇒ `A×2, B×2, C×6` ⇒ **C 占比 60%** `[!]`

⇒ **修复前 8 代出 6 个 C 档（60%）· 修复后第 7 代出 A 档（0%）** ✓✓

★ 附带收获：`F10_1000` 的**剥后 Calmar = 0.587**，是 1000 池目前**剥风格后最好的因子**
（超过 `F07_1000` 的 0.519），原口径 Calmar 0.891、IC +0.0469。

---

## [0.5.3] — 2026-09-14

> 主题：**修 `--pool_gate_or_all` 静默绕过两道硬门槛的 bug**（`loop_todo §1.17`）
> + 用户拍板**产品定位 = B. 真中性增强**（`loop_todo §1.4`）

### Fixed
- 🔴 **`--pool_gate_or_all` 的快照点取错 ⇒ 「剥风格门槛」与「分段独立验证」被静默绕过**
  （`engine/loop_engine.py`）。原实现：
  ```python
  _ok_prev = ok                 # 快照取在 _ok_q **之前**
  ok = ok and _ok_q
  ok = ok and seg_ok            # 分段独立验证
  ok = ok and strip_ok          # 剥风格门槛
  ...
  elif _pool_gate_or_all:
      ok = _ok_prev and (_ok_q or _pok)   # ★ 用旧快照**整个重建** ⇒ seg/strip 一起被撤掉
  ```
  注释本意是「只撤掉 `_ok_q`」（否则 OR 退化成 AND），但快照取早了。
  ⇒ **`--pool_gate_or_all` 一旦开启（§8.26，2026-09-13 起），这两道硬门槛完全失效且无告警**。
  **实测后果**：池后缀 13 个入库因子里 **C 档（纯风格）8 个 = 62%，`strip_calmar` 全为负**，
  本该被 `--min_strip_calmar=0.15` 全部拦下 ⇒ 也是 `§1.9`「库约一半是纯风格」的真因。
  **修法**：把判定组合提取为**纯函数** `loop_engine.combine_ok(ok_base, ok_q, ok_pool, ok_hard,
  pool_gate_on, or_all)` —— **OR 只作用于「全A 口径 vs 池内口径」这个二选一**；
  `seg_ok` / `strip_ok` 另存进 `_ok_hard`，**最后统一 AND 回来，不参与 OR**。

### Added
- `tools/_test_ok_gate.py` — **22 项**真值表回归测试。核心用例：
  `ok_q=False, ok_pool=True, ok_hard=False`（池内达标但剥风格失败）⇒ **必须不通过**
  （旧实现给"通过" ⇒ 直接复现漏拦）；并含**新旧公式同输入对比**，证明本次修复确实改变行为。
- `tools/prune_style_only.py` — 从某个池的 state 剔除**纯风格（C 档）**因子。
  为什么必须剔：`--dup_ex_corr`/`--decorr` 的对照集是**各自轨道的 bank** ⇒ 这些因子会**挡住新因子**
  （自我封锁）；且合成/组合会**自动带上**它们，污染 S2（真中性）口径。
  安全设计沿用 `cleanup_repo.py` 风格：**默认 DRY-RUN** + **先备份** + **原子写** + **只按判据删**
  （对不上的 expr **一律保留**，宁可留不误删）。**只改 state**；`docs/factor_library_*.md` 与 `facs/`
  保留历史记录（可追溯"这些因子确实被挖出来过，只是当时门槛没生效"）。

### Changed
- **1000 池 state：`bank` 9 → 3**（剔除 6 个纯风格因子）。保留的 3 个正是"剥风格后仍有效"的：
  `F01_1000`(B) · `F03_1000`(B) · `F07_1000`(A)。
  备份 `ai_test/_state_prune_1000_20260914_163627.pkl`（可人工恢复）。
- `docs/loop_todo.md §1.4` 记录用户拍板：**产品走「B. 真中性增强」**（市值+行业中性，S2 口径）；
  §1.2 / §1.8 的**两条错误结论已更正**（见下）。

### Removed
- 3 个兼容垫片 `ai_test/{run_tracks,build_facs,cross_pool_review}.py`（**轨道已结束**）。
  ★ **实测有效**：本次收尾①② 都走的是 `ai_test/` 路径，没有垫片就会直接失败 ✓

### Notes
- **两条被更正的结论**（都是此前写错的，已在 `loop_todo` 与 `MEMORY.md` 标注）：
  ① 「组合侧支持 1000 ≫ 500 ≫ 300」**不成立** —— `combo_constrain.py --pool=X` **只改选股范围
     （成分股掩码），不改因子集**（`--fac` 默认是全A 41 因子的合成）⇒ 那是"同一组合换选股范围"，
     **不是"各池自己的因子在自己池里的表现"**。正确数字：全A **2.515/+14.46%** > 1000 内 1.192 > 500 内 0.522 > 300 内 0.046。
  ② 「1000 池效率是全A 的 3.6 倍」**不成立** —— 那是未剥风格口径；按「剥风格后仍有效」重算为
     全A 0.33、1000 0.60、500 0.18、**300 = 0** ⇒ **差距缩到 1.8 倍**。
- **本次收尾① 未用上 v0.5.0 的 `--only-new` 提速**（花了 23.7 min）：
  那个 `run_tracks.py` 进程 13:26 启动，**内存里是改动前的代码** ⇒ 仍走全量。下次跑轨道即生效。

---

## [0.5.2] — 2026-09-14

> 主题：**建 `tools/`，把生产管线从 `ai_test/` 迁出并纳入 git**（治 `loop_todo §1.16`）
> 动机：`ai_test/` 被 `.gitignore` 整个忽略，但收尾管线住在里面
> ⇒ **`git checkout v0.x` 回退代码时整条收尾管线会消失**（回退点不完整）。

### Added
- **`tools/`（新目录，纳入 git）— 23 个生产管线/工具脚本**。判据（三层，不靠"被任何文档提到"）：
  | 层 | 判据 | 例 |
  |---|---|---|
  | **A** | 被 `engine/`、`standard/` 的 **.py 代码**引用 | `build_crosspool_view.py` · `add_quant_copy.py` · `bench_quant_read.py` · `backfill_bank_ex.py` · `analyze_diversity.py` · `scan_non_gbk.py` · `qa_loop_pools.py` · `check_strip_style_pool.py` · `probe_models.py` · `l1_shape_calib.py` |
  | **B** | **收尾/轨道管线成员** | `run_tracks.py` · `build_facs.py` · `cross_pool_review.py` · `critic_sensor_report.py` · `fix_csv_schema.py` · `backfill_library_pool.py` · `tracks_status.py` · `journal_view.py` |
  | **C** | 被 `README` 引用 / 回归测试 / 常用质检 | `calib_gates.py` · `_test_critic_sensor.py` · `_test_build_facs_merge.py` · `_check_quotes.py` · `_doc_outline.py` |
  > ⚠ **不要**按"被任何文档提到"扫 —— 那样会得到 **106 个**（含 `round5_fund.py` / `verify_*.py` 等
  > **roadmap 历史叙事里的"当时用过的一次性脚本"**）。判据必须是「**被代码引用 或 属当前管线**」。
- **3 个兼容垫片** `ai_test/{run_tracks,build_facs,cross_pool_review}.py` —— 转发到 `tools/`（`runpy.run_path`）。
  存在的唯一原因：**迁移时轨道正在跑**（`run_tracks.py` 用硬编码 `ai_test/build_facs.py` 调收尾①②）。
  **轨道结束后可 `git rm`。**

### Changed
- **114 处引用** `ai_test/xxx.py` → `tools/xxx.py`：`engine/*.py`(17) · `tools/*.py`(自引用) ·
  `README.md` · `docs/loop_todo.md`(37) · `docs/factor_library*.md` · `docs/factor_pool_selected.md`。
  **不改**：`docs/factor_roadmap.md`（**历史档案** —— 改写历史路径 = 篡改档案，改为顶部加**导航注记**）·
  `docs/history/**`（归档只读）· `docs/loop_journal*.md`（运行日志是既成事实，不可回改）。
- `.gitignore`：`ai_test/` → **`ai_test/*` + 3 个 `!` 例外** + 补 `docs/history/**/ai_test/`。
- `tools/run_tracks.py` 收尾①②改调 `tools/build_facs.py` / `tools/cross_pool_review.py`
  （**新跑的用新路径**；垫片只服务正在跑的那一次）。
- `.codebuddy/`、`ai_test/` 的定位在 README「目录结构」里改写清楚（工具入 git、产物继续忽略）。

### Fixed
- ★★ **`HERE` 产物路径会被劈成两半**：`run_tracks.py` 的 `_tracks`、`critic_sensor_report.py` 读的 `_tracks`、
  `build_facs.py` 的 `_facs_built.csv` —— 若沿用 `HERE`，迁走后新进程会写 `tools/_tracks/`，
  而**正在跑的进程仍在写 `ai_test/_tracks/`** ⇒ 日志/清单分裂。
  **修法**：显式 `os.path.join(ROOT, 'ai_test', ...)`（**代码进 tools/，产物仍留 ai_test/**）。
- ★ **gitignore 回归（我引入的）**：原 `ai_test/`（**无斜杠 ⇒ 匹配任意层级**）改成 `ai_test/*`
  （**带斜杠 ⇒ 锚定仓库根**）后，**误放开** `docs/history/**/ai_test/` 下 **101 个归档文件**
  （`001311` 下 88 · `001515` 下 13）⇒ 补 `docs/history/**/ai_test/`。
- ★ **垫片不能写 stderr**：初版往 stderr 写提示 ⇒ PowerShell 报 `NativeCommandError`，
  且收尾段会检查子进程 stderr 非空并记 `[stderr]` ⇒ 改 **stdout**。

### Notes
- **迁移脚本**（可复跑，带 `--dry-run`）：`ai_test/_migrate_to_tools.py` / `_fix_refs_tools.py` / `_scan_refs.py`
  （留在草稿区不入 git —— 一次性任务，但保留以便复核与回滚）。
- 验证：26 个文件编译通过 · 引号检查全绿 · 两处垫片转发实测（退出码 0）· 回归测试 11/11 + 32/32 ·
  `git status` 仅显示 `tools/` + 3 垫片 · **引擎全程未受影响**（迁移前后 PID 3460/2540 均在跑 1000 池 gen4）。

---

## [0.5.1] — 2026-09-14

> **PATCH / 纯文档**（不改引擎行为）—— v0.5.0 提交后的收尾补充。

### Fixed
- `README.md` 的「已建版本」表：v0.5.0 行由 `HEAD` 改为**实际提交号 `3da89fe`**
  （表格是给人查回退用的，写 `HEAD` 等于没有信息）。
- README 版本表新增**表格约定**：最新一行的"提交"列写 `tag 自身`。
  原因：**一个文件无法记录"包含它自己的那次提交"的哈希**（哈希由内容算出，自指无解）
  ⇒ 与其每版都靠"提交后再补一次"制造永无止境的追赶，不如把这个死结写成显式约定。

### Added
- `docs/loop_todo.md §1.15` — **规则1 也在重演「固定动作」，且连压 13 代完全无效**。
  由 `journal_view.py --index` 意外发现：全A 池 **gen61~gen73 连续 13 代**都在压同一个叶子
  `barra_residual_volatility`，而占比始终回到 62%~100% ⇒ **动作施加了但完全无效**。
  ⇒ 缺的能力：**动作有效性追踪**（现在只记"施加次数"，不记"施加后指标变好没"）。
  建议：施加时登记基线指标，冷却解禁时比对，无效则标记并降权。
- `docs/loop_todo.md §1.16` — **`ai_test/` 全目录被 gitignore，但收尾管线已住在里面**。
  `run_tracks.py` / `build_facs.py` / `cross_pool_review.py` / `critic_sensor_report.py` /
  `fix_csv_schema.py` / `backfill_library_pool.py` / `_test_*.py` 都是**生产路径**，
  而 `.gitignore` 仍写着"临时诊断脚本（可随时删）" ⇒ **约定已漂移**。
  ⚠ 后果：`git checkout v0.x` **回退代码时整条收尾管线会消失**（回退点不完整）。
  已在 `v0.5.0` 的 tag 说明里显式标注；修法（建 `tools/` 迁出管线脚本）待拍板。

---

## [0.5.0] — 2026-09-14

> 主题：**池化挖掘闭环 + B角「诊断→决策」链条修复 + 收尾提速**
> 覆盖 `roadmap §8.24 ~ §8.45`。此提交后入库因子：全A 41 · 中证300 2 · 中证500 3 · 中证1000 6。

### Added（新增）

**因子值落地仓（`facs/`）**
- `engine/factor_store.py` — 因子值持久化仓（2026-09-14）。每因子落 `facs/<hash2>/<name>/`：
  `meta.json`（expr/sign/freq/unit/source/IC）+ `values.h5`（原始 float32）。
  **双写 `values_q.h5`**（uint8 截面分位快查副本）：省 3.99× 空间，**秩类用法（IC/分层/多空/正交诊断）快 14.4×**。
  ⚠ 需要原值的用法（中性化/回归/剥风格/合成）**必须读 `values.h5`**。
- `engine/qa_bench_cw.py` / `qa_ex_dedup.py` / `qa_pool_tag.py` — 三个机制自检脚本。

**分池因子库与跨池视图**
- `docs/factor_library_{300,500,1000}.md` — 分池因子库（池隔离，与全A 库互不读写）。
- `docs/factor_library_crosspool.md` — 跨池视图（一个因子在哪些池达标）。
- `docs/factor_pool_selected.md` — **L2 跨池审查 + L3 精选池**（准入档 A → 相关去重后入选）。
- `docs/loop_journal_{300,500,1000}.md` — 分池 journal（`--mine_pool` 池隔离的必然产物）。

**因子值工具链（`ai_test/`，按 `.gitignore` 本地专用）**
- `ai_test/build_facs.py --only-new` / `--force` — **增量落地**（见 Changed）。
- `ai_test/cross_pool_review.py` — L2 跨池 + L3 精选池报告。
- `ai_test/fix_csv_schema.py` — CSV 混合宽度体检（治 §8.30 的"加列导致下游全崩"）。
- `ai_test/backfill_library_pool.py` — 池因子库补录（治"只做插入不做创建"）。

**B角 机制（`engine/loop_critic.py`）**
- **规则动作登记表 `RULE_NAMES`** — 给每条规则动作稳定 ID（`r1_leaf_conc` … `r7_zero_pass`）。
- **动作饱和检测** — 连续 `SAT_N=2` 代施加同一动作 ⇒ 判饱和 ⇒ **冷却 `COOL_N=3` 代**。
- **LLM 否决通道** — `parse_veto()` 解析 LLM 末行的 `否决: <ID列表或 无>`；
  **连续 `LLM_MUTE_N=2` 次否决 ⇒ 写入哨兵 `MUTE` = 永久停用**（用户要求"让它永久闭嘴"）。
- **诊断新传感量** — `fail_calmar_neg`（`calmar<=0`，**真·信号弱，不随配置漂移**）·
  `fail_pool_calmar`（**池口径** = 池内模式的真实卡点）· `gate_min_calmar` / `gate_min_pool_calmar`（门槛回显对账）。

**引擎（`engine/loop_engine.py`）**
- `_gate_of(args)` — **生效门槛单一事实源**（诊断与判定同源，防漂移）。
- `_pool_best(pool_rows)` — 池口径聚合 `{expr: 最好池 calmar}`，并随 state 存 `last_pool_map`。

**回归测试**
- `ai_test/_test_critic_sensor.py` — **32 项**（门槛对账 / 饱和 / 否决 / 永久哨兵 / parse_veto 容错）。
- `ai_test/_test_build_facs_merge.py` — **11 项**（增量合并不得清空已有数据）。

**文档**
- `change_log.md`（本文件，根目录）。
- `docs/loop_todo.md` — **跨会话任务真相源**：待办表 / 判断分支 / 进度日志 / 已知误报。

### Changed（变更）

- **收尾① 落地改增量**：`run_tracks.py` 收尾① → `build_facs.py --only-new`。
  **`build_facs.py` 在载面板之前三档分流**：`full`（值缺 ⇒ 求值+回测+剥风格）/
  `quant_only`（值在、`values_q.h5` 缺 ⇒ **只补副本，不载面板不重算**）/ `skipped`（都在 ⇒ 跳过）。
  ⇒ **无新因子时连面板都不载**。实测收尾①②合计 **23 min → 4.1 s（提速 ~340×）**；缺 3 个副本 ⇒ 5.9 s。
- `loop_critic.diagnose()` 新增 `gate=` / `pool_map=` 参数；`fail_calmar` 改用**生效的** `min_calmar`
  （原**硬编码 0.5 + 全A 口径**）。
- `loop_critic.ai_review()` 返回值 **`str` → `dict`**（`{'status','veto','text'}`）；引擎把 `veto`
  写进 `next_cfg['_veto']`（键 = **目标代** `gen+1`）⇒ 下一代 `suggest()` **真的跳过**被否决的动作。
- `ai_review()` 的 system prompt 新增第 (4) 项要求 + **末行格式约定**，并把「本代实际施加的动作 ID」
  与「已永久关闭的动作」一并喂给 LLM（不给 ID 它无从否决；不给已关闭名单它会年年否决同一件事）。
- `run_tracks.py` 的 **GUARD 判据重写**：原用 `fail_calmar > 0.9`（该量当时恒为 1.000）
  ⇒ 改为**直接核验命令里有没有那组 flag**（`--pool_obs` / `--min_pool_calmar` / `--pool_gate_or_all`）。
- `--pools=300,500,1000` / `--pool_obs` / `--min_pool_calmar` / `--pool_gate_or_all` 的**池门槛 OR 语义**落地（§8.26）：
  多门 AND 会让入库率**联合归零**，改「任一池达标即达标」。
- `--dup_ex_corr` **收益流去重**（§8.34）：按候选与历代入库因子的**收益流**相关，而非表达式/因子值。
- `--min_strip_calmar` **剥风格后超额**入库判据（§8.14）。
- 组合构建约束（`combo_constrain.py` 方向）：对**真实指数** 000300/000905/000852 计算组合 Calmar。

### Fixed（修复）

- **`fail_calmar` 传感器失真**（§1.1 问题①②）：硬编码 0.5 + 全A 口径 ⇒ 池内模式下**测的不是真正卡住候选的那道门**。
  实测 `pool=1000 gen1` 报 `fail_calmar=1.000`，而同代**确有 2 个候选 Calmar 0.661/0.561 入库**（指标自相矛盾）。
- **规则5 退化成固定动作**（§1.1 问题①）：`fail_calmar` 触发条件 **96/96 代 = 100% 成立**
  ⇒ `depth` 被**永久**推到 `[3,4,4]`、`mix` 交叉撞上限 0.4。
- **LLM 审查只写日志、从不参与决策**（§1.1 问题③）：`ai_review()` 返回值**只用于打印**
  ⇒ **85/98 次**独立反驳全部被浪费，且方向与规则**相反**（LLM 每代建议降深度，B角 每代升深度）。
- **GUARD 必然误报**（§1.1 问题④）：判据用了失真的 `fail_calmar` ⇒ 轨道跑完时报了一次
  「全A 门槛在砍全部门」，而**参数其实传全了**。★ 用不可靠的量当报警判据 ⇒ 必然误报，
  "狼来了"会让真警报被忽略（比不报警更糟）。
- **`_lib_sync` 只做插入不做创建**：池因子库文件不存在时**静默**不建 ⇒ 已修（gen4+ 生效）。
- **`bank` 被截断丢失历史**（§8.17/§8.18）：去掉 `bank` 上限 + 补回丢失因子 + 一致性守卫。
- **候选池萎缩**：`300 gen11` 候选数骤降至 **13**（其它代 29~30）—— 冻结骨架 + 失败库累积到 1582 条所致，已记录待观察。

### Deprecated / Removed

- `docs/loop_archive.csv` 等逐代 CSV 继续按 `.gitignore` 排除（运行产物，非 git 跟踪）。
- `docs/history/cleanup_2026091{4}_*` — 2026-09-14 的归档清理清单（保留落档）。

### Notes（本次会话内验证，未入库的证据）

- 端到端验证：50 池（**冒烟专用池**）全新跑 **3 代**，验证门槛对账 / 传感器"活"了
  （`fail_calmar` 0.900→0.700、`fail_pool_calmar` 1.000→0.900）/ LLM 否决生效 / **连续 2 次否决 ⇒ 永久闭嘴**。
  证据：`ai_test/_smoke50_g{1,2,3}.log` + `docs/loop_journal_50.md`。
- 收尾链手跑：`build_facs --only-new` 2.0s + `cross_pool_review` 2.1s ✓
- ⚠ **已知遗留限制**：**参数棘轮（ratchet）—— 新机制只"停手"不"回退"**。`depth` 被推到 `[3,4,5]` 后，
  即使 LLM 永久否决该动作，`depth` **仍停在 `[3,4,5]`**（而 LLM 建议 `[2,3]`）。是否要做"永久闭嘴时回退基线"**待拍板**。

---

## [0.4] — 2026-09-12

> 主题：**三池并行挖掘 + 池内判据 + 整代末尾崩溃修复**（`roadmap §8.19 ~ §8.23`）
> tag：`v0.4`（提交 `6c60c7f`）

### Added
- **`engine/loop_pools.py`** — PIT 指数成分**单一事实源**：`pool_path/load_pool/pool_union/pool_mask/pool_gate_ok`。
  成分数据 `E:\rq\constituents\index\`（000300 / 000905 / 000852 / 000016）。
- **`engine/loop_metrics.py`** — `neutral_rank()`（风格中性化，与 `standard_test` 【6】同口径）。
- **`standard/pool_tags.py` + `standard/obs_analysis.py`** — 池标签宽表（长表→宽表，喂 PG）+ 观测分析。
- **`engine/qa_mine_pool.py`** — `--mine_pool` 校验（路径派生 / 幂等 / 未知池报错 / 参数注册；
  `--heavy` 含 L1 池并集与 PIT 掩码当期成分数，实测 300 池 925 列 / 中位 299 只）。
  另含"全新轨迹保存状态"的复现路径（用一次性池 50 冒烟）。
- **`docs/loop_todo.md`** — 跨会话任务真相源（轮询者纪律 / 占用锁 / 待办表 / 判断分支 / 进度日志）。

### Changed
- **`--mine_pool=all|300|500`** — 全A / 沪深300 / 中证500 **三条完全独立**轨迹。状态与输出全部按池加后缀
  （幂等，从原始路径快照派生）；**L1 子面板列 = 该池并集**；**L1 的 IC 按 PIT 池掩码算 = 池内 IC**
  （这是"池内挖掘"起作用的核心）。
  依据（§8.13）：全A 含微盘 ⇒ 低流动性溢价让风格因子轻松过关（30/30 的 `lnamt`/`lntr` 全负、剥成交额后仅 3/30 为正），池内不存在该溢价。
- **`--strip_style` / `--min_strip_calmar`** — 入库剥风格判据，口径与 `standard_test`【6】逐位一致
  （rank→逐日截面 OLS 残差→rank→重跑回测），落 `loop_strip_style*.csv`。
- **`--pool_obs` / `--pools` / `--min_pool_calmar`** — L2 在指数成分内重跑，逐池指标落 `loop_pool_obs*.csv`（长表），供入库打池标签（§8.15）。
- **`engine/loop_watch.py`** — `--pool=300|500`：journal / 日志名加池后缀（否则三条轨迹**同名碰撞**）、
  启动引擎透传 `--mine_pool`、`is_engine_running/already_running` 按池区分（否则三条 watch 互相误判）。
- **`.gitignore`** — 数据区改**通配**（`--mine_pool` 会产出带池后缀的变体 `loop_archive_300.csv` 等，
  原精确名匹配不到 ⇒ 会变成"未跟踪"甚至被误提交）；新增 `QuantaAlpha-main/`（外部只读参考，不入库）。

### Fixed
- **全新轨迹（首次运行、无既有 state）崩在整代最后一行**的 `UnboundLocalError`（§8.23）。
  根因：`open(STATE,'wb')` 本身会创建文件 ⇒ 其后 `os.path.exists(STATE)` 恒真，
  而 `st` 只在"载入上一代"处绑定。**破坏**：30 分钟计算白做 + 旧 state 被截断成 0 字节。
  **修法**：① `n_tested` 基准提前到载入处取；② 保存改**原子写**（tmp + `os.replace`）根治半成品状态。

### Docs
- `roadmap §8.19~§8.23`：三池并行提案的离线验证（**「剥风格进 L1 排序」被证伪**）、`--mine_pool` 落地、
  QuantaAlpha 精髓清单、目标函数现状核实、事故复盘。

---

## [0.3] — 2026-09-12

> tag：`v0.3`（提交 `41dbb9d`）

### Added / Changed
- **`new_n` 判定落地** — 形状量改用**风格中性后**收益（判定 ✅）。
- **`--reuse_v`** — 提速 **35%**（含相位插桩与对拍验证）。
- **`--shape_neutral`** — 形状量用风格中性收益的开关。

---

## [0.2.1] — 2026-09-12

> tag：`v0.2.1`（提交 `3fb5f28`）

### Added
- **`shape_pos_n`** — 记录"风格中性后"的**第二套**形状量（批2 候选，**默认仅观测**）。

---

## [0.2] — 2026-09-12

> tag：`v0.2`（提交 `641fe33`）

### Added
- **`--style_obs`** — 风格暴露观测（默认关闭）。

### Docs
- 批1 排序分的**配对实测结论**（`standard/style_paired_analysis.py` → `docs/loop_style_paired.md`）。

### Chore
- 风格观测 / 配对分析脚本移入 `standard/` 纳入跟踪；README 目录结构补 `standard/`。

---

## [0.1] — 2026-09-11

> tag：`v0.1`（提交 `6ed6b00`；主体在 `4ebf734`）

### Added
- **`engine/loop_metrics.py`** — L1 指标层。
- **成本档单一事实源**。
- **`--min_mono` / `--score_mode`**（默认关闭）+ `software_framework.md`。

### Docs
- README 新增**「版本与回退」约定**（每版本 = commit + annotated tag，说明记四项）。

---

## [0.0] — 2026-09-10

> tag：`v0.0`（提交 `e582921`）— **代码基线（改造之前）**

### Baseline
- gen51 **双闸门治同代近重复**；watcher 目标 50 → 70。
- 此前无版本号的历史提交（对应 `v0.0` 之前）：
  `a309a14` 工作区首次提交（Loop 引擎：五维演化 / FSA / 失败库 / gen13 修复）·
  `007c9d7` LLM 双子代理落地 + 五维配比运行态护栏 ·
  `dfebf3e` gen1-22 收官 + 双子/接力/复盘，`factor_library` 收在 9 因子 ·
  `cf4e6bc` **扩叶子池 v3**（`mf16` 资金流 + BARRA11 风格 + 财报 PIT8 共 **54 叶**；`loop_fields.py` 单一事实源）·
  `b5b3894` gen23 新叶首代完成 + watcher 无人值守接力 ·
  `6f4939e` 文档归档整理 + 入库自动同步（`_lib_sync`）·
  `ca09bca`/`807aea1`/`ee10ea0` QuantaAlpha 结构族机制（冗余检测 + 正交换血）。

---

<!--
模板（发布时复制）：
## [X.Y.Z] — YYYY-MM-DD
> 主题：一句话
### Added
- 
### Changed
- 
### Fixed
- 
### Removed
- 
-->

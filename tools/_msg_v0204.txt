v0.20.4 — 文档大复核（loop_todo §0/§4.2/§4.6 过期 + §1.3-A 该归档）

用户：「然后 loop_todo.md 是不是又要清理了？看看里面有没有错的？有没有要归档的？把各个文档再查一轮」

## ★★ 抓到 5 处（全部已修）

### ① `loop_todo.md §0`「现在在哪（一屏速览）」**停在 09-13** ✗✗（最严重）
- 旧文写着：「`run_tracks.py --pools=1000,300,500 --rounds=3` **09-13 21:47 起** · 当前代 `pool=1000 gen=3`」
- **实测**：**无任何挖掘进程在跑**（`--rounds=47` 已于 17:21 自行收尾退出）；各池早已到 75/27/14/12 代
- **旧文的 bank 数也是旧的**：`all` 41 · `300` **1** · `500` **0** · `1000` **3**
  ⇒ **实测（state 权威）**：`all` **41** · `300` **2** · `500` **3** · `1000` **5**
- 旧文参数表**缺 `--style_obs`**（2026-09-15 才补上）
⇒ **已按实测重写**，并加"本节是快照、唯一事实源是代码与数据文件"的维护提醒 ✓

### ② `§4.2`「现役门槛参数」**缺 4 个生产在用的参数** ✗
漏了：`--strip_style` · `--style_obs` · `--dup_ex_corr=0.90` · `--min_strip_calmar=0.15`
⇒ 属"文档副本漂了" ⇒ **已补齐**，并明确「**唯一事实源 = `tools/run_tracks.py` 的 `extra`**」✓

### ③ `§4.6`「关键工具清单」**列了 ~18 个已归档的 `ai_test/*` 工具** ✗✗
`status_all.py` · `night_pipeline.py` · `chain_*.py`(4) · `interpret_valid.py` · `combo_*.py`(3) ·
`_kill.ps1` · `qa_pool_tags.py` … 而 **`ai_test/` 已整体归档到 `history/ai_test_20260915/`**
⇒ 本节自称「**新会话最需要的一页**」⇒ **新会话照着跑会全部失败** ✗
⇒ **已改为只列常驻工具**（`tools/` 54 个 + `engine/` QA + `standard/`），
  并加 `ai_test/` 归档说明（本文其它地方的 `ai_test/xxx` 一律视为归档件）✓

### ④ `§1.3-A` = **「★ 组合构建约束 —— 首版已完成」却留在 `## 1. 🔴 待办` 里，还带 87 行详节** ✗
⇒ 与 `v0.19.0`（P1b 清掉 18 条【已完成】）**同一类病**（说明新条目会再沉淀进来）
⇒ **已归档到 `docs/log/todo_done.md`**，`loop_todo` 只留一行指针 +
  ★ 在待办区加**维护提醒**（"已完成的不要留在本列表里"）防复发 ✓
⇒ `loop_todo.md` **1034 → 1012 行** · `todo_done.md` **1099 → 1186 行**

### ⑤ `README.md` 引用旧名 `tools/calib_gates.py`（已改名 `calib_dedup_leaf.py`）⇒ 已改 ✓

## 新增工具（防止同类问题再漏）
`tools/_audit_doc_refs.py` —— **扫所有文档里引用的仓库内路径，检查是否还存在** ✓
- ★ **关键设计**：**只判"带目录前缀"的路径** —— 初版把**裸文件名**也当路径 ⇒
  把 `loop_todo:351` 的「不能用 `values_q.h5` 做中性化」（那是**泛指名字**，实体在
  `facs/<2位hex>/<name>/values_q.h5`，实测 57 份）**误报成"路径不存在"** ✗
- ★ **按文档分类报告**：`change_log.md`/`docs/log/*.md` = **历史记录** ⇒ 路径失效**是正常的**，
  **不改** ✓（否则会去改历史记录 ✗）
- 效果：活跃文档 56 → **40 处**；剩余 32 处是 `ai_test/*`（已被 §4.6 + README 的全局说明覆盖）、
  2 处是"改名说明"里**有意保留的旧名**、3 处是"已归档"的记录 ⇒ **均属可接受** ✓

## Notes
- **无代码行为变更**（纯文档）· 回归：编译 91 个全过 · 引号干净 · 12/12 测试全过 ✓

## ⚠ 本轮我自己的两个判断偏差（记下避免重犯）
1. `_audit_doc_refs.py` 初版**假阳性满天飞**（裸文件名当路径 ⇒ 228 处）⇒ 修判据后降至 134 处，
   且**必须按文档分类**才看得出真问题 ✗
2. 我一度准备把 `tools/calib_gates.py` 等"剩余失效引用"也"修掉" —— 但它们其实是
   **"改名说明"里有意的旧名**与**"已归档"的记录** ⇒ **再改就是改历史** ✗
   ⇒ ★ 教训：**"文档里有失效路径"不等于"都该改"**，要先分清"活引用"与"历史记录/说明" ✓

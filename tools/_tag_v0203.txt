v0.20.3 — 复核会话结论（更正我写错的"最大函数"）+ 回答「clone 多大 / 跑起来生成多少」

【引擎行为】**无任何变更** —— 本版**只改文档 + 新增只读审计脚本**，未动一行生产逻辑 ✓

【已完成代】未推进。`pool=300` 既有 27 代 · `500` 既有 14 代 · `1000` 既有 12 代（未变）

【关键变更】
 1. ★★★ **更正我自己写错的结论**（用户问「你几乎每轮都会出错，要不要再查一轮」→ 查了，立刻抓到）：
    · `change_log.md` 的 **v0.20.1 条目**曾写「最大函数 **1291 行 → 223 行**（`evaluate_real`/`run_tracks.main`）」
    · ⇒ **错在张冠李戴**：把「`run()` 1291→921」与「`evaluate_real` 222 行（**第 4 大**）」混成一句，
      读起来像「`run()` 已缩到 223 行」⇒ **严重误导** ✗
    · ⇒ **实测真值（AST 权威）**：`loop_engine.run()` = **920 行（仍是全项目最大）** ·
      `loop_critic.suggest` 303 · `run_tracks.main` 240 · `factor_miner.evaluate_real` 222（第 4 大）
    · ⇒ ★ **结论方向不变、程度不同**：`run()` **仍是 920 行的巨型函数**
      ⇒ `loop_todo` 的 **P0-2b（引入 `ctx` 拆剩下 5 个大段）依然必要** ✓
 2. ★ 澄清 P0-1 的「加 1 个算子：10 处 → 1 处」**口径**（回原文核实后）：
    · **原文（`change_log` v0.16.0）**：「**加 1 个算子要同步改 10 处**（**4 个代码文件 + 6 个文档**）」
    · ⇒ 改造后**机械枚举清单**只剩 **1 处**（`engine/ops_registry.py`）✓
    · ⇒ 但 `fastops.py`（**实现**）与 `_test_ops_math.py`（**数值测试用例**）仍必须写 ——
      它们是**职责，不是副本** ⇒ **不算"没消干净"** ✓
      ⚠ 我 2026-09-15 就把这两处**误读成"副本没消干净"** ✗ ⇒ 已写进 `loop_todo` 防止再犯
 3. 新增只读审计脚本：
    · `tools/_audit_clone_size.py` —— **clone 拿到什么 / 跑起来生成什么**（用 `git ls-tree -r -l` 精确算）
    · `tools/_audit_rebuild.py`     —— 重建链条（谁生成哪个大件、依赖哪个外部路径）

【回答用户的问题（数据）】
  · **clone = 3.66 MB**（231 文件）；`engine/` 的**代码**只有 **0.45 MB** ✓ 用户猜对了
  · **跑起来生成 ≈ 8 GB**（**不是 2 GB**）：`engine/` 2.1G + **`facs/` 4.8G** + `strategies/` 0.7G
  · ★★ **家里跑不起来**：唯一外部依赖 **`E:\rq` = 187.95 GB**（米筐 bundle 160G + others 8.7G
    + finance 3.9G + constituents 1.7G + moneyflow 1.3G）⇒ **没有它就是一代都跑不了** ✗
  · 重建顺序：`build_panel.py`（基础）→ `build_mf_leaves` / `augment_panel` / `build_barra` /
    `build_fa_pit`（**幂等扩展**，往 panel 加列）→ `build_universe.py`；
    `tools/build_facs.py` 从 state 导出 `facs/`（**唯一不需要 `E:\rq`**）✓

【回退方式】
  · 纯文档/脚本改动 ⇒ `git checkout v0.20.2 -- change_log.md docs/loop_todo.md README.md`（或整体回退）
  · **未动任何状态文件** ⇒ `loop_state*.pkl` / `loop_archive*.csv` **无需回退** ✓

【⚠ 本项目「出错模式」已归类（供后续自查）】
  我们（AI）在本会话的 9 次错误，**根因只有两条**：
    ① **把文本匹配当结论**（5 次）—— 注释/文档串/docstring 里的名字被当成"引用"
    ② **用转述/记忆代替原文**（4 次）—— 包括本次的"最大函数 223 行"与"P0-1 夸大了"两次判断
  ⇒ **对策**：① 已工具化（`_audit_paths/imports/deadcode` + `_test_ghost_args`，且 `_audit_deadcode`
     显式声明了"动态按名查找抓不到"的盲区）；② **只能靠纪律：任何数字/事实结论，必须在本轮回原文复核**

v0.20.2 — 补上 --style_obs（长期遗漏）+ research/ 归档

【引擎行为】**挖掘结果不变** —— `--style_obs` 本来就存在（默认关），只是从未被 `run_tracks.py` 透传；
             它**只落观测、不进任何选择逻辑** ⇒ 打开它**不改变**选谁入库 ✓
             唯一变化是**数据侧**：`docs/loop_style_obs.csv` 从下一轮起**恢复逐代累积** ✓

【已完成代】未推进（本轮是遗漏修复 + 归档）。
            `pool=300` 既有 27 代 · `500` 既有 14 代 · `1000` 既有 12 代（未变）

【关键变更】
 1. ★★★★ `tools/run_tracks.py` 的 `extra` **补上 `--style_obs`** —— 这是**长期遗漏**，不是有意权衡：
    · 症状：`docs/loop_style_obs.csv` 停更 09-12（那次是我手动跑单代做测量），而引擎一直在跑
    · ★ **成本 ≈ 0（代码级证据）**：`loop_engine.py:2051-2063` —— `style_features(B)` 是**一代只算一次**
      的贵函数，而 `if _style_obs or _shape_neutral or _strip_style:` ⇒ **`--strip_style` 早已在生产开着**
      ⇒ 增量只是「一代一次 4 个 `rank_rows` + 一次 `neutralize_rows` + 每候选几个廉价相关」
      ⇒ **无任何额外回测** ✓
    · ★ **用途仍必需**：生产含 `--score_mode=new` ⇒ 它是**唯一**能验证"new 排序分是否让因子更往低换手/低成交额挤"的手段
    · ★★ **补一个已知盲区**（`loop_todo §1.24-①`）：我们"剥风格"**只剥 2 个**（`lncap`+`lnamt`），
      而 `--style_obs` 记录 **4 个**（+`lntr` 换手率 / `lnpx` 价格）⇒ **`lntr`/`lnpx` 一直看不见** ✗
      实证代价：`F07` 号称"最独立"（剥两风格后 Cal 1.209 > 原 0.812），
      但它 **`lntr` 暴露 −0.51（很强）** ⇒ "独立有效"的准确含义只是"剥掉市值+成交额后仍有效"
 2. `research/` → **`history/research/`**（`git mv`，91 个文件保留历史）
    判据：**0 生产引用**（engine/tools/standard 里无任何引用）· 停更 09-08 ·
    内容是 factor_miner 时代的 round1~10 / 归因拆解 / qlib 对拍 / dev 探查 ⇒ **方法留档，非生产** ✓
    ★ 保留 `strategies/`（同样 0 引用，但属**定稿成果**，不是历史草稿）✓
 3. 顺带修正文档不一致：README 的 `docs/history/` → 根 `history/`；`research/` 描述改为"已归档"；
    `standard/` 列表里**已归档的** `style_paired_analysis.py`/`prof_evalreal.py` 明确标注（原描述已失效）

【回退方式】
  · 只回退 `--style_obs`：把 `tools/run_tracks.py` 的 `extra` 里那个 `--style_obs` 删掉即可（单行）
    —— 或临时用 `--extra="..."` 覆盖（见文件头用法）
  · 回退归档：`git mv history/research research`（或 `Move-Item`）
  · **未动任何状态文件** ⇒ `loop_state*.pkl` / `loop_archive*.csv` **无需回退** ✓
  · 代码整体回退：`git checkout v0.20.1 -- tools/run_tracks.py`

【⚠ 本轮我纠正的自己的错误（记下避免重犯）】
  · 曾把 `--style_obs` 的开销说成"每候选 +1 次回测" ⇒ 查证后那是 **`--strip_style`** 的
    （`2026-09.md:1246` = §8.14「剥风格」表），同一行只说两者**共用** `style_features` ✗
    ⇒ 教训：**引用"开销/成本"数值前必须回原文确认它说的是哪个开关** ✓

# loop_code — Loop 式因子自动挖掘引擎

> **当前版本 v1.21.21**（2026-09-21）


从 `D:\rqalpha_demo` 整理迁移（2026-09-08），原目录保留备份。机器外部数据依赖：米筐 bundle `E:\rq`（本地数据盘）。

## 目录结构
- `engine/` — **Loop 挖掘引擎（可独立运行，相互引用均用相对自身路径）**
  - `loop_engine.py` 主引擎（五维演化 + 跨量纲审查 + 失败模式库 + FSA 冻结 + 结构族/叶子代理闸门 + 同代近重复去重 + 两级筛选 + 费后验证）
  - `loop_critic.py` B角诊断（每代自动写 gen 建议进 journal）+ `loop_llm.py` LLM 双子（A角语义引导 / B角候选审查 / 代末 AI 审查）
  - `loop_watch.py` 常驻接力进程（每 5min 轮询，空闲自动启下一代，支持无人值守多代跑）
  - `loop_status.py` 进度/进程/档案查询
  - `loop_fields.py` **叶子字段单一事实源**（基础7+派生12+资金流16+BARRA11+财报8=54；新增字段族只改这一处）
  - `factor_miner.py` 因子挖掘基础框架（panel 加载/预筛选/两段式验证）
  - `fastops.py` L1 向量化快速算子
  - `build_mf_leaves.py` / `build_barra.py` / `build_fa_pit.py` 扩展数据源叶子构建（资金流16列入 panel / barra.h5 / fa_pit.h5，幂等可重建）
  - `panel.h5` / `universe.h5` / `barra.h5` / `fa_pit.h5` L1 筛选用数据（由 `build_*.py` 从 E:\rq 重建）
  - `loop_state.pkl` 引擎滚动状态（运行中会变）
  - `_check_*.py` 机制自检（dry-run 零误杀验证）
- `tools/` — **生产管线与工具（纳入 git）**。2026-09-14（v0.5.2）从 `ai_test/` 迁出：
  - `run_tracks.py` 多池轨道驱动（生产唯一入口）· `build_facs.py` 收尾① 因子值落地（`--only-new` 增量）
  - `cross_pool_review.py` 收尾② L2 跨池审查 + L3 精选池 · `critic_sensor_report.py` B角 传感器审计
  - `fix_csv_schema.py` / `backfill_library_pool.py` / **`backfill_library_sign.py`**（回填各库明细块的 `sign`）/ `tracks_status.py` / `journal_view.py`（journal 倒序查看）
  - `build_crosspool_view.py` / `add_quant_copy.py` / `backfill_bank_ex.py` 等（被 `engine/` 代码引用的脚本）
  - `_test_critic_sensor.py`（32 项）· `_test_build_facs_merge.py`（11 项）· `_test_inject_pools.py`（17 项）
    · `_test_parent_sel.py`（20 项）· `_test_reports_expr.py`（34 项，**报告表达式必须完整**）
    · `_test_action_efficacy.py`（33 项，**动作有效性 + 参数棘轮**）
    · `_test_ops_registry.py`（16 项，**算子单一事实源**；★ 含"**只改一处就够**"的**行为证明**）
    · `_test_ops_sync.py`（28 项，**算子 + 叶子覆盖 + 防副本漂移**；★ 测**渲染后**的真实 prompt，两份都测）
    · `_test_ops_math.py`（51 项，**新算子数值对拍**：`np.polyfit` / `scipy.stats`）回归测试
    · `_check_quotes.py` 质检
  - ★★ **`calib_gate.py`** — **门标定器**（`loop_todo §1.3-B`）：**任何门启用前先跑它**。
    给两个位置（`--target=pre_l1` 结构判据 / `--target=l1_l2` L1 指标）的
    「正/负样本分布 + 各阈值的**误杀/拦获**」，并支持复核具体规则（`--eval "mono>=0.75"`）。
    判据：**误杀 ≈ 0 且拦获 ≥ 20%** 才值得开 —— 这是「**动作施加了但完全无效**」（§1.1/§1.15）
    这个反复教训的**工具化防御** ✓
  - ★ **`verify_test_catches.py`** — **负向验证**：把 bug 人为放回源码 → 跑测试 ⇒ **必须 FAIL** → 还原 ⇒ PASS。
    **一个"永远通过"的测试没有价值** —— 必须证明它真的抓得到它要防的 bug（`try/finally` 保证还原）
  - ★ **`smoke_gen_only.py`** — **引擎改动后的几十秒真机冒烟**（用引擎自带的 `--gen_only`：
    「只跑候选生成段、不跑 L1/L2、**不写状态**」）⇒ 断言行真的打印了 + 无 `Traceback`/`NameError`
    + **全部 `loop_state*.pkl` SHA256 未变**。★ 意义：**把"改引擎要等 50 分钟才知道崩没崩"降到 30 秒**
  - ⚠ **为什么迁**：`ai_test/` 长期被 `.gitignore` 整个忽略，但收尾管线住在里面
    ⇒ `git checkout v0.x` 回退代码时**整条管线会消失**（回退点不完整）。
- `ai_test/` — **草稿区 + 运行产物（按 `.gitignore` 忽略，可随时删）**：一次性诊断/探索脚本、
  日志（`_tracks/`）、中间产物（`*.csv`/`*.pkl`/`*.md` 报告）。
  - ⚠ 仅留 3 个**兼容垫片**（`run_tracks.py` / `build_facs.py` / `cross_pool_review.py` → 转发到 `tools/`），
    为「迁移时正在跑的轨道」保留；**轨道结束后可 `git rm`**。
- `standard/` — **统一检验 / 分析工具**（均为独立可跑的绝对路径脚本）
  - `standard_test.py` 因子检验模板：一次跑齐「引擎指标复现校验 + 全A宽池频率扫描 + 沪深300/中证500 成分内 + 风格归因 + 分段独立验证」，含 `--pool_mode=A|B|S`、`--cost-name`；成本口径取自 `engine/cost_presets.py`
  - `qa_style_obs.py` 风格观测链路自检（18 项：向量化 vs 朴素对拍、退化安全、20 日滚动轴、`--help` 冒烟）
  - `pool_tags.py`（★ **被 `engine/` 引用** ⇒ 生产依赖）· `obs_analysis.py`（观测跑后的联合分析）
  - ⚠ `style_paired_analysis.py` / `prof_evalreal.py` 已于 2026-09-15 **归档**到 `history/20260915_cleanup/standard/`（一次性验证件）✓
- `docs/` — **只放活跃文档/档案**：`factor_roadmap.md`（**研发档案主入口**：Round 叙事 + 附录A Loop gen1~22 整体复盘 + 附录B 扩叶落地）/ `factor_library.md`（**Loop 入库因子**，引擎代末自动同步新增）/ `loop_journal.md`（B角代际诊断）/ `loop_todo.md`（**唯一权威待办**，已完成项归 `docs/log/todo_done.md`）/ `loop_archive.csv` 与各池 `.csv`（引擎逐代累积的 L2 明细与观测）
- `history/` — **归档区（仓库根）**：★ 2026-09-15 由 `docs/history/` 移来 ✓
  · `history/factor_archive.md|.csv`（factor_miner Round1~7 旧档案，停更）
  · `history/loop_archive.legacy_pre_gen16.csv`（gen16 前 L2 旧快照，**`build_facs.py` 仍需读它**）
  · `history/research/`（**早期研究脚本留档**，91 个 `round*.py`/`probe_*`/归因拆解；**0 生产引用**，停更 09-08）
  · `history/ai_test_20260915/`（旧草稿区整体归档）· `history/20260915_cleanup/`（含 `manifest.txt` 记录来源与理由）
- `strategies/` — 定稿 rqalpha 回测策略：`all01/`（all00/01/03 + factor_snapshot 数据）、`all04/`（barra 落地 + g6 数据）、`vol/`、`big_small/`，共享 `bt_utils.py`
- `ai_test/` — **草稿区**（临时脚本/产物，可整删；`run_tracks.py` 等会自动重建所需子目录）

## 运行
```powershell
# 引擎一代（后台）：参数与三道闸详见 loop_engine.py argparse
cd D:\loop_code\engine
D:\miniconda3\envs\rqdata\python.exe loop_engine.py --gen=13 --n=800 --l2=30 --seed=123
#   LLM 双子开关（均默认 auto，可 on/off 强开/强关；无 key 或调用失败自动回退纯规则，不影响无人值守）：
#     --llm_guide  A角生成引导：候选引导位由 loop_llm 解析语义候选，失败/无 key 回退本地 13 机制族
#     --ai_jury    B角候选审查：L1 硬滤后随机抽 --jury_n(默认5) 精判，KILL 剔除出 L2
#     --ai_critic  代末 AI 审查：点评本代诊断+规则建议，全文写进 docs/loop_journal.md“AI 审查(DeepSeek)”小节
#   key=桌面 1.txt 或环境变量 DEEPSEEK_API_KEY

# 无人值守多代接力（可选）：启动 watcher 后每 5min 轮询，空闲且该代无 err 自动启下一代
#   停止条件：任一代 err 非空即停下等人工（铁律）；代数达 loop_watch.py 上限（默认 50）自动结束。
#   防多实例/防重复代已内置；引擎在跑时 watcher 只等待不动作，可随时手动启动。
cd D:\loop_code\engine
Start-Process "D:\miniconda3\envs\rqdata\python.exe" -ArgumentList '-u','loop_watch.py' -WorkingDirectory 'D:\loop_code\engine' -WindowStyle Hidden

# 进度查询（脚本在 engine/ 下且按自身路径定位：带全路径即可，从任意目录运行都行）
D:\miniconda3\envs\rqdata\python.exe D:\loop_code\engine\loop_status.py

# 无人值守期间人工查看（代号 N 换成当前代，如 loop23C.*）：
Get-Content D:\loop_code\engine\loop_watcher.log -Tail 20   # watcher 接力事件: [START]/[OK]/[RUN]/[SKIP]/[STOP]/[DONE]
Get-Content D:\loop_code\engine\loop23C.log -Tail 20        # 当前代引擎进度（阶段字样见 loop_status.py）
Get-Content D:\loop_code\engine\loop23C_err.log             # 当前代错误日志（空=正常）
Get-Content D:\loop_code\docs\loop_journal.md -Tail 30      # B角每代诊断 + AI 审查落档

# 定稿策略回测（示例）
cd D:\loop_code\strategies\all04
D:\miniconda3\envs\rqdata\python.exe all04.py
```

## 换机器：pull 之后怎么把「库 / facs / 曲线」重建出来（2026-09-17）

`git clone` 之后，**清单类文件都在仓库里** ✓：
`docs/factor_library*.md`（人读：编号 · 入库代数 · 家族 · 一句话 · 完整公式 · sign · 池标签 · 费后指标）、
**`docs/factor_registry.json`**（**机器可读**：同一批字段 + **node 结构**，`tools/export_factor_registry.py` 生成，
收尾管线每轮自动重建）、**`docs/library_entries.jsonl`**（★ 新入库事件日志：时间 / 池 / 代数 / 编号 / 公式，
看板「新入库日志」卡读它；引擎入库时写，历史由 `tools/backfill_library_entries.py` 回填 ✓）。
但 `.gitignore` 忽略 `*.pkl` / `*.h5` ⇒ **数据与因子值必须自己重建**：

```bash
# ① 数据面板（按需：缺哪个字段就补哪个；各脚本头部注释写了数据源要求）
python engine/build_panel.py          # → engine/panel.h5
python engine/build_barra.py          # → engine/barra.h5（11 个 Barra 风格因子）
python engine/build_fa_pit.py         # → engine/fa_pit.h5（财报，PIT）
python engine/build_universe.py       # → engine/universe.h5（股票池/可交易）
python tools/build_industry.py        # → engine/industry.h5（申万一级：看板风格/行业暴露要用）
# ② 因子值 → facs/
python tools/build_facs.py --only-new
# ③ 指标表 → docs/factor_metrics.csv
python tools/factor_metrics.py --only-new
# ④ 曲线 → docs/factor_curves/*.json（看板详情页的图；两段跑，别一次等太久）
python tools/factor_curves.py --only-new --stage=core
python tools/factor_curves.py --only-new --stage=strip
```

★ 关键：②③④ **都优先读 `docs/factor_registry.json`**（不必有 `engine/loop_state*.pkl`）⇒
**全部已入库因子的值 / 指标 / 曲线都能重建** ✓（`tools/_test_registry.py` 专门守这条：
"本机 pkl 里的每个入库因子 ⊆ JSON" ⇒ 换机器不会**悄悄少算** ✗）

⚠ 但**"继续往外挖"仍然需要 pkl**：`engine/loop_state{_pool}.pkl` 里还存着**挖掘的记忆**
（种子 / 失败模式库 / FSA 冻结骨架 / 收益流库 / B角策略 cfg）⇒ 想接着挖，请从原机器拷这些 pkl；
否则只能**从零重挖** ✓

### 规模化了怎么办（几千个因子；2026-09-17 实测）

**登记表本身不会成为瓶颈** ✓：每因子约 **1.2 KB** ⇒ 1000 因子 ≈ 1.2 MB · 3000 ≈ 3.4 MB ·
10000 ≈ 12 MB；`json.load` 解析 10 MB 只要 **0.18 s**（10000 因子也才 0.18s）✓
· 导出器每轮收尾都会重建，但**内容没变就不写文件**（不因时间戳变化白给 git 添版本 ✓）
· 单文件超 **4 MB** 会告警 ⇒ 届时**按池分片**（为的是 **git diff/评审可读**，不是性能）

⚠ 大库时**真正的瓶颈是"重建"本身**（每个因子都要一次完整回测）：
· `build_facs` 全量 ≈ 21 min / 52 因子 ⇒ 约 **24 s/因子**
· `factor_curves --stage=core` ≈ 12 min / 51 因子 ⇒ 约 **14 s/因子**（`--stage=strip` 更久）
⇒ 3000 因子 ⇒ 只 core 就要 **≈ 12 小时**（strip 另算）✗
⇒ 正确姿势：① **一律 `--only-new`**（已算的跳过 ✓）② 分批按池跑 ③ 真要全量重建就**并行**
（`factor_curves` / `build_facs` 目前是单进程，可按因子分片多进程跑）✓

## 说明
- 引擎所有文件读写都相对 `engine/` 定位，`loop_code` 可整体搬移；唯一外部依赖是 `E:\rq` 数据盘。
- `loop_state.pkl` 为滚动态：**gen23 起 watcher 无人值守接力，已跑至 gen70 达标收官（2026-09-11）**，入库因子 **30**（F01~F30）。整体复盘见 `docs/factor_roadmap.md` 附录 A/B + Round27；入库因子明细见 `docs/factor_library.md`；逐代诊断见 `docs/loop_journal.md`。跨系统（存储/数据库/对齐）决策见 `docs/software_framework.md`。
- **gen51 起新增两道抗冗余闸门**（治 gen50 同代近重复 F20~F23）：`--dedup_corr`（默认 0.85，同代 L1 TopN 两两 |rank corr| 超阈即丢弃后者）+ `--fam_sole`（默认开，族指纹并入"单叶变换"维度把"同叶不同壳"的叶子代理候选归同族）。标定/验证见 `tools/calib_dedup_leaf.py`（★ 2026-09-15 由 `calib_gates.py` **改名**，与 `calib_gate.py` 只差一个 `s`、用途不同）；冒烟脚本 `qa_fam_smoke.py` 已随 `ai_test/` 归档到 `history/ai_test_20260915/`。
- 数据文件（*.h5/*.pkl）体积大且可由 `build_*.py` 重建，git 入库时按 `.gitignore` 排除。

## 版本与回退（2026-09-11 起）

**约定：每个"版本" = 一次 commit + 一个 annotated tag**（版本名 `v<major>.<minor>` 递增）。
tag 说明固定记录四项，便于回退时判断影响面：

| 项 | 说明 |
|---|---|
| **引擎行为** | 是否改变挖掘结果（阈值/排序分/生成端改动 = **变**；纯工具/文档 = 不变） |
| **已完成代** | 该版下的代数与入库范围（如 gen70 / F01~F30） |
| **关键变更** | 3~5 条要点 |
| **回退方式** | `git checkout <tag>` |

⚠ **引擎滚动状态不在 git 内**：`engine/loop_state.pkl`（bank / 种子 / 冻结 / 失败库）与
`docs/loop_archive.csv` 均按 `.gitignore` 排除。**回退代码 ≠ 回退库状态**——若要连状态一起回退，
需另行备份 `loop_state.pkl`。

```powershell
git --no-pager tag -l -n20                 # 列出所有版本及说明
git --no-pager show v0.1 --stat            # 看某版改了什么
git checkout v0.0                          # 回退到基线（再 git checkout main 回来）
git tag -a v0.2 -F <说明文件>               # 建新版（说明按上表四项写）
```

**已建版本**（**完整变更台账见根目录 [`change_log.md`](change_log.md)**；下表只列"该版一句话 + 回退影响面"）

> ⚠ **表格约定**：**最新一行的"提交"列写 `tag 自身`** —— 这不是偷懒：
> **一个文件无法记录"包含它自己的那次提交"的哈希**（哈希由内容算出，先有内容才有哈希，自指无解）。
> 要用它时 `git show v0.5.1` 即可（tag 直接指向该提交）。历史行才有具体哈希。
>
> ✅ **登记完整性（2026-09-19 补登）**：`v1.4.0`～`v1.21.4` 已**逐版补登** ✓（一句话取自各 tag 的说明 / 提交标题 ⇒ 早期版本会比新版**简略** ✓；要更细的内容看 [`change_log.md`](change_log.md) ✓）

| tag | 日期 | 提交 | 一句话内容 |
|---|---|---|---|
| **v1.21.21** | 2026-09-21 | `tag 自身` | ★★★★★ **修「动态暴露图出不来」的真因（接口层固定白名单漏了 `expo`）+ UI 收尾（药丸按钮 / 标签全中文 / 共动性隐去）+ 调度器「退出原因」诊断 + 曲线写盘互斥与原子写 + 新增接口契约守门**（用户：_"图出来了，但是风格暴露的时序图怎么没有呢？"_ → _"这个全部显示全部隐藏按钮有点丑啊，改成跟其他按钮一样的多好看，然后其他标签都是英文的，全都搞成中文吧，然后 comovement 是啥，为啥是一条水平线"_ → _"接着的待办你一口气都做完"_）—— ① ★★★★★ **真因不在数据、不在打包、不在前端渲染** ✗：`dashboard/api/app/sources/factors.py::curves()` 把响应拼成**固定白名单** ⇒ 后续新增的键 `expo` **从未透传** ⇒ 前端 `c.expo` 恒为 undefined（"数据在、图不在" ✗）⇒ 出口补 `expo` 块（`_down` 下采样 + `_nums` + 口径串 `_plain` 清洗 ✓）；② **UI 四处收尾**：「全部显示 / 全部隐藏」从 `.state` 米色方按钮改为与风格芯片**同族药丸**（透明底 + 1px 描边 + 圆角 999 ✓）；芯片 / 图例 / 悬停**一律走 `styleName()` 全中文**（连 `规模 size` / `成长 growth` / `Beta` 三条中英混排也收干净 ✓）；★ **`comovement`（共动性）当前是占位数据**（`barra_comovement` 全库**恒等于 1** ✗ · 418 期 std=0 · 唯一值 1 ✓，其余 10 个风格 std 0.11~0.57 都是真值 ✓）⇒ 前端识别**常量序列**后**从图与芯片中隐去** ＋ 一行说明 ✓（等真实数据接入会自动回来 ✓）；清掉用户可见文案里的 `**` / `⚠`（`expo` 的 `caliber` 串 ✓）；③ ★★★★★ **调度器「退出原因」诊断**：`parallel_runner._exit_reason()` = **退出码语义**（0xC0000005 访问冲突 / 0xC00000FD 栈溢出 / 137 被 OOM 杀 … ✓）＋ **stderr 关键词**（MemoryError / PermissionError / 缺列 … ✓）＋ **日志末行** ⇒ 三处落地：调度器日志一行 ✓ · `_tracks/_engine_exits.log` **专档**（好翻 ✓，比在 1 MB 日志里翻强 ✓）· 控制文件 `exits` 字段（结构化，前端以后可直接显示 ✓）—— 起因：以前只有"疑似崩溃"一句，**崩在哪全靠猜** ✗；④ ★★★★ **曲线写盘互斥 + 原子写**：`docs/factor_curves/.write.lock`（阻塞等待 / **陈旧锁自动接管**（持有 pid 已死或超 4h ✓，⚠ 探活用 `tasklist` 而非 `os.kill(pid,0)` —— Windows 上后者**会真杀进程** ✗）/ 超时给出"谁在写 + 怎么手动清" ✓）⇒ 根治"两个 stage 并发互相覆盖"（读旧 JSON → 改自己那段 → 整份写回 ✗）；写盘改 `.tmp` + `os.replace` ⇒ 接口不会再读到**半截 JSON** ✓；⑤ ★★★★★ **新增守门 `tools/_test_curve_dto.py`**：**静态对账**（66 个曲线文件 · 26 个顶层键 · 嵌套段 strip/expo 逐键 ✓ ⇒ 以后加键必须同步改接口，否则当场红 ✗）＋ **动态实测**（真调 `factors.curves()` ✓ 断言带 `expo` 的样本确实透传 ✓）＋ **前端靶向**（`App.tsx` 用了 `c?.expo` / `c.style` ⇒ DTO 必须给 ✓）；⑥ 补 `_test_frontend_wiring.py` 对「行业中性」图例数组的**硬断言**（v1.21.19 遗留：只加 `STRIP_LABEL` 漏了写死的图例数组 ✗）；⚠ 回退影响面：接口出口 + 前端两件 + 曲线工具 + 调度工具 ✓ **引擎求值与既有指标口径未改** ✓；回退 ⇒ 暴露图又不显示、退出原因又不记 ✓ |
| **v1.21.20** | 2026-09-20 | `tag 自身` | ★★★★★ **新增「动态风格暴露」图（11 个 Barra 风格随时间的暴露曲线 + 逐个开关）**（用户：_"我觉得是不是加个图，可以看 11 个风格的动态暴露曲线？然后鼠标移动的时候可以显示每个风格的动态暴露值，然后加个按钮全部隐藏、全部显示，这样我可以只看某一个风格暴露的曲线？这不更牛逼吗？只是这样会不会影响性能算起来太慢？"_ → _"前端赶紧做起来~现在就跑"_）—— ① **后端** `tools/factor_curves.py` 新增 `expo_for()` ＋ `--stage=expo` ＋ 增量门控（`styCal` 口径串变了就重算 ✓，同 allsty 铁律 ✓）：口径三条按用户纠正 ✓ = **组合取因子最强十分之一等权** ✓ · **暴露值取 Barra 原生值的等权平均** ✓ · ★ **中性线 = 0** ✓（原生值本身即"市值加权 0 均值"口径 ⇒ 市值加权全市场天然为 0 ✓；**禁用池内等权均值当参照** ✗ —— 实测我们面板 size 等权均值 −1.68 / 市值加权 +0.09 ⇒ 用等权当基准会把 −0.8 误读成"大盘" ✗）；只取**换仓日 418 期**（不是 3309 天 ✗ ⇒ JSON 小 ✓）；样本不足/退化返回 None（不臆造 ✓）；② **前端** `App.tsx` ＋ `api.ts`（`CurvesDto` 加 `expo` 类型 ✓）：详情页新增这张图 —— 11 条曲线（复用现成 `Chart`，带 **y=0 中性参考线** ✓）＋ 一排**风格芯片**（逐个点开/收起，选中时变该曲线颜色 ✓）＋ **全部显示 / 全部隐藏** 两个按钮 ✓ ＋ 悬停显示各条当值 ✓（默认全部显示 ✓）；③ ⚠ 踩坑：`caliber` 文案里写 `Top 10%` 被 `%` 格式化当占位符 ⇒ `ValueError: unsupported format character` ✗ ⇒ 改成"最强十分之一"避开 ✓；性能：**计算 <1 秒/因子** ✓（风格面板现成、全库复用 ✓），传输按期频 × 3 位小数 ≈ 15 KB/因子 ✓；⚠ 回退影响面：曲线工具 + 前端两件（**引擎求值与既有指标逻辑未改** ✓）；回退 ⇒ 详情页少这张图 ✓ |
| **v1.21.19** | 2026-09-20 | `1eedee5` | ★★★★★ **行业中性并入剥风格（方案 A：新增口径、并存、默认不动任何现有数字）** + 悬停提示精简（用户：_"把行业中性纳入正式回测可以的"_ → _"做到 ③"_）—— ① ★★★★★ **行业中性 = 剥风格第 8 条**：`tools/factor_curves.py` 的 `_allsty_neutral` 多出一路 **C = 只剥行业**，口径 = **去全市场均值 → 行业内去均值 → 再排秩**（= 行业内相对强弱 ⇒ 选股自然行业均衡 ✓），与「剥全部」的 A 口径**同构**但**无需回归**（空风格集 ⇒ 设计矩阵只剩截距 ⇒ 残差就是去均值 ✓）⇒ 纯向量化 ⇒ **几乎零成本** ✓；`strip_for` 新增 `newv[indneu]`（与 allsty 两条同批产出、走同一个"失败不致命"的 try ✓）；前端 `STRIP_LABEL` 加 `indneu: 行业中性` ✓；守门折进第 8 条断言 ✓；★ **它是新增一条曲线** ⇒ raw / 剥市值 / 剥成交额 / 剥两者 / 剥全部 **五条一个都没动** ✓；冒烟实测（1 个因子）：剥Calmar 原 **0.5067** / 剥市值 0.2721 / 剥成交额 −0.0536 / 剥两者 0.1876 / 剥全部 −0.1193（只剥风格不剥行业 −0.1078 · **行业中性 0.3796**）⇒ 该因子行业中性后 Calmar **−25%** ⇒ 收益里相当一块来自**行业暴露** ✓；⚠ 生效范围：库里已有因子的曲线需**重算**才带第 8 条（口径串已变 ⇒ 收尾增量会自动重算 ✓）；② **悬停提示精简**：相位卡悬停只留 `第 N 轮 · 轮次上限 M` ✓（删掉括号里的口径解释 ✗ 与「本轮已完成代数合计」那一行 ✗），逐池明细（各池状态 / 本轮已跑 N 代 / 当前 gen M）保持不变 ✓，口径细节只留在 `tools/parallel_runner.py` 的注释里 ✓；⚠ 回退影响面：曲线工具 + 前端图例 + 一个守门断言（**引擎求值与既有指标逻辑未改** ✓）；回退 ⇒ 图例少一条「行业中性」曲线 ✓ |
| **v1.21.18** | 2026-09-20 | `54dee12` | ★★★★★ **修「轮永远收不了口」（方案 B）+ 徽标/登记表/表格三处收尾**（用户：_"1000 都已经跑完 3 代了，鼠标放在挖掘中，怎么显示还是第 1 轮…是不是以后这个轮次显示就没有意义了？"_ → _"啥叫三池都收齐才+1？"_ → _"最少的都跑 3 代了，按理说应该至少 3 轮了。所以你赶紧把轮循环读完"_ → _"按方案 B 改掉收轮判据"_）—— ① ★★★★★ **方案 B：改掉收轮判据** —— 原判据（不限模式）是 `any(每池已完成 < 1 代) or 还有别的池在跑` ✗ ⇒ 只要还有池在跑，刚跑完的池就**立刻重排队** ⇒ 候选永不为空 ⇒ 内层唯一 break（既无在跑引擎、又无待启动）**几乎永不成立** ⇒ **轮永远收不了口** ✗（实测：本轮 4.5 小时、25 代，`round` 仍 = 1 ✗）；真正危害不是显示 ✗ 而是**轮末全局收尾从不执行** —— 跨池审查 / 精选池 / 统一口径指标表 / **登记表重导** / 剥风格 / 风格画像，日志实测**上次是 09:37、之后 9 小时没跑** ✓ ⇒ 新判据 = **每个启用池都完成 ≥ `--min_gens_per_round`（默认 3）代才收轮** —— 收口前快池**照旧一直领** ✓（09-19 的诉求不丢 ✓），最慢的池攒够 K 代后不再重排队 ⇒ 等它跑完 ⇒ 自然 break ⇒ 全局收尾 ⇒ `round + 1` ✓；② **「启动即崩」徽标语义修正** —— 原先崩溃标记**只追加、从不清除** ✗ ⇒ 早上 10:26 一次**偶发** `PermissionError [WinError 5]`（`os.replace(loop_state_500.pkl.tmp)` 被 Windows 文件锁挡住）⇒ 500 的卡片**一直**挂"启动即崩" ✗（此后 gen74/75/76 全部 rc=0 ✓）⇒ 现在**跑通一代即撤** ✓（真·必崩的池每代都崩 ⇒ 立刻又被写上 ⇒ 能力不减 ✓），并已清掉那条陈旧标记 ✓；③ **登记表学会「已移出」语义** —— `export_factor_registry.py` 的 `parse_overview`（总览表状态列）＋ `parse_detail`（明细块）**双处跳过** ⇒ 登记表 = 当前在库清单 ✓（否则每次重导都把 F42 写回来 ✗，守门当场红 ✓）；④ **表格「详情」按钮不再折行** —— 一句话列 440→320px ＋ 状态/详情两列 `nowrap` ✓；⑤ **轮次文案说清口径** —— 悬停写「每个启用池都完成至少 3 代才加一」，并补一行「本轮已完成代数合计 N 代」✓；⚠ 回退影响面：调度工具（`parallel_runner` / `run_tracks`）+ 导出器 + 前端三件 ⇒ **引擎求值逻辑未改** ✓；回退 ⇒ 轮次回到"永不为 1 之后"、徽标不清除、登记表写回已移出项 ✓ |
| **v1.21.17** | 2026-09-20 | `ef299e9` | ★★★ **Alpha191·Alpha143 外部基准走一遍全流程 + 公式勘误 + 新增 10 日窗口算子（`ts_rank10`/`ts_delta10`）**（用户：_"看看是不是能够加入全A池子，让管线全流程测一遍"_ → _"你看下 alpha143 因子是不是找错公式了"_ → _"测出来不行，再给它移出不就行了"_）—— ① ★ **新增 10 日窗口算子**：`ts_rank`/`ts_delta` 的窗口名单补上 **10**（此前只有 20 日 ⇒ 想表达 `ts_rank(close,10)` 只能近似 ✗），并让 `ts_delta(delay(close,5),5)` = `ts_delta10 − ts_delta5` 的化简有直接支撑 ✓；② ★★★ **F42 外部基准入全A → 全流程实测**：费后超额 **−18.2%/年** · 日频 Calmar **−0.216** · 夏普 −1.62 · 单期换手 71.7%（年化约 36 倍）· **负年 9 年** ⇒ **不合格**；③ **已移出**（用户：_"测出来不行，再给它移出不就行了"_）—— `in_bank` 置 0 + 退出入库历史名单 ⇒ 看板「因子库」显示 **已移出**（历史明细仍可查 ✓）；④ ★★★★★ **公式勘误**（用户看研报发现 ✗）：研报原式（`20170615-国泰君安-数量化专题之九十三` 第 812 行）是**递归式** `CLOSE>DELAY(CLOSE,1)?(CLOSE-DELAY(CLOSE,1))/DELAY(CLOSE,1)*SELF:SELF`（`SELF` = **t-1 日的 Alpha143 因子值** ⇒ 逐股沿时间递推 ✓）⇒ 我们先前那条 `neg(mul(cs_rank(ts_rank10(close)),cs_rank(ts_delta10−ts_delta5)))` **根本不是它** ✗ ⇒ 真式四池实测（`ai_test/_alpha143_true.py`，只读 ✓）：20 日 rank-IC 全A −0.019 / 1000 −0.021~−0.023 / 500 −0.023~−0.025 / 300 −0.020~−0.027（t −11 ~ −16）⇒ **显著为负 = 短期反转因子** ⇒ 方向必须**取反**（买低分端）✓，五档分层详见 `ai_test/alpha143_true/` ✓；⚠ 回退影响面：只动「算子表窗口名单 + 库文档/入库名单」三类文件，**引擎求值逻辑未改** ✓；回退 ⇒ 撤回 10 日窗口与 F42 记录 ✓ |
| **v1.21.16** | 2026-09-20 | `cb13461` | ★★ **修守门 `_test_library_log` 的提取窗口**（v1.21.15 回归暴露 ✗）—— 它检查"徽标 tooltip 里不许有机味符号" ✓，但窗口取的是 `const libStateTip` → `function EntryLogCard` 之间的一切 ✗；v1.21.15 正好在这两者之间插入了 `GradeTag` 组件（**注释里有 ★/⇒** ✓ 属维护者可见、**不是用户文案** ✓）⇒ 被误判成"文案有机味" ✗ ⇒ 窗口**收窄到 `libStateTip` 这一个语句** ✓（`re.split` 到下一个 `const`/`function`/块注释为止 ✓）—— ⚠ 同版记一条**流程教训**：v1.21.15 我是"测试与 git 提交写在同一条命令里"✗ ⇒ 测试失败时提交已经跑掉了 ✗✗（**同一晚犯第二次** ✗）⇒ 从本版起硬规矩：**全量回归必须先单独跑完、全绿，再执行提交/tag/push** ✓；⚠ 回退影响面：只动一个 `tools/_test_*`（守门 ✓），产品代码与数据未动 ✓ |
| **v1.21.15** | 2026-09-20 | `86241cf` | ★★★ **因子库页与入库日志都加上「剥风格档位」标签（A/B/C + 未测灰标）**（用户：_"因子库页加个 A、B、C 标签就好了，这样我一看就知道啥档位的，入库日志那里我看也放得下，也加上吧"_ ＋ _"补一个未测标签，然后发版"_）—— ① 新增 `GradeTag` 组件：**只取后端 `detail.strip` 的**首字母** ✓**（那串由 `loop_pools.STRIP_DESC` 生成 = **单一事实源** ✓）⇒ 前端**不重算** ✓（重算就有两套口径、迟早漂移 ✗）；② **两处接入**：因子库表格「编号」列 ✓ + 新入库日志行（池/编号后面 ✓）；③ 配色与悬停解释：`A 独立有效` 绿 ✓ · `B 弱独立` 琥珀 ✓ · `C 纯风格` 红 ✓ · `未测` 灰 ✓；④ ★ **没记录的显示灰标「未测」** ✓（原来"不显示"会被读成"漏了" ✗；"不知道/没记录"要看得见 ✓，仍**不臆造档位** ✓）；⑤ 顺带把**语义**钉进注释与悬停（用户曾问"弱有效不应该移出吗？"）：**档位只做标注、不改留库** ✓ —— 有效库（bank）收"过 L2 门槛"的因子（**含 B/C** ✓，它同时是引擎的**去重对照集** ✓），"**只收 A**"的是**精选池 L3** ✓；⑥ 新增守门 `tools/_test_grade_tag.py`（解析口径 + 两处接线 + 四档解释 + 文案无机味符号 ✓）；⚠ 回退影响面：**纯前端**（`App.tsx` 一个组件 + 两处插入）+ 一个 `tools/_test_*`，引擎/数据未动 ✓；回退 ⇒ 档位标签消失（不影响任何计算 ✓） |
| **v1.21.14** | 2026-09-20 | `c62007c` | ★★★★ **修「池内收尾」从上线起就没跑成过（`avail_gb` 未定义 ⇒ 每次 NameError 被静默吞掉）**（用户：_"`500/F07` 我点了详情页，还是没曲线啊？你会不会算错因子曲线了？把别的覆盖了？"_ ⇒ 查明 **不是算错、更没覆盖** ✗，是**收尾自己崩了** ✓）—— **实录**：`09-20 01:44:22 [!] 池内收尾失败(NameError) -> 继续挖掘` ✔（因子 01:44:21 入库、收尾 01:44:22 就炸 ✓）⇒ `docs/factor_curves/F07_500.json` **不存在** ✓（F07 只有 `F07.json` / `F07_1000.json` ✓）—— **真因**：`run_tracks.do_pool_tail` 的**内存护栏**那行 `free = avail_gb()` ✗，而 **`avail_gb` 在 `run_tracks.py` 里从未定义/导入** ✗（它住在 `parallel_runner` ✓，本文件到 **L702** 才 `import parallel_runner` ✗ ⇒ 必然 NameError ✗✗）⇒ **这个功能从 09-19 上线起一次都没成功执行** ✗，且 `parallel_runner` 那层 `except` **只记异常类型** ✗ ⇒ 日志只留一行温和的"继续挖掘" ✗ ⇒ 症状全被藏住 ✓ —— **修法**：① `do_pool_tail` 内**延迟 import** `parallel_runner`（不能放文件头，会与 `import run_tracks` 循环依赖 ✗）✓ ② 调用方**连异常信息与定位行一起打** ✓（这次教训：只记类型 = 把病根藏起来 ✗）③ 新增守门 `tools/_test_pool_tail.py`：静态钉住"不许裸 `avail_gb()`" ✓ + ★ **功能校验**：给一个不可能满足的 `min_free_gb` ⇒ 必须**优雅返回 False 且不抛异常** ✓（实测 `可用 17.0 GB < 1e9 GB ⇒ 跳过` ✓ —— 正是这次崩掉的那条路 ✓）④ 顺手**手工补算** `500` 池（`do_pool_tail('500', force=True)` ✓）：`facs/指标/曲线` 各"完成 1 个" ✓ ⇒ `F07_500.json`（222.8 KB）已生成 ✓；⚠ 回退影响面：只动收尾的护栏那一行 + 报错文案（引擎/数据未动 ✓）；回退 ⇒ 池内收尾又会静默全废 ✗ |
| **v1.21.13** | 2026-09-20 | `84dc27d` | ★★★ **修 `export_factor_registry.compare()` 的格式化崩溃**（v1.21.12 回归暴露 ✗）—— **真 bug**：`key` 是**三元组** `(pool, no, expr)` ✓ 而三处格式串只写了 **两个 `%s`** ✗ ⇒ `'...%s/%s' % k` 抛 `TypeError: not all arguments converted during string formatting` ✗✗ ⇒ **一旦 registry 真的没同步，比较函数不是"报告差异"而是"当场崩"** ✗（把它自己的诊断能力废掉了 ✗）—— 修成三个占位符 ✓ ⇒ 它立刻**如实报出**：`JSON 缺条目 500/F07（式子 ts_mean150(corr100(barra_momentum, ts_mean5(fa_gm)))）` ✓✓（**正是用户那个"入库又被移出"的因子** ✓）；同版**重导 `docs/factor_registry.json`**（收尾每轮本来就会做 ✓）⇒ `_test_registry` **20/20 ✓**、全量回归复原 ✓；⚠ 回退影响面：只动导出器的报错分支（`tools/export_factor_registry.py` 一处 ✓），数据/引擎未动 ✓；回退 ⇒ 该守门再遇到不同步时会重新崩成 TypeError ✗ |
| **v1.21.12** | 2026-09-20 | `870257e` | ★★★★ **补掉"入库 → 收尾"的空窗：收尾名单口径扩成「曾入库过的全部历史 ∪ 当前 bank」**（用户之问：_"刚入库又被移出的因子，都不会自动算指标/出曲线吗？"_ —— 他 01:44 看到 `500 池 F07` 入库又被移出、点详情**没指标没曲线** ✗）—— **真因**：收尾三工具（`build_facs` / `factor_metrics` / `factor_curves`）的"该算谁"原来只有两源：`docs/factor_registry.json` **∪** `loop_state_*.pkl` 的 `bank` ✗ ⇒ **两者都只反映"现在的归属"** ✗ ⇒ 因子若在「**入库** → 收尾」这段（20~40 分钟）空窗里离开 bank、且库文档 md 还没登记它 ✗ ⇒ 三工具**都看不见它** ✗（指标/曲线长期缺失）—— **修法**：新增第三源 **`docs/library_entries.jsonl`**（引擎在**入库那一刻**写的 append-only 日志 ✓ 早就有 ✓ 只是收尾没用它 ✗）⇒ `load_bank_nodes` = registry ∪ **入库历史** ∪ pkl ✓ ⇒ **入库过的因子永远会被补算** ✓（`--only-new` 保证不重复算 ✓）；⚠ 解析历史 expr **必须 `max_size=10**9`** ✓（`parse_expr` 的默认上限是"防 LLM 超大式子"的护栏 ✗，对已入库因子没意义，却会让合法公式**静默返回 None** ✗ —— 2026-09-14 全A `F01` 就栽在这 ✓）；★ 新增守门 `tools/_test_bank_source.py`（含**功能校验**：逐池断言名单 ⊇ 入库历史 ✓ 实测补上 `500` 池那 1 个 ✓）；★ 同版另修**两处悬停显示**：`参与的池` 剔掉已停的池 ✓（原来与 `已停的池` **同屏打架** ✗）、已停的池**不再写"并行中"** ✗（改成 `已停止` / `已停止（这一代跑完就退出）` ✓）；⚠ 回退影响面：动 `tools/build_facs.py`（收尾工具，**引擎求值逻辑未改** ✓）+ 前端一处，回退 ⇒ 空窗因子又会漏算 ✗ |
| **v1.21.11** | 2026-09-19 | `011eb74` | ★★★★ **不限模式下"快池不再空转"：慢池还在跑 ⇒ 快池就一直领活**（用户：_"上证50、中证500现在在并行中吗？怎么并了那么久还没并进去？"_ ⇒ 查明是"在等慢池那一代"✗ ⇒ 用户拍板："**慢池还在跑 ⇒ 快池就继续领活，对，就这样**"）—— 原规则只保证"**每池至少 1 代**"：`_again = any(gens[池] < 1)` ✗ ⇒ **一旦所有池都 ≥1 代，快池立刻停手空转** ✗（实录 14:09：50 池刚跑完 gen41 就空着，而 300 的 gen88 还在跑 ⇒ 并行上限 3 只用了 1 个槽位 ✗；50 一代 4~6min、300 一代 20~40min ⇒ 这段等待本该产出 5~8 代 ✓）—— 修法（`tools/parallel_runner.py` 不限模式分支）：**条件加上"别的启用池此刻还有引擎在跑"** ✓ ⇒ `_again = any(gens[池] < 1) or bool(其它在跑的池)` ✓ ⇒ 慢池跑多久、快池就领多久 ✓；⚠ **不变项**：轮长仍由最慢的池决定 ✓（收轮必须等所有在跑的引擎结束 ✓）；⚠ 本次为生效**重启了调度器**（当前这一代作废重跑，用户拍板接受 ✓）；⚠ 回退影响面：**只动调度器派活条件一处**，引擎与数据未动 ✓；回退 ⇒ 快池在所有池 ≥1 代后重新空转 ✗ |
| **v1.21.10** | 2026-09-19 | `a964d53` | ★★★ **修精选池"好几个因子没有曲线"（真因 = 文件名拼重 ✗）+ 删掉页面底部的括注**（用户：_"页面底下（各改一处）删掉，然后我发现精选池还有好几个因子没有曲线嘛！"_）—— ① ★★ **曲线缺失的真因不是"没算"** ✗：曲线落盘命名是 `all` 池 = `F06.json`、其它池 = `F06_500.json`（`tools/factor_curves.py::_name_of` ✓），而**精选池条目的 `code` 本身可能已带池后缀**（`F07_1000` + `pool=1000`、`F05_500` + `pool=500` ✓）⇒ 前端再拼一次 `_{池}` ⇒ 去找 `F07_1000_1000.json` / `F05_500_500.json`（磁盘上只有 `F07_1000.json` / `F05_500.json` ✓）⇒ 面板显示"暂无曲线数据" ✗（**7 个精选因子里 2 个中招** ✓）—— 修法：**已带该池后缀就不再重复拼** ✓（`name.endsWith('_'+pool) ? name : name+'_'+pool` ✓）；复核 **7/7 全有曲线** ✓ ② footer 那句 `端口来自 dashboard/config.json（各改一处）` ⇒ 去掉括注 ✓；③ 守门 `_test_frontend_wiring` 那条**加严**：既钉"非 all 池必须补池后缀"（防拿到全A 同名编号的**另一个因子**的图 ✗✗）又钉"**已带后缀不许再拼**"（这次这个坑 ✗）；⚠ 回退影响面：**只动前端一个函数 + 一行文案**（`App.tsx`），引擎/调度器/数据未动 ✓；回退 ⇒ 精选池那 2 个因子又"没有曲线"✗ |
| **v1.21.9** | 2026-09-19 | `310a29a` | ★★★ **看板"看得懂"收口：相位卡真正对齐 + 逐池进度文案定稿**（用户连否 5 版："这个收尾审查搞到第二行去了，把面板搞宽点" → "挖掘中的面板太宽啦，跟下面的在跑的池没对齐啊" → "那个挖掘中也忒大了，动画绿点跑到上面去了，**弄成之前的样子**" → "索性改成 `50 3代 | 300 1代 | 500 1代`，**注意起始从 1 开始**，1 表示正在挖第 1 代" → "还是**没对齐**…挖掘中字体大小就跟旁边的 `3 / 5` 一样，这样就能对齐了吧"）—— ① ★★ **相位卡位置**：从顶栏挪进 `section.summary` 的 KPI 网格（**同格 = 同宽** ✓ —— 写死像素对不齐任何自适应网格 ✗，那行是 `minmax(160px,1fr)`，列宽随窗口变 ✗）；② ★★★ **相位卡整卡复用 `.kpi`**（`div.kpi.phasecard` + `.kpi-v` 状态行 + `.kpi-l` 进度行 ✓）⇒ 与"在跑的池"卡**同一套 CSS** ⇒ 字号/行距/内边距逐像素一致 ⇒ **天然对齐** ✓✓ —— **教训：想和旁边卡片对齐，别再调像素** ✗（我为此改了 5 版 ✗）；③ **逐池进度文案定稿**：`50 4代 \| 300 2代 \| 500 1代`（= 各池**正在挖第几代** = 本轮已完成代数 **+1，从 1 起** ✓ 不与卡片"挖掘中 · gen86"重复 ✗ ✓，进度多的排前面 ✓，悬停逐池列明细 ✓）；④ 撤掉卡片里"轮转模式的当前池"小字 ✗（会让卡变**三行** ⇒ 必然与邻居错位 ✗，该信息在悬停里 ✓）；⑤ 5 条相关守门断言同步改写 ✓；⚠ 回退影响面：**只动看板前端**（`App.tsx` / `styles.css` / `api.ts`）与守门，**引擎与调度器未改** ✓；回退 ⇒ 相位卡回到顶栏、文案回到 `第N代`、徽标回到"一律并行中" ✓ |
| **v1.21.8** | 2026-09-19 | `59c70b2` | ★★★ **看板"看得懂"这一批：徽标三态（新增「审查中」）+ 逐池进度（不再是全局 1/50）+ 相位卡与池卡片同宽 + 算子手册上线 + 不限模式与池内即时收尾**（用户：_\"300/500 一直都是并行中，是在审查呢还是在等 50 池挖完？\"_、_\"池徽标语义是不是加一个审查中？这样跟并行中就能区分开\"_、_\"轮次显示逻辑按照我想要的改\"_、_\"这个收尾审查搞到第二行去了，把面板搞宽点，跟底下在跑的池面板一样宽吧…这里就显示 50 第48代 | 300 第24代\"_）—— ① ★ **徽标三态**：**挖掘中**（有它的引擎在跑，带该池自己的 gen）= 真的在算 · **审查中**（`ctl.tailPool == 它` 或轮末 `phase=='tail'` ⇒ 刚挖完在做收尾：facs 落地/指标表/曲线）= 挖完了在审 · **并行中**（既没挖也没审，典型是\"**本轮它已跑完、在等其它池那一代结束**\"⇒ 用户被这个坑问住的正是它 ✗）；② ★ **轮次显示改逐池**：相位卡那格由「全局 1/50」改成 `50 第1代 | 300 第0代`（= 各池**本轮已完成代数** ✓ 不与卡片上\"挖掘中 · gen86\"重复 ✗ ✓），悬停列出每池（状态 + 本轮已跑 N 代 + 当前 gen ✓），池卡片新增「本轮已跑」字段 ✓；③ **相位卡固定 310px**（= 池网格 `minmax(310px,1fr)` 的卡最小宽 ✓）—— 原来\"收尾审查中\"被挤成两行 ✗（**推翻** 09-17 的\"贴合内容\" ✓）；④ **算子手册**：`dashboard/api/app/sources/ops.py` 覆盖全部算子与字段，面板内可查 ✓；⑤ **不限模式 + 池内即时收尾**：`--gens_per_round=0`（快池每轮可跑多代，慢池照旧 1 代 ✓）+ `--pool_tail=on`（每池挖完**立刻做该池收尾**，不再等全轮结束 ✗）；⑥ 顺带查证「研发占比」= **数据有但覆盖率 9.95%**（`rnd_to_revenue`）⇒ 2026-09-15 已试过并删掉 ⇒ **不加** ✓；⚠ 回退影响面：动看板控制层 + 调度工具（`mine.py` / `run_tracks.py` / `parallel_runner.py` / 前端三件）+ 一个新增只读接口，**引擎的因子求值逻辑未改** ✓；回退 ⇒ 徽标回到\"一律并行中\"、轮次回到全局 1/50、快池每轮只跑 1 代 ✓ |
| **v1.21.7** | 2026-09-19 | `d98ac1c` | ★★★★ **同一类「空 bank 池首代必崩」连修三处 + 按类清扫（不再一个个等崩 ✗）**（用户：_"上证50还是崩了"_ ⇒ 我挂机轮询三池、崩了接着修，**标准 = 三池连挖两轮都干净** ✓）：① v1.21.6 修 `_critic_review_prev` 的 `diag/reasons/r`（return 带出未绑定变量 ✗）② 本版修 `_build_fam_blacklist` 的 `nd`（同形态 ✗）③ 本版修 `run()` 里 `frozen, nd, r, s = _fsa_stats(…, nd, r, s, …)` 的 **`s`**（★ "**等号两边同名**"：右边要读的 `s` 此刻还没绑 ✗ —— 它唯一的真赋值在 `if seeds and r < cut[2]:` 里 ⇒ **首代没有种子（50 池 bank=0）走不到** ✗；⚠ 这处 traceback 是**抢读**才拿到的 —— 调度器重试会用**同名文件截断日志** ✗ ⇒ 写了 `ai_test/_grab_crash.py`（每 200ms 轮询 + 抢在截断前复制 ✓）；两处 `r`/`s` 均已核实为**死透传**（`_save_state` 里 `s` 是局部自己 `s = skeleton(nd)` ⇒ 传进去的参数从未被读 ✓）⇒ 给安全初值**语义零变化** ✓；★ **溯源**：全部由 v0.17.0 `41705f4`「P0-2 拆 run()」**给抽取函数补 return/补参数**时暴露 ✗（原内联版只是"潜伏没人读" ✓）· ★★ **新增常驻静态守门 `tools/_test_unbound_return.py`**：规则 A（return 带出未绑定的局部）+ 规则 B（**等号两边同名**，含**元组解包** ✗）；口径含四条硬教训（`with`/`try` 体必执行 ⇒ 透明 ✓ · **推导式是独立作用域** ✗ · 判定**只认更早的绑定行**（否则"当前语句自己"自证安全 ⇒ 漏报 ✗，实测踩三次 ✗）· 完整 `if/else` 两支都绑 ⇒ 算 ✓）；**自带自检**（内置必崩样本 2/2 命中 ✓、正例 0 误报 ✓）+ 2 处人工登记豁免 ✓；扫描 `loop_engine/loop_critic/factor_miner` **全干净** ✓ · ★ **验收（实测两轮）**：第 3 轮 `50 gen8 ✓4.0min · 300 gen71 ✓17.4min · 500 gen27 ✓35.9min`、第 4 轮 `50 gen9 ✓4.8min · 300 gen72 ✓33.6min · 500 gen28 ✓38.7min` —— **三池连续两轮全 `rc=0`、err 全 0 B** ✓（修复前 50 池 0.1~3.8 分钟必崩 ✗）· 全量回归 **29/29** ✓ | 回退该 tag ⇒ 空 bank 池（如 50）会重新"起来即崩" ✗ |
| **v1.21.6** | 2026-09-19 | `3982c29` | ★★★ **修真 BUG：空 bank 池「首代必崩」**（用户：_"上证50怎么崩了"_；`pool_50_gen8_err.log` **640B 有 traceback** ✗、日志只 535B ⇒ 起来就死 ✗）—— **根因** `engine/loop_engine.py::_critic_review_prev`：`diag`/`reasons`/`r` **只在"有上一代"分支里绑定** ✗，而**首代走 else** ⇒ `return (cfg, critic, diag, r, reasons)` 引用未绑定变量 ⇒ `UnboundLocalError: cannot access local variable 'diag'` ✗✗（何时走首代：`prev_l1` 空 = 该池种子/bank 为空，如 `50` 池 bank=0 ✓，日志实证「载入上一代种子 0 个」+「[B角] 首代, 使用默认策略」✓）；★ **溯源**：v0.17.0「P0-2 拆 run()」把内联代码抽成函数时**新增了 return 这三个值** ✗ —— 原内联版里它们只在后面被**重新赋值**（`critic.diagnose/suggest`）⇒ 老代码"不崩"只因当场没人读它 ✗（那次提交标题写着"顺带修潜伏 NameError"，实际**引入**了这一个 ✗）；**修法**：与"有上一代"分支**同形**的默认值 `diag, reasons, r = {}, [], ''` ✓（下游 `_agg_style_diag(..., r, ...)` 的 `r` 是**死参**、已核 ✓；`diag`/`reasons` 也会在 L2935/L2939 被重新赋值 ⇒ **语义不变** ✓）；★ 守门 `tools/_test_critic_firstgen.py`（新增 ⇒ 回归 **28/28** ✓）：`prev_l1=None`/空 DataFrame ⇒ 必须正常返回 5 元组且默认值同形 ✓ + 静态钉住"初始化必须在 `if prev_l1 is not None` 之前" ✗；⚠ 只影响引擎**启动阶段**（首代路径）⇒ 正在跑的那一代不受影响 ✓（代码在进程启动时载入 ✓），下次 spawn 自动生效 ✓ | 回退该 tag ⇒ 空 bank 的池（如 50）会重新"起来即崩" ✗ |
| **v1.21.5** | 2026-09-19 | `27494d0` | ★ **docs：README「版本与回退」表逐版补登**（用户："逐版补登，然后push"）—— 该表曾停在 `v1.3.10`（2026-09-16）✗，`v1.4.0`～`v1.21.3` 共 **49 个版本漏登记** ⇒ 本次**按各 tag 的说明/提交标题生成**、逐版补上 ✓；另把 `v1.21.4` 行的"提交"列从 `tag 自身` 回填为真实哈希 `5b06b08` ✓（表格约定：只有**最新一行**用 `tag 自身` ✓）| 无引擎行为变更 ✓ |
| **v1.21.4** | 2026-09-19 | `5b06b08` | ★★★ 修「**只停一个池，却误杀了其它池正在跑的引擎**」（用户报「我刚才停了一个上证50池，然后它报错说没有全部停掉…**我本来就想要跑 3 个池，那 3 个池我不想停呀**」）—— ★ **真凶**：单池停止里的兜底 `if c.get('curPool') == pool`，而 `curPool` 是「**最后启动过的那个池**」、**跑完并不清零** ✗（实录：50 池 gen7 于 00:33 正常跑完，`curPool` 仍留着 `'50'`；00:36 点「停止 50」⇒ 兜底命中 ⇒ **把正在跑的 300/500/1000 三个引擎全杀了** ✗✗，调度器 00:36:29-30 三个同时 `rc=1` 还记成"疑似崩溃"）；且残留检查用**同一陈旧条件** ⇒ 接口回 `ok=False` ⇒ 前端假报「**有进程没停掉**」✗（"过一会儿就没了" = 调度器立刻重试 + 错误 toast 12s 自动消失）；★ **修法**：新增 `mine.pool_engines()`（**逐池**归属 = 命令行 `--mine_pool=` **或** 调度器 `active` 表的 `{pool,pid}`，且 pid 必须在**真实活进程**里）**替换** `curPool` 兜底；残留检查改**轮询等待**（`taskkill /F` 返回 ≠ 进程已退出 ✗，原来 `sleep(1)` 就判定 ⇒ 假警报）；★ 新增守门 `tools/_test_stop_scope.py`（复现原场景断言"**一次 taskkill 都不许发生**"；全程打桩、不写任何文件 ⇒ 线上挖掘时可安全跑）⇒ 全量回归 **27/27** ✓；★ 同期（v1.21.3）另修两件「顶上/页面」的毛病：README 顶部**堆了 59 行**版本历史 ⇒ **压成一行最新版本号**（用户「顶上就搞个最新版本号不就行了」）· 看板版本号改**每次请求动态读** `VERSION`（原来 `VERSION = _read_version()` 冻结在进程启动时 ⇒ 用户「页面我刷新怎么还是 1.19.3 版本」✗，且"改一次版本要重启一次服务"是隐性坑）；⚠ 回退影响面：**只动看板控制层**（`dashboard/api/app/mine.py` + 一个 `tools/_test_*`），**引擎代码未改** ✓；回退该 tag ⇒ 单池停止会重新出现"误杀其它池"✗ |
| **v1.21.3** | 2026-09-19 | `d95fb62` | README 顶部瘦身 + 版本号动态读（刷新即生效） |
| **v1.21.2** | 2026-09-19 | `a6b956a` | 收尾默认池补上 50（单一来源 tool_pools） |
| **v1.21.1** | 2026-09-18 | `5491582` | 剥风格 Calmar 分两行（剥全部独立一行） |
| **v1.21.0** | 2026-09-18 | `b6dd2c2` | 剥全部=11 Barra+行业（口径标记 styCal）+ 状态查询命令 |
| **v1.20.9** | 2026-09-18 | `c2babb8` | 剥风格图版式回退（短标题 + 一行图例 + tip 承载解释） |
| **v1.20.8** | 2026-09-18 | `04419e8` | 剥全部：风格相关性三态对比 + 净值第7条 |
| **v1.20.7** | 2026-09-18 | `ec0a414` | 门控与 touched 修复 + 数据补齐 |
| **v1.20.6** | 2026-09-17 | `c35ca34` | 别名副本合并修复 |
| **v1.20.5** | 2026-09-17 | `964aabe` | 收尾补 strip/style + 未分类真因 |
| **v1.20.4** | 2026-09-17 | `b3a1e36` | docs: 并行轮同步成因 + §1.29 |
| **v1.20.3** | 2026-09-17 | `f9cd708` | docs: 大小盘轮动费后实测 + §1.28 |
| **v1.20.2** | 2026-09-17 | `c8a4ddf` | docs: 档案提交 + loop_todo §0/§4.7 更新 |
| **v1.20.1** | 2026-09-17 | `6f28be7` | docs: loop_todo §1.27（FSA 待观察项） |
| **v1.20.0** | 2026-09-17 | `dae9faf` | FSA 冻结期数化（2→4→8 封顶 + 冷却期遗忘） |
| **v1.19.4** | 2026-09-17 | `57cbe4d` | sign 显示源修正（退回重算指标表 + 标来路） |
| **v1.19.3** | 2026-09-17 | `776e270` | 入库日志状态徽标（三态 inBank）+ 测试误报真因修复 |
| **v1.19.2** | 2026-09-17 | `d1f274f` | 曲线按池取名 + 收尾补齐指标/曲线 |
| **v1.19.1** | 2026-09-17 | `3dd3b63` | 入库日志卡：时间带年份 + 详情按钮不贴滚动条 + 改两行式让公式整行宽 |
| **v1.19.0** | 2026-09-17 | `c157cb2` | 新入库日志（时间/池/编号/公式 + 详情） |
| **v1.18.2** | 2026-09-17 | `910c6fa` | 修吸顶表头重影（z-index） |
| **v1.18.1** | 2026-09-17 | `56a837f` | 登记表规模化加固 |
| **v1.18.0** | 2026-09-17 | `f8e885f` | 因子登记表（换机器重建的唯一入口） |
| **v1.17.3** | 2026-09-17 | `bc73a76` | 相位卡收窄 + --pools/--from_ctl 语义修正 |
| **v1.17.2** | 2026-09-17 | `2087901` | 池子秒崩真因与加固（None 权重/崩溃可见/active 过滤/ctl 锁） |
| **v1.17.1** | 2026-09-17 | `9635df6` | 去极值/标准化口径澄清 |
| **v1.17.0** | 2026-09-17 | `4acd210` | 顶栏文案：并行语义 + 逐池提示 |
| **v1.16.1** | 2026-09-17 | `70053fb` | e2e 测试安全跳过 |
| **v1.16.0** | 2026-09-17 | `1593268` | 风格剥后 NaN 修复 + 顶栏定宽 |
| **v1.15.0** | 2026-09-17 | `394ce17` | 历史编号指标与曲线 |
| **v1.14.2** | 2026-09-17 | `242e9a9` | 顶栏控件栏单行固定 |
| **v1.14.1** | 2026-09-17 | `a333d70` | 历史编号指标说明 + Barra 风格语义实测 |
| **v1.14.0** | 2026-09-17 | `2efa478` | 精选池指标显示修复 + 口径串去机味 + 守门升级 |
| **v1.13.0** | 2026-09-17 | `fd56f1c` | 文案去掉引号（出口清洗 _QM + 引号/AI味双守门） |
| **v1.12.0** | 2026-09-16 | `aa4c358` | 运行中加池马上并行（动态候选 + 动态上限 + 端到端回归） |
| **v1.11.0** | 2026-09-16 | `29c0722` | 默认并行 + 挨个点启动本池（单池语义 + 动态队列马上生效） |
| **v1.10.1** | 2026-09-16 | `2a95b88` | 并行动态队列：运行期加池马上生效（同时保住停池不自顶） |
| **v1.10.0** | 2026-09-16 | `2331ca8` | UI 只留模式开关：并行数自动算 + 停池后不补位 + 面板共享默认常开 |
| **v1.9.3** | 2026-09-16 | `870c3eb` | 并行实测后的 4 个修（参数沿用/无滚动条/撤预算/状态不谎报） |
| **v1.9.2** | 2026-09-16 | `aec5d43` | 顶栏控件禁止收缩/断行（flex:0 0 auto + nowrap） |
| **v1.9.1** | 2026-09-16 | `b704010` | 精简「面板共享」提示文案 |
| **v1.9.0** | 2026-09-16 | `b0e5134` | 看板可直接切换并行 + 面板共享（默认行为逐字不变 / 启动参数不可热改 / 内存护栏分档） |
| **v1.8.1** | 2026-09-16 | `17af5b7` | 修 --only-new 完整性检查 + 全库风格画像结论 |
| **v1.8.0** | 2026-09-16 | `b44a111` | 风格相关性柱状图（逐期截面 Spearman，t 三口径）+ 剥流通市值/剥总市值+限售 + self-test 连抓 3 个真 bug |
| **v1.7.1** | 2026-09-16 | `0d7b71f` | 所有曲线图：点图例即可显隐曲线（图例按钮化 + 隐藏退出 Y 轴范围 + 压暗删除线标示 + 换因子重置） |
| **v1.7.0** | 2026-09-16 | `e149cc3` | — 详情页曲线（离线预算）+ 指标补年化/回测区间 |
| **v1.6.0** | 2026-09-16 | `6e0f3a0` | — 组合自身日频风险 + 口径四格 + 精选池详情 + 顶栏统一高度 |
| **v1.5.1** | 2026-09-16 | `e4a0453` | — 因子库「历史」标记可信化 + 展示"在库里但文档没编号"的因子 |
| **v1.5.0** | 2026-09-16 | `19888d5` | — 因子库详情面板 + 全量费后指标补算 + 精选池文案 |
| **v1.4.0** | 2026-09-16 | `c29f5ed` | — 面板只读共享 + 有界并行调度（全是开关） |
| **v1.3.10** | 2026-09-16 | `bd98124` | ★★★ 修「**把全A停了再启动 ⇒ 其余等候轮转的池子全被取消**」（用户报「我把全A停了再启动，怎么剩下的等候轮转的池子都给取消掉了？」）—— ★ **真凶是一行接线**：池卡片 `onStart={() => doStart([p.key])}` ⇒ 走 `/api/mine/start`（**整体启动接口**）⇒ 后端 `start(["all"])` **把 `enabled` 整体改写为 `["all"]`**⇒ **其余 4 个池全被移出轮转** ✗✗（现场证据：`enabled=[all]` · `curPool=300`）；**正确** = `doStartPool(p.key)` ⇒ `/api/mine/start_pool`（**只动该池**）—— ★ 该函数 `v1.3.9` 已写但**忘了接线**（死代码）✗；另给「一键启动全部」确认框加透明提示（它会清「停止」标记）✓；★★★★ **为什么上次没测出**：`v1.3.9` 的端到端测试是**直接打 API** ⇒ 后端对，但**没覆盖"前端点按钮调哪个接口"** ✗✗ ⇒ **新增常驻回归 `tools/_test_frontend_wiring.py`**（命名 `_test_*` ⇒ 自动进全量回归）：① 池卡片 `onStart` 必须接 `doStartPool`（且不得误接 `doStart([`）·② 顶栏两个按钮的接线 · ③ **三个处理函数都必须真的被引用**（防"写了没接"）· ④ 页面的 `api.mine*` 必须在 `api.ts` 有定义 ✓；**验证**：停 all（`enabled` 仍 5 池）→ 启动 all（**`enabled` 仍 5 池** ✓✓）→ ★ **对照实验**证实旧接法 `/api/mine/start {pools:["1000"]}` ⇒ `enabled=["1000"]` ✗（bug 本体）✓；★ 教训：**"后端对"≠"用户点得到"** —— 端到端必须覆盖"点击→接口"这一跳 ✗ |
| **v1.3.9** | 2026-09-16 | `tag 自身` | ★★★ 修「**点 2 个池停止，再启动另一个池 ⇒ 其它池的剔除全被取消**」（用户报「我点了2个池子停止，然后重新启动中证500池，怎么其它池子都给取消了？然后中证1000池子还是在跑的状态，但是启动本池按钮还能点……你这前端逻辑整的也太混乱了吧」）—— ★ **根因：4 处代码都在清 `stopped`**（`mine.start()` 两处 / `mine.stop(None)` / **`run_tracks.py` 调度器退出复位**）—— 但 **`stopped` 是「用户的配置」、不是运行时状态** ✗；**修法：明确「配置 vs 运行时」边界** ⇒ `enabled`/`stopped` = **用户配置**（持久，只有「一键启动全部」显式重置）· `running`/`curPool`/`phase`/`stopAll` = **运行时状态**；`start()` 新增 **`reset_stopped`** 参数（`True` 默认＝一键启动全部会清；`False`＝`start_pool` 内部重启时保留）· `stop(None)` **只设 `stopAll`** · 调度器退出**去掉 `stopped=[]`** ✓；**前端按钮规则定稿（互补无重叠）**：启动 = `!inRotation` · 停止 = `inRotation || mining`；新增 `leaving`（在跑但已移出轮转）⇒ 徽标「**运行中·已移出**」✓；**端到端 5 项不变量全过**（含 ③ 单独启动 300 ⇒ `stopped` 仍 `[1000,50]`）✓；★ 教训（用户批评「逻辑混乱」，我接受）：**症状反复出现说明不是「每处写错了」，而是【状态模型没定义清楚】** ⇒ 修法**不是逐个打补丁，而是先画清「谁拥有、谁可改」** ✗ |
| **v1.3.8** | 2026-09-16 | `tag 自身` | ★★ 池卡片「停止本池」改为**随时可剔除**（用户报「已参与轮转还未轮到的，我也要可以停止那个池吧？不然我得**挨个点停止才会轮到我想停的池子**，这不科学。我一键启动之后要能**选中间的池子剔除出去**，然后剩下的池子轮这样才对」）—— ★ **v1.3.5 理解错了**：当时把"停止"当成"只停它当前那一代" ⇒ `disabled = busy || !mining` ⇒ **「轮转中/待启动」（还没轮到它）时按钮是灰的** ✗；但用户的语义是「**把它从轮转里剔除**」（**不管在不在跑都该能做**）✓ ⇒ 修正为 **`disabled = busy || !configured`**；后端 `stop(pool)` **本来就只写 `stopped`、不动 `enabled`**（v1.3.0 BUG A 的修法）⇒ 语义天然支持 ✓；本次只改**前端可点条件** + **提示文案**（按"当时是否在跑"区分措辞）✓；**实测**：停止"未在跑"的池 ⇒ `killed=[]`（没杀进程）· `stopped` 更新 · `enabled` 未变 · 其他池不受影响 ✓；`tsc` 0 错 · 15/15 测试 ✓。★ 教训：**"停止"的含义要与用户的动作意图对齐** —— 用户想的是"**别再跑这个池**（状态变更，随时可做）"，而不是"结束此刻那一代（瞬时动作，需"正在跑"前提）"✗ |
| **v1.3.7** | 2026-09-16 | `tag 自身` | ★★★ 修「**点了停止全A池，顶上『在跑的池』还是 5/5**」（用户报「我点了停止全A池，怎么顶上在跑的池还是5/5」）—— **真凶在 `dashboard/api/app/sources/pools.py` 的 `pool_status`**，两处错：① `not pr["pools"]` **宽兜底**（"解析不到归属 ⇒ 就当它跑遍所有池"）⇒ 旧引擎会让 5 池全显示在跑 ✗；② ★★ **`driver`（调度器）也参与判定** ⇒ 其命令行 `--pools=all,300,500,1000,50` ⇒ **5 个池全部命中** ✗—— 但它只是"**管**这 5 个池"，**不代表此刻都在跑** ✗；两者叠加 ⇒ **永远 5/5** ✗✗。**修**：**"在跑的池" = 「该池真的有 engine 在跑」**（只按 engine 的 `--mine_pool` 归属；调度器"正在跑哪个池"由控制文件 `curPool` 表达 = `mine.py` 的职责）✓；**实测**：修复前 `5 / 5` ⇒ 修复后 **`1 / 5`**（全A False / 300 True pids=[36308] / 其余 False）✓；引擎命令行已带 `--mine_pool=300` ⇒ 归属准确 ✓ |
| **v1.3.6** | 2026-09-16 | `tag 自身` | ★★★ 修「**停了全A却还在跑 / 显示 5 池 / 两个按钮都能按 / 下一个池不开始**」（用户报「我一键启动后，停止了全A池，怎么还显示在跑5池，而且全A池面板启动本池和停止本池两个按钮都能按，这有BUG，而且下一个池没有开始运行」）—— ★★★ **真凶**：`tools/run_tracks.py L363` 的 `extra` 里**硬编码 `--pools=300,500,1000`**（设计意图是让每个引擎**额外**算这几个池的指标、配合 `--pool_obs` 打池内标签，**不代表它在跑那几个池** ✗）；而老 `classify_proc` 是"先 `--mine_pool`、**没有就退到 `--pools`**" ⇒ **`all` 的引擎（当时没传 `--mine_pool`）被误判成"在跑 300/500/1000"** ✗✗ ⇒ **四个症状同一根因**：① `stop(all)` 找不到引擎 ⇒ **杀不掉** · ② `byPool` 一个 PID 出现在 3 个池 ⇒ **「在跑的池」虚高** · ③ `mining`（看 `curPool`）与 `engine`（看归属）**不一致** ⇒ **两个按钮都亮** · ④ 引擎没死 ⇒ 调度器还在等它 ⇒ **下一个池不开始**。**修 4 处**：① `run_tracks.py` **所有池都显式传 `--mine_pool`（含 `all`）**（`set_mine_pool(all)` 幂等 ✓）· ② `pools.py` **归属按 `kind` 分开解析**：`driver` 用 `--pools=`，**`engine`/`watcher` 只认 `--mine_pool`** ✓ · ③ `mine.py` 的 `mining` 改为「**真的有它的引擎在跑**」（与 `engine` 一致）· ④ ★ **`stop(pool)` 兜底**：`ctl.curPool == pool` 时**把当前引擎一并算进来杀**（不依赖命令行解析）✓；另给正则加参数边界 `(?<![\w-])`。**验证**：修复前 `engines=[(34568,[300,500,1000])]` + `byPool.all={stopped:True,mining:True,engine:[]}`（矛盾）⇒ 修复后 `engines=[(34568,[])]` + `byPool` 全一致 ✓；编译 118 · 14/14 测试 ✓。★ **教训（我连错两次诊断）**：诊断"解析类"问题**必须先把【原始输入】完整打出来** —— 我第一次只打印了命令行末尾 96 字符 ⇒ 漏掉中间那个 `--pools=` ✗ |
| **v1.3.5** | 2026-09-16 | `tag 自身` | 池卡片「**停止本池**」空闲时也变灰（用户报「底下的停止本池在空闲的时候也要变灰」）—— 旧条件 `busy || (stopped && !mining)` ⇒ 只有"已被停止"时才灰 ⇒ **「轮转中」「待启动」仍可点** ✗（但那两个状态里池**根本没在跑**，点了没意义且误导）；⇒ 改为 **`disabled={busy || !mining}`**（以"是否正在跑它这一代"为准）✓；★ **按钮一致性原则**：**只在"动作真的有效"时可点**，其余变灰 + `title` 说明原因（不是静默禁用 —— 见 v1.2.0 教训）✓；矩阵：挖掘中⇒停止可点；轮转中/待启动/已停止 ⇒ 停止均灰 ✓ |
| **v1.3.4** | 2026-09-16 | `tag 自身` | ★★ 修「启动按钮不变灰」+ **后台静默不弹黑窗**（用户报「一键启动全部后，这个按钮没有变灰还能按啊，还有你启动不要开 python 窗口啊，还有审查之类的，都搞成后台静默启动」）—— ① **按钮**：应按「**是否在运行**」判定，而不是"是否正在请求"⇒ 启动按钮 `disabled={mineBusy || running}`（在跑则文案变「已在运行」）、停止按钮 `disabled={mineBusy || !running}`（没跑就灰）✓；② ★★★★ **弹黑窗的真因是 Windows"控制台继承"陷阱**：看板后端用 `DETACHED_PROCESS` 起调度器 ⇒ 调度器**没有控制台** ⇒ 它再用 `subprocess.run` 起引擎时，Windows 会**新建一个控制台窗口** ✗（点启动弹一个、每代再弹、收尾还弹）；⇒ ★ **关键洞察：`CREATE_NO_WINDOW` 优于 `DETACHED_PROCESS`** —— 前者"**有控制台但隐藏**"⇒ **孙进程默认继承** ⇒ 全链路静默 ✓；后者"没有控制台"⇒ 孙进程会**新建窗口** ✗；**改动**：主链路 **12/12 处全覆盖** —— `run_tracks.py` 3（起引擎/`build_facs`/`cross_pool_review`）· `mine.py` 5（起调度器/收尾/`tasklist`/`taskkill`×2）· ★ `sources/pools.py` 1（**看板每 10 秒查进程** ⇒ 不加会一直闪黑窗）· `loop_watch.py` 3；外加 `loop_status.py`/`tracks_status.py` 3 ✓；★ 已核实 `build_facs`/`cross_pool_review`/`loop_engine` **都不 spawn 子进程** ⇒ 只需一层 ✓；**验证**：AST 静态核对 12/12 + 编译 115 + 14/14 测试 + `tsc` 0 错 ✓ |
| **v1.3.3** | 2026-09-16 | `tag 自身` | ★★ 修「点一键启动全部没反应」（用户报「点一键启动全部怎么没反应」）—— **诊断**（三步定位到前端）：后端日志里 `POST /api/mine/start` **0 条** ⇒ **请求根本没发出去** ✗；`/api/mine/state` 无变化；控制文件 `updated` 无新写入 ⇒ **卡在前端** ✓。**根因**：`window.confirm()` 在 **IDE 内置浏览器**（及部分受限环境）里**被阻止**：不弹窗、直接返回 `false` ⇒ `if (!confirm(...)) return` **直接返回什么都不做** ✗✗（与 `v1.2.0` "按钮被 `disabled` 悄悄禁用" **同一类毛病**）。**修**：新增 `askBox` + `ask()` + **自定义确认弹窗**（遮罩/标题/多行说明/取消·确定，危险操作用红按钮）⇒ 三处 `doStart`/`doStop`/`doStartPool` 全替换 ✓。★ 教训：**任何会"静默吞掉用户操作"的东西都要查** —— `disabled` 悄悄禁用 ✗ · `window.confirm` 被阻止 ✗ ⇒ **交互必须给可见反馈** ✓ |
| **v1.3.2** | 2026-09-16 | `tag 自身` | ★ **前端所有鼠标提示改自然语言**（用户报：「你的所有鼠标提示里面的引号都改改，**看着就是 AI 输出的**」）—— **根因**：`title`/`confirm`/`toast` **都是纯文本**、浏览器**不渲染 Markdown** ⇒ `**加粗**` 原样显示，叠加满屏 `★ ⇒ ✓ ✗` ⇒ **一眼机器味** ✗；**改**：7 处 tooltip + 3 处确认框 + 4 处 toast + 2 处页面文字（因子库「口径说明」）全部改成自然句子，去掉 `**` 与"工程符号"，只用 `「」，。；：` ✓；**新增常驻检查** `tools/_chk_ai_tone.py`（扫用户可见区、**排除代码注释**，实测**残留 0 行**）✓；★ 教训：**给用户看的文本用纯文本 + 自然标点，不要用 Markdown（不渲染）和 `★⇒✓`（显 AI）** ✗ |
| **v1.3.1** | 2026-09-16 | `tag 自身` | ★ 修 **UI 自相矛盾**（用户报：「前端看是**空闲**状态，怎么底下池子显示**已参与轮转**？…是BUG吗」）—— **真 BUG**：`PoolCard.inRotation` 只看 `slot.enabled` ⇒ 调度器停止后（`enabled` 保留上次配置）卡片**谎报"轮转中"**✗，且「启动本池」被 `disabled` ⇒ **想启动点不动** ✗✗。修：拆成 `configured`（**意图**）与 `inRotation = schedRunning && configured`（**此刻在跑**）⇒ 卡片四态（挖掘中/轮转中/**待启动**/已停止）+ 按钮在"待启动"时**可点**（文案「启动（调度器未运行）」，会自动重启调度器）+ 顶栏区分「N 池参与轮转 / N 池已配置·未运行 / 未配置」✓。★ 教训：**"配置状态"与"运行状态"必须分开建模** ✗ |
| **v1.3.0** | 2026-09-16 | `tag 自身` | ★★★ **挖掘改「单调度器 + 池轮转」**（用户拍板：「1、走 2；2、立即停不影响其他池挖掘审查吧？ 3、每轮结束自动收尾。4、……**启停任意池子都不能影响收尾这个不冲突吧？然后一键全部停掉后，它就自动进入收尾阶段**」；用户质疑「**四个池四份内存这不科学吧**？数据应该是共享一份只读才科学吧？」——★ **他是对的**）—— ★ **实测**：只读 `B`（49 个 float32 字段 3309×5384）**3.25 GB/进程** + `P` 2.52 GB + VCACHE≤2 GB ⇒ 单引擎 **≈6 GB** ⇒ 5 进程 **30 GB** > 可用 **27 GB** ⇒ **起不来** ✗；而每个池**只是它的列子集**（`set_mine_pool`）⇒ **同时只跑 1 个引擎** ⇒ **~6 GB（省 5 倍）** ✓；CPU 6 核/12 线程而单引擎已吃满多核 ⇒ **并行反而互拖 ⇒ 损失≈0** ✓。★ **架构**：`run_tracks.py --pools=<全部> --rounds=N` 单调度器；**交换两层循环**（原来"每池连跑 N 代"⇒ 实测 9.3 小时里 300 独占、500/1000/50 一代没跑 ✗；现"每轮每池各 1 代"）；代数**每代现取**（journal 为唯一事实源）；控制面 = `ai_test/_tracks/_control.json`（前端只读写它、调度器每代前重读 ⇒ 启停即时生效）。★ **收尾**（用户要求 3/4）：**每轮结束自动** + **一键全停后自动**；条件是"**无池在跑**"+"**本轮有进展**"，**不是**"所有池跑完"✗ ⇒ 停某池只是"不参与轮转"、**绝不阻塞收尾** ✓（"停某池、其他池继续"时暂不收尾是对的——别的池正在写 docs/，会撞车）。★ **立即停安全**（已核实）：池间**文件隔离** + 引擎落盘**原子+代末**（`loop_engine.py:1709` 注释明写 `.tmp`+`os.replace` 是为「**或进程被杀**」加固）⇒ 该代作废、旧状态完好 ✓。★ **实修 5 个真 BUG**：A 停池误清 `enabled`（只启 1 池 ⇒ 调度器直接退出）· B `start_pool` 调度器不在时报 409 （停掉最后一个池后点不动）· C `stopAll` 残留**阻塞下次启动** · D 全部停时调度器已死 ⇒ **无人收尾**（后端兜底）· E `phase` 卡在 `tail`（收尾函数自己复位）。★ **前端**：顶部**状态条**（空闲⚪/挖掘中🟢/收尾审查中🔵 + 当前池·代数·本轮进度）；**隐藏「收尾审查」按钮**（功能保留 `/api/mine/global`）；池卡片三态（挖掘中/轮转中/已停止）。★ **验证**（实测）：启动→单独停（`enabled` 保持）→单独恢复（自动重启）→全部停（兜底收尾+`stopAll` 已清）→收尾完成 `phase` 复位；调度器日志实证 `[SKIP] pool=50 已被单独停止 ⇒ 本轮跳过（不影响其他池）` ✓；`tsc` 0 错 | ★ 引擎代码**未改**（只改调度方式 + 一个开关）|
| **v1.2.0** | 2026-09-16 | `tag 自身` | ★★★ **看板改「每池一个独立进程」**（用户：「我点全部停止好像没反应嘛，然后要做成**真正能单独启停某个池**，要注意**每个池之间相互不能干扰、搞串了**哈」）—— ★ **病根（实测）**：旧版 1 个串行驱动跑 5 池 ⇒ 2026-09-15 23:38→09-16 08:57（**9.3 小时**）**300 池独占**（已测 20,000→39,200、journal 30→54 代），而 **500/1000/50 一代没跑** ✗；"停某个池"根本不可能。★ **新架构**：每池一个 `run_tracks.py --pools=<单池> --rounds=N --no_global` ⇒ 归属由命令行 `--pools=` 识别 ⇒ 可单独启停 ✓；★★ **安全性依据**：`set_mine_pool` 把 7 个路径（STATE/ARCHIVE/JOURNAL/LIBRARY/STYLE_OBS/STRIP_OBS/POOL_OBS）**全按池派生** ⇒ `docs/` 每族都是 5 份独立文件 ✓；**新增 `--no_global`** 挡掉唯一全局冲突点（`build_facs`/`cross_pool_review` 跨池去重必须看全所有池；5 份并行会重复 5 倍且并发写同一份 crosspool/selected ✗）⇒ 全局收尾改由单独一次 `--rounds=0` 完成（看板「收尾审查」按钮）；`all` 的注入对照集是只读 ⇒ 不冲突。★ **内存硬约束**：47.9 GB 总/27.1 GB 可用 · 单引擎 6~9 GB ⇒ **最多并行 ~3 个** ⇒ 启动前查内存、不够即 **409 拒绝**（防 OOM）；顶栏实时显示可用内存与"还可再启几个"。★ **修「停止没反应」**：真因是按钮 `disabled` 阻断太狠 ⇒ 改为**不阻断**、点了必给 toast 反馈（成功含 PID / 被拒含后端原因）✓；新增 `POST /api/mine/global`（有池在跑 ⇒ 409）· `state` 新增 `byPool`/`freeGB`/`canStartMore`/`maxParallel`。★ **验证**（用 50 池全链路）：启动→归属正确(`driver=[33244] engine=[34064]`)→重复启动被 `skipped`→**只停 50 且完全恢复**→收尾审查放行 ✓；`tsc` 0 错 | 引擎行为不变（看板控制架构 + 一个开关）|
| **v1.1.0** | 2026-09-16 | `tag 自身` | ★ **看板加「启动/停止挖掘」按钮**（用户：「直接在前端每个池子都加上启动/停止挖掘的按钮，然后在立即刷新按钮左边加个一键启动/全部停止的按钮，这样不就不会出现可能启动失败的情况了么」）—— 新增 `dashboard/api/app/mine.py` 与 `/api/mine/{state,start,stop}`。★★★★ **核心安全 = 防重复启动**：已有 `run_tracks.py`/`loop_engine.py` 在跑 ⇒ **409 拒绝**（两个驱动会抢同一份 `engine/loop_state*.pkl` ⇒ **因子库损坏、不可自动恢复** ✗✗）；另有池名白名单（非法 ⇒ 400）· 轮数 1~200 校验（非法 ⇒ 400）· 只允许启动固定脚本（**不接受任意命令** ✗）· `taskkill /T /F` 树杀 + 兜底杀孤儿引擎 · 子进程 `DETACHED_PROCESS` 脱离（后端重启不影响挖掘）· 注入 `PYTHONIOENCODING=utf-8`。⚠ **单池按钮的诚实语义**：`run_tracks.py` 是**串行驱动**（进程不分池）⇒ 「停止」只能停**整个驱动**，UI 的 tooltip 与确认弹窗**都明写**；「启动本池」仅在当前无任何挖掘进程时可点（否则禁用）。前端：池卡片加两按钮（带禁用逻辑+解释）+ 顶栏「轮数/一键启动/全部停止/立即刷新」+ confirm 二次确认 + 顶部 toast。验证：`tsc` 0 错 · **409/400 防御性实测通过** · 命令拼装用 `--dry` 验证 | 无引擎行为变更（看板控制能力新增）|
| **v1.0.3** | 2026-09-15 | `tag 自身` | ★★★ **修「`del f` 后又用 `f`」每代必崩真 BUG**（用户：「我点刷新怎么已筛选合计还是 92433，这么长时间连一轮都没有搞完吗」⇒ 一查**不是慢，是每池都崩**）—— `_driver.log` 实证：`300 gen28` 22:16 崩（24.8min）、`500 gen15` 22:45 崩（28.6min），两个 `_err.log` **均 626 字节、内容完全相同**：`L2663 _save_state(…, f, …) UnboundLocalError`。★ 根因（**两个盲区叠加**，故 v0.20.0 没抓到）：① `run()` 内 **L2519 `del f`**（候选循环里释放面板）⇒ 循环后 `f` 已删；② 循环外 L2663 仍传 `f`；③ **而该形参本就是幽灵** —— `_save_state` 内 `L1714 with open(_tmp,'wb') as f:` **局部重绑定**了它，L1735 读的是文件句柄。★ 修法：把 `f` 从形参+实参都删掉（26→25，仍逐一对齐），**保留 `del f`**（合理内存优化）。★ 新增 **`tools/_test_del_reuse.py`**（判「`del X` 后、下次 `X=…` 前又读 `X`」）；**已负向验证**（注入 `del top` ⇒ FAIL+exit 1；撤销 ⇒ 通过）；当前 `loop_engine.py` 9 处 `del` 全干净。★ 23:38 重启 5 池×50 轮 | 引擎行为**修正**（原为每代必崩）|
| **v1.0.2** | 2026-09-15 | `tag 自身` | ★ **修看板「在跑」判定失效真 BUG**（我引入）：`dashboard/api/app/sources/pools.py` 的进程过滤写 `CommandLine -match 'loop_code'`，而实际命令行是 `python.exe tools\run_tracks.py …` / `… --mine_pool=300` —— **都不含 `loop_code`**（那只是**工作目录**，不在命令行里）✗ ⇒ 引擎正跑却报 `anyRunning=False`/`在跑池=0`；改为按**脚本名**匹配 `loop_engine|run_tracks|loop_watch` ⇒ 实测 `anyRunning=True`、在跑池 5/5 ✓。★ 同时**启动「5 池 × 50 轮」挖掘**（`--pools=300,500,1000,50,all`；起点续代数 28/15/13/**4**/76；含 `--style_obs` ⇒ 新增池级 `docs/loop_style_obs_*.csv` = `§1.24-①c` 的输入）；⚠ 成本：单池单轮 60~80min ⇒ 5 池/轮 ≈ 5.8h、50 轮 ≈ 12 天，但**可断点续跑**。（教训：判"进程属不属于本项目"**不能靠工作目录**，要按脚本名/参数匹配；修完必须真跑验证）| 无引擎行为变更（看板进程匹配修复）|
| **v1.0.1** | 2026-09-15 | `tag 自身` | **恢复上证50（`pool=50`）数据文件 + 预建库骨架**（用户：「我看你都把前端加上上证50池子了……那就别归档了，重新拖出来」）—— 4 个文件 `Move-Item` 回 `docs/`（`loop_journal_50.md` / `loop_archive_50.csv` / `loop_pool_obs_50.csv` / `loop_strip_style_50.csv`）；★ 预建 `docs/factor_library_50.md` —— **不手写模板**（会与引擎打架 ✗），而是用 `ast` **从引擎 `_mk_library_skeleton` 提取模板** ⇒ 逐字一致 · 3 个锚点 3/3 ✓；⚠ 为何不直接调那个函数：它写到模块级 `LIBRARY`（默认=全A 库文件）⇒ 会**覆盖 `factor_library.md`** ✗；⚠ 提取时踩坑：初版要求 `.format` 有**位置参数**，而引擎是 `(...).format(t=, s=)` **只有关键字** ⇒ **总是提取失败**（静默回退）✗ 已修（改取 `sub.func.value`）。★ `engine/loop_state_50.pkl` 一直未动（bank=0 / n_tested=300）· 看板实测 50 池 代数 0→3 / 流水 0→30 | 无代码行为变更（恢复数据 + 新建库骨架 + 文档）|
| **v1.0.0** | 2026-09-15 | `tag 自身` | ★★ **里程碑：前后端看板建成** —— 新增 `dashboard/`（FastAPI 后端 + React/Vite 前端）：**池运行状态**（在跑/空闲 · 当前库 · 已测 · **跑过代数** · L2 流水 · 冻结）· **各池因子库** · **精选池**（L3 双闸门）· **进程** · **配置/口径**。★★ **端口"只改一处"已实证**（`dashboard/config.json` → 改后重启，前后端含 Vite proxy 全部跟着变；前端业务代码**零硬编码端口**）★ **版本也"只改一处"**：新增根 `VERSION`（此前 tag/`change_log`/README **三处打架** ✗）· 避开 `qlib_code` 端口 `5173/8001/27017` ⇒ 用 `8101/5273`。★ 期间抓到并修 6 个真缺陷（含 **`factor_library_<池>.md` 的"因子数"有**三个不同数字**：表格行数 10 / 声明 4 / **`state.bank` 5** ⇒ 看板以 `state.bank` 为准）| 引擎行为**不变**（纯新增看板 + 文档）|
| **v0.21.1** | 2026-09-15 | `tag 自身` | **`frontend/` → `dashboard/` 改名**（用户拍板，「frontend 和 backend 是不是并列？」⇒ 原名 `frontend` 却内含 `backend` **名不副实**）—— 新结构 `dashboard/{config.json, api/(后端), web/(前端)}`；★ **只改【目录名】不改【前端服务】概念**（`FRONTEND_DIR`→`DASHBOARD_DIR`，而 `config.json` 的 `"frontend"` 键 / `FRONTEND_PORT` / `qlib_code_frontend` **一律保留**）· 11 文件 48 处 · ★ 抓到一处**功能性漏网**（`_verify_port_change.py` 的进程匹配路径，不改会**杀不掉后端**）· 端口抽象与 `config.json` 位置**均不变** | 无代码行为变更（纯改名）|
| **v0.21.0** | 2026-09-15 | `tag 自身` | **新增 `frontend/` 挖掘看板**（首次有前端）—— 四路交叉判"在跑"（进程 + journal 代数 + `loop_state.pkl` + `loop_archive.csv`）；★ **免 import 引擎**（宽容 Unpickler 把 `Node` 换轻量替身 ⇒ 秒级读 state）· 修 3 个 bug（表头解析 `TypeError` · **空行截断表格** · Windows **GBK** 打印崩）| 引擎行为**不变**（纯新增）|
| **v0.20.8** | 2026-09-15 | `tag 自身` | ★★ **`§1.3-E` 换手优化完成**：**`--buffer=0.25` 双赢** —— 1000 池行业中性方案（S1）**换手 21.8%→16.0%（↓27%）· Calmar 1.064→1.413（↑33%）· 超额仅掉 0.10pp**（根因：换手降⇒成本降⇒净值更稳⇒回撤小⇒Calmar **反升**；⚠ 上限 0.5，`≥0.75` 反降）· ★ 修 `combo_constrain.py` **2 个真 BUG**（`--modes` 缺 `s05` 时 `KeyError`；**`--buffer>0` 从未跑通** = 索引空间混淆）· ★ **归档误伤修复**：6 个 `combo_*` 工具从 `history/` **迁回 `tools/`** | 组合层参数（新增推荐 `--buffer=0.25`）；**引擎行为不变** |
| **v0.20.7** | 2026-09-15 | `tag 自身` | **`loop_todo §1` 大整理：11 条 → 3 条真待办**（其余全部归档 `docs/log/todo_done.md`；`loop_todo 1079→712 行`）—— ★★ **核实推翻 `§1.5` 的关键前提**：它说"数据已在 `loop_pool_obs_*.csv` 里 ⇒ 可**离线 re-derive** 全池标签"，实测**每行只含一个池**，且**三池都测过的候选仅 2/1039 = 0%** ⇒ 缺池就是缺池 ⇒ **只能标注制**（已落地）✗ · `§1.24-④` 落地为 `factor_library.md` 顶部**口径警示** | 无代码行为变更（**纯文档**）|
| **v0.20.6** | 2026-09-15 | `tag 自身` | **核对 `loop_todo §1` 真实状态** —— ★★ **12 条里 8 条早已做完却没标完成**（完成记录其实都在 `change_log` 的 v0.5.2/0.6.0/0.6.1/0.7.0/0.8.0/0.9.0/0.11.0 里）⇒ 真待办只剩 3 条（`§1.3-E` 换手优化 · `§1.24-①c` 剥 2→4 等数据 · `§1.4` 产品定位待拍板）；`§1.6` 可关闭 | 无代码行为变更（**纯文档核对**）|
| **v0.20.5** | 2026-09-15 | `tag 自身` | **复核「剥风格」概念**（用户问「叶子本身用 BARRA 11 个构造，剥离不是白剥了么」）—— ★★ 厘清 **`STYLE_KEYS`（4 个系统性暴露，来自 `base_fields` 现算，**这才是剥离清单**） vs `BARRA_LEAVES`（11 个构造原料，**不能剥**，全库 28/57 因子含 `barra_` 叶子，`barra_residual_volatility` 一个被 22 个因子用）** ⇒ 名字像（`lncap` vs `barra_size`）但**来源完全不同** ⇒ **剥 `lncap`+`lnamt` 不是"白剥"**（实证 `F33` 剥后日频回撤 −19%→−10%）✓ | 无代码行为变更（**纯文档厘清**）|
| **v0.20.4** | 2026-09-15 | `tag 自身` | **文档大复核**（用户：「`loop_todo.md` 是不是又要清理了？看看里面有没有错的？有没有要归档的？把各个文档再查一轮」）—— ★★ 抓到 **5 处**：① **`§0`「一屏速览」停在 09-13**（写着"正在跑 ×3 轮 / 当前代 gen=3"，而**轨道早已结束、引擎停止**）⇒ 按**实测**重写（各池 75/27/14/12 代；入库 41/2/3/5）② **`§4.2`「现役门槛参数」缺 4 个生产在用的**（`--strip_style`/`--style_obs`/`--dup_ex_corr`/`--min_strip_calmar`）③ **`§4.6`「关键工具清单」列了 ~18 个已归档的 `ai_test/*` 工具**（自称"新会话最需要的一页"，照着跑会**全部失败**）⇒ 改为只列常驻工具 + 加 `ai_test/` 归档说明 ④ **`§1.3-A`（"首版已完成"却留在**待办**里 + 87 行详节）** ⇒ 归档到 `docs/log/todo_done.md`（`loop_todo` 1034→1012 行）⑤ README 里旧名 `tools/calib_gates.py` · 新增 `tools/_audit_doc_refs.py`（**扫文档里的路径引用是否存在**；★ 只判带目录前缀的 —— 初版把 `values_q.h5` 这类**泛指名字**误报为"路径不存在" ✗）| 无代码行为变更（**纯文档**）|
| **v0.20.3** | 2026-09-15 | `tag 自身` | **复核会话结论（用户："你几乎每轮都会出错，要不要再查一轮"）+ 回答「clone 多大 / 跑起来生成多少」** —— ★ **抓到并更正我自己写错的**：`change_log` 的 v0.20.1 条目曾写「最大函数 1291 行 → 223 行」（**张冠李戴**：把 `run()` 1291→921 与 `evaluate_real` 222 行混成一句）⇒ **真值：`run()` 仍是 920 行的全项目最大函数** ⇒ **`P0-2b`（引入 `ctx` 继续拆）依然必要** ✓ · 澄清 P0-1 的"10 处 → 1 处"口径（**4 代码 + 6 文档 → 机械清单 1 处**；实现/数值测试是**职责不是副本**）· 新增 `tools/_audit_clone_size.py`（**clone=3.66 MB / 生成≈8 GB**）与 `tools/_audit_rebuild.py`（重建链条：`E:\rq` **188 GB** 是唯一外部依赖）| 无代码行为变更（**纯文档更正 + 审计脚本**）|
| **v0.20.2** | 2026-09-15 | `tag 自身` | **补上 `--style_obs`（长期遗漏）+ `research/` 归档** —— ★★ `run_tracks.py` 一直**没透传** `--style_obs`，而它**成本≈0**（与已开的 `--strip_style` **共用** `style_features`，一代一次；**无额外回测**）却能补一个**已知盲区**：我们"剥风格"**只剥 2 个**（`lncap`/`lnamt`），`--style_obs` 记录 **4 个** ⇒ `lntr`/`lnpx` 一直看不见（实证：`F07` 号称"最独立"，`lntr` 暴露 **−0.51**）· `research/`（91 py，**0 生产引用**，停更 09-08）→ **`history/research/`** | 引擎行为**不变**（`--style_obs` 只落观测、不进选择）；**数据侧**：`docs/loop_style_obs.csv` 将从下轮起恢复累积 |
| **v0.20.1** | 2026-09-15 | `tag 自身` | **清生产侧硬编码路径 14 处** —— `standard_test.py`(PANEL/UNIVERSE/BARRA) · `qa_style_obs.py` · `loop_watch.py` · `calib_dedup_leaf.py` · `l1_shape_calib.py` · `gen_f11_daily.py` 全改 **`__file__` 派生**（**等价性已验证**）⇒ **换目录/换机器不再崩** · `_audit_deadcode.py` 加"**动态按名查找抓不到**"警告（实测差点误删活代码 `cs_demean_op`）· 全项目盘点（`research/`+`strategies/` = **0 生产引用**属历史；重复代码 24 组**全在历史目录**；最大函数 1291→223 行）| 引擎行为**不变**（纯路径派生，逐字等价） |
| **v0.20.0** | 2026-09-15 | `tag 自身` | **★★★ 修 P0-2 引入的「幽灵实参」崩溃**（`UnboundLocalError: e`，**已实际崩掉 `pool=300 gen28` 与 `pool=500 gen15` 两代**）+ **文件精简归档**（`docs/history/`→根 `history/`；`ai_test/` 整体归档；`standard/` 可归档件归档；50 池=上证50 产物归档）| 引擎行为**不变**（纯删多余形参，不改任何计算）；★ 但**修复了必崩** ⇒ 受影响代可正常跑完 |
| **v0.19.0** | 2026-09-15 | `tag 自身` | **架构清扫 P1b —— 拆 `loop_todo.md`** —— ★★ 病灶：`## 1. 待办（**唯一权威列表**）` 里**混着 18 条 `### 1.x 【已完成】`** ⇒ 列表从"该做什么"变成"做过什么"，**读待办要先过滤** ✗（与 roadmap 同源，换了个形态）· ★★★ 而外部有 **139 处** `loop_todo §x.y` 引用（`engine/loop_engine.py` 12 · `run_tracks.py` 7 · `change_log` 29 · 多个 `_test_*.py`）⇒ **编号一个都不能变** ⚠ · **做法**：**逐条分流 + 逐字搬运** ⇒ `loop_todo.md` **1,986 → 920 行（-54%）** + 归档 `docs/log/todo_done.md` **1,099 行**（18 条已完成 + `## 6. 进度日志` + `## 7. 归档 R1~R8`）· **保留 11 条**（`1.3/1.3-A/1.4/1.5/1.6/1.9/1.18/1.19/1.20/1.21/1.24`）· ★★ **一个"规则自己就对了"的验证**：判据「`✅` 或 `【已…】`」**恰好自动**把 `1.3-A「首版已完成」`（是"路线下一批"的一部分、结论影响 `1.4`）与 `1.20「铁律」`（是**原则**不是待办）分到**保留** ⇒ **无需白名单**（白名单会忘记维护）✓ · 编号不变 ⇒ `## 1.` **跳号是有意的**（文件里已写明）· 验证：**覆盖 29 条无遗漏无重复** + **逐字节在位** · 全量：编译 **83** · **11 个测试全绿** · 冒烟 **4/4** |
| **v0.18.0** | 2026-09-15 | `tag 自身` | **架构清扫 P1 —— 文档分家**（用户：「**921 行对大工程来说可以了**，然后搞 P1 文档分家」）—— ★ **先量结构**：`factor_roadmap.md` **4,704 行 / 325 KB**，其中**逐日开发日志占 3,692 行（79%）** ⇒ 这就是"把历史当现状"的根因；★★ 且**多套编号混用**（`## 1.`/`## 2.` 出现两次以上）✗；★ **9 处 `§8.x` 深链**（**8 处在代码注释里**）⇒ 搬家会全断链 ⚠ · **做法**：**按 `## ` 标题切 + 逐字搬运**（不手抄）⇒ **归档 15 段 / 4,542 行 → `docs/log/2026-09.md`（4,560 行）** + **保留 8 段 / 144 行** ⇒ 主文件 **4,704 → 264 行（-94%）** · ★★ **两个关键设计决策（不是照搬）**：① **「当前状态」只给指针不抄数值**（再抄一份=又造副本，正是 P0-1 刚治的病）⇒ 指向 `loop_todo §0` ② **9 处深链改指归档文件**（§号不变）· ★ **机器校验**：归档+保留段**逐字节在位** + **覆盖 23 个 `## ` 段无遗漏无重复**（遇未分类段**拒绝执行**）+ §8.x **索引 50 条自动生成** + 残留引用 **0** · ⚠ **我犯的错**：替换脚本**吞了相邻反引号**（`x` §8.46`` 配对错乱）**且顺手改了记忆文件** ⇒ 从 git 恢复 6 文件 + 改成**最小侵入**（只换文件名）+ 跳过 `.codebuddy/` · 全量：编译 **83** · **11 个测试全绿** · 冒烟 **4/4** · ⚠ 观察：`_test_reports_expr` 项数 **34→33**，已查 `docs/*.md` **15==HEAD 15 未变** ⇒ 非本次所致（引擎在跑、报告文件波动）· 遗留：`loop_todo.md`（1,900+ 行）未拆（可作 P1b） |
| **v0.17.0** | 2026-09-15 | `tag 自身` | **架构清扫 P0-2 —— 拆 `run()`：1291 → 921 行**（承接 v0.16.0 的架构债登记）—— ★ **先量测再动手**（`_map_run*` 三探针）：`run()` **1,291 行 / 24 段 / 282 局部变量**，**全拆的 params+returns 合计 269** ⇒ ★★★ **"全拆"是错的**（会造出几十个参数）；逐段看**13 段 cost ≤ 9** ⇒ **只提低耦合段** ✓ **结果**：`run()` **-29%** · 新增 **17 个函数**（`_critic_review_prev`/`_build_fam_blacklist`/`_run_l1`/`_run_l2`/`_save_state`…）**平均 28.6 行** · ★★★★ **方法：不手抄 —— 用 AST 精确算 params/returns + 源码切片逐字搬运**（手抄 487 行必然引入转录错误）· ⚠⚠⚠ **工具四代缺陷史（每条都靠实测抓出）**：漏形参 `args` · 段按"注释"划而**与语法块错位**（`import X as Y` 的 `Y` 是 `alias`）· 用**行号**判"先读后写"（同一行 `x = f(x)` 判错）· **跨界 `for` 赋值不被任何段收录** · `ast.walk` 把**推导式私有**变量当外层绑定 ⇒ **3/4 是编译期看不出的绑定错，全靠真机冒烟现形** · ★★ **检查器本身也会错**：【3】启发式**大量假阳性**（把 `for x in` 的循环变量报成"无来源"）⇒ 改用 **`symtable`** 精确判定（天然处理推导式/`import as`/闭包）· **验证三层**：**逐字节**（22 个函数体逐字存在 + 新 run() 剩余 **890 行 == 890 行、差异 0**）+ **`symtable`**（无漏传；曾抓 3 处 `cands`/`top` 已补）+ 真机冒烟 **4/4** · ★★★ **Fixed（重构顺带照出）**：`_lib_sync` 用了 `_lp.TAG_STRIP_DD_MIN` 却**无可解析来源**（`_tag_desc` 的 import 是它的局部、模块级也没有）⇒ 走该分支即 `NameError`，**且被 `try` 吞成静默失败**（因子库文档不更新却不报错）⇒ 补 `import loop_pools as _lp` · ⚠ **诚实遗留**：`run()` 仍 **921 行**（**5 个大段 cost 22~30，强拆更糟** ⇒ 需 `ctx` 方案，登记 `loop_todo §2.5` 的 **P0-2b**）· 全量：编译 **83** · **11 个测试全绿** |
| **v0.16.0** | 2026-09-15 | `tag 自身` | **架构清扫 P0-1 —— 算子「单一事实源」**（用户问「**你经常出错又自我修正，别的项目不这样 —— 是不是架构太乱/零碎？该不该先清扫？**」）—— 先用三个**只读体检**脚本量化（`_audit_codebase`/`_audit_coupling`/`_audit_deadcode`）：`184 个 .py / 32,042 行`；★ 单点巨人 **`loop_engine.py` 3,134 行**（其中 **`run()` 一个函数 1,291 行**）· ★ **加 1 个算子要同步改 10 处**（同一名单被抄 **4 份**）· 八维评分 功能性 **8**/可测试性 **8**/工程纪律 **9**/可维护性 **4** ⇒ **综合 ≈6.4** ⇒ **结论：不是"混乱"，是"高质量但未拆分的单体"**（纪律/测试 A 级、结构 C 级）· **做法**：新增 **`engine/ops_registry.py` = 算子唯一声明处**，`loop_engine.UNARY/BINARY` + `loop_critic.SLOW_OPS` + `loop_llm` 的 prompt **全部派生**（prompt 用 `{{OPS_*}}`/`{{LEAF_*}}` **运行时注入**）⇒ **加算子 10 处 → 1 处**、`loop_engine` **净减 ~150 行** · 删 **4 个死函数 + 13 个旧 helper** · ★★★★★ **最值钱的副产物**：抓到 **`engine/skills/gen_skill.md`（运行时真正生效的那份）缺 21 个算子 + 7 个叶子** ⇒ 而 prompt 又硬性要求"窗口必须是枚举值" ⇒ **v0.13.x 的 16 个算子 + v0.15.0 的 7 个财报叶子对 A角 从未生效** ✗✗（旧测试 grep 的是**源码=回退份**，看不见生效版）· **验证**：改造前先落**金标准快照** + **56+10 个算子逐位等价**（`allclose(atol=0,rtol=0)`）+ **`_test_ops_registry.py` 的"只改一处就够"行为证明** + 真机冒烟 4/4 + 11 个测试全绿 · ⚠ **遗留**：`UNARY` 顺序变了 ⇒ 候选流与旧版不同（集合逐字相同）；**P0-2 拆 `run()` 尚未做** |
| **v0.15.0** | 2026-09-15 | `tag 自身` | **财报叶子扩展 8 → 15 个**（用户问「财报数据不是有一堆吗？什么 ROE、PB、归母净利润同比」）—— 源里 **~394 个科目我们只用 8 个**；本次 **+7**：**`fa_pb`（PB，本次补上；与旧叶子截面 rank 相关仅 +0.063 ⇒ 最独立）** · `fa_accrual`（盈利质量 `ocf/np`）· `fa_asset_turn` · `fa_gw`（商誉占比）· `fa_inv_turn`/`fa_recv_turn` · `fa_sell_exp` · ★ 试过但**删掉 5 个**（覆盖率不够：`fa_int_cov` 0.01% / `fa_rnd` 9.95% / `fa_rnd_cap` 2.29%；信息重复：`fa_roe_wa` 与 `fa_roe` 截面 0.855 且口径是**报告期累积**会产生假变化 · `fa_roa` 与 `fa_np_margin` 0.867）· ★★ **判重复必须用「逐日截面 rank 相关」**（pooled 虚高，混进了跨时间的水平变化）· ★ 修两处**静默错误**（科目缺失从"整只跳过"改"容忍" · `fa_pb` 分母 `mktcap` 日变须按日算）· ★★ **旧 8 个逐值 0 差验证** · 叶子 54→61 |
| **v0.14.2** | 2026-09-15 | `d6c860d` | **回退 `--min_mono=0.75`** —— 跑完 3 轮 9 代实测：**省成本无效**（门在 `head(TOPN)` 之前，而 L1 候选 600~700 ≫ `TOPN=30` ⇒ top-N 照样填满；**根因是我标定时在 top-N 30 个上算、口径错了**）+ 选人改善无证据 ⇒ **无收益 + 有风险 ⇒ 不该开** |
| **v0.14.1** | 2026-09-15 | `f452e62` | **`--min_mono=0.75` 接入生产参数** + 修 `tracks_status.py` 的 `LOGD` 与 `run_tracks.py` **分家**导致进度视图全空的 bug（根因：`run_tracks.py` 迁入 `tools/` 后本文件仍按旧位置推算）· ★ 新增**长跑衔接器**手法（后台等 driver 结束再续跑，不用干等）· ⚠ 坑：**PowerShell 5.1 无 BOM 时按 ANSI 读 `.ps1`** ⇒ 中文字面量变乱码 ⇒ 凡 `.ps1` 写纯 ASCII |
| **v0.14.0** | 2026-09-15 | `0b25c5b` | **§1.3-B 核查：「生成后、L1 前」那道门在本项目没有可用判据**（用 `bank` vs `last_l1` 未入库 做正负样本标定：`length`/`n_nodes`/`n_fields` 作上界**只能拦 0~5%**；结构 Novelty **正负中位重合**）—— 根因：**`cfg['depth']` 已在生成端管住复杂度**（QuantaAlpha 需要它是因为候选由 **LLM 写**）；且我们表达式表示不同（`ts_mean20(close)` vs `TS_MEAN($close,20)`）⇒ **照抄 `unique_vars_ratio>=0.5` 会误杀 21.7% 已入库因子** · ★★★★★ **真正的发现**：预算瓶颈在 **L2**（`n_l1=28→n_l2=28→n_pass=2`，命中率 4.8%），而那里**已有一整套门**，其中 **`--min_mono` 已标定（1150 条：零误杀拦 27%）却默认关着** —— 我用**独立数据**（124 条）复核得 **35.6%**，两次独立标定互相印证 · **新增 `tools/calib_gate.py`（加门前先跑它）** · ⚠⚠ **更正**：早先报的"Novelty 方向反了"是**实现 bug**（`zip` 两列表错位），真相是**无区分度** · ★★★ 教训：**并行列表绝不要靠两个同条件推导式对齐**（我同一次任务犯了两次）· **未改引擎行为** |
| **v0.13.1** | 2026-09-15 | `39e0a1e` | **回归族补 `w=10`（`UNARY` 53→56）+ 定下「窗口档位四原则」** —— 治用户追问「**为啥不做 10/15/30**」：① 近似等比（短端密长端疏）② **优先复用已有档位**（实测 `w=15`/`w=30` **全项目从未出现** ⇒ 不加）③ 别加冗余档（趋势类相邻窗口高度相关）④ 短窗对高阶矩无意义 ⇒ **由此自我修正补 `w=10`**（`5/10/20/60` 间距 2×/2×/3×）· 真面板复核 **首有效期 = 4/9/19/59** ✓ · ⚠ 顺带记：插入函数定义时锚点选错会把上一个函数后半段挤进去（**编译通过但运行炸**）|
| **v0.13.0** | 2026-09-15 | `45fbad2` | **算子族扩展 ②③：回归三件套 + 偏度/峰度**（+16 个算子，`UNARY` 40→**56**）—— 用户点名 **slope + R²**：`ts_slope{5,10,20,60}`（趋势方向+强度）· `ts_rsqr{5,10,20,60}`（趋势**干净度**）· `ts_resi{5,10,20,60}`（同一次回归 ⇒ 近零额外成本）+ `ts_skew{20,60}`/`ts_kurt{20,60}`（**分布形状**，与波动率不等价）· ★★ **回答「R² 高说明涨得稳，你觉得呢」：方向对，但补三点** —— ① **R² 不含方向**（涨得稳/跌得稳 R² 相同，已固定成测试 ⇒ 必须与 `ts_slope` 组合）② 价格不动时 **0/0 ⇒ 输出 NaN**（不臆造、也不给假 1.0）③ **尺度无关** ⇒ 捕捉的是「路径有无单边趋势」而非「涨得多不多」· ★★ **「成本高的」用实测定义**（前缀和型 0.42s / 排序型 10× / **位置型 164×**）⇒ 顺带发现**「位置」可用「幅度差」替代**（`sub(ts_max100(x), x)` 能组合）· ★ **数学类不做**（`sqrt`/`pow`/`exp`：**对秩类因子无效**）· ★★★ **窗口档位原则**（用户追问「**为啥不做 10/15/30**」）：① 近似等比（短端密长端疏）② **优先复用已有档位**（实测 `w=15`/`w=30` **全项目从未出现**）③ 别加冗余档 ④ 短窗对高阶矩无意义 ⇒ **由此自我修正补上 `w=10`**（53→56）· 对拍 **`np.polyfit`/`scipy.stats` 逐位一致** + 两个负向验证 + 真机冒烟 5/5 · ⚠⚠⚠ **事故与修复**：**并行跑两个负向验证把源码改坏**（进程②把①的错公式"还原"回去）⇒ 给 `verify_test_catches.py` 加**并发守卫 + 还原后逐字节校验** |
| **v0.12.0** | 2026-09-15 | `2a621c1` | **加 `ema` 算子**（`ema5/ema12/ema20/ema26/ema60`，`α=2/(w+1)`）—— 治盘点发现的**唯一真缺**（`ts_mean` 等权 vs EMA 指数加权 ⇒ **组合不出来**）；★ **`sub(ema12,ema26)` 就是 MACD** ⇒ 不必单加 `MACD` · ★★ **顺手抓到一处已存在漂移**：`loop_critic._ops_of()` **硬编码 28 个算子而引擎有 45 个**（**缺 15 个长窗算子 ⇒ 一直被 B角 结构诊断忽视**）⇒ 改成**从引擎派生**（惰性 import 避免循环）+ **`_test_ops_sync.py` 22 项锁住**（含穷尽搜"白名单副本"、prompt 简写展开、`ts_ema` 手算递归/满窗/**无未来信息**）· ★ `min_periods=w` 满窗（EMA 早期值严重依赖起点）· ★★ **性能实测推翻估算**：真面板 `ema12` **0.58s** vs `ts_mean20` **1.17s** ⇒ **ema 反而快一倍** |
| **v0.11.3** | 2026-09-15 | `848021e` | **特征/算子盘点**（对照**本地 `QuantaAlpha-main` 源码**，不凭记忆）—— 结论：**四价一量 ✅ 全有**（`close`/`open`/`high`/`low`/`volume` 是**基础 7 叶子直接可用**）· **MA ✅**（`ts_mean` ×8 档）· **HHV/LLV ✅**（`ts_max`/`ts_min`，但只 2 档窗口）· ★★★ **真正缺的只有「加权均线族」= `EMA`/`MACD`**（`ts_mean` 等权、EMA 指数加权 ⇒ **组合不出来** ✗）—— 而这**正是文档里既定的"三期"项**（非遗漏）· 🟡 **RSI/BOLL/ATR 都能用现有算子组合出来**（`ts_rank` / `ts_mean`±`ts_std` / `ts_mean(true_range)`）· ★ 我们的**优势**：**资金流 16 列 + BARRA 11 + 财报 PIT 8**（QuantaAlpha 都没有）· **建议**：若补**只加 `ema` 一个算子**（加了就能组合出 MACD），并配套复杂度门（否则只扩大搜索空间） |
| **v0.11.2** | 2026-09-15 | `72098be` | ⚠ **更正 v0.11.1 的建议**（用户一问引出：「扩到 11 个**要不要等**每个风格都有 1-2 个因子？」）—— 我的答复：**① 「不用等」**（剥离用的是**风格暴露值**，与库里有没有该风格的因子无关）；**② ★★★★ 但「扩到 11」本身方向错** —— 实测**全库 28/56 = 50% 的因子用 BARRA 叶子构造**（`barra_residual_volatility` 一个被 **22 个**用；精选池 7 个里 5 个含它）⇒ 那 11 个是「**因子的构造原料**」而非「要剥的风格」，**剥它们=把因子掏空** ✗ ⇒ **正确动作 = `2 → 4`（加 `lntr`+`lnpx`），零边际成本**（`style_features` 本来就算这 4 个，剥离代码一行 `if` 过滤掉了后两个）· ★ 由此确立分工原则：**「诊断暴露」要全（15 个都可看）、「剥离清单」只含背景暴露（4 个）** —— **诊断维度 ≠ 中性化维度** |
| **v0.11.1** | 2026-09-15 | `789c3ab` | **核对「外部独立审查」的 7 个精选因子** —— 结论：**他的判断基本正确、引擎值 14/14 复现** ✓（我们 CSV 7/7 完全一致；`sign` 7/7 一致；300 内全员失效与 §1.3-A 交叉验证成立）。★ 但核对暴露**两条重要局限**：① ★★★ **「剥风格」只剥 2 个风格（lncap+lnamt），而暴露是 11 个 BARRA 风格** ⇒ `F07` 号称"最独立"但**仍暴露于换手率 `lntr` −0.51** ⇒ 「A 档」的准确含义只是「剥掉市值+成交额后仍有效」（**已记 roadmap §8.47**，是否扩到 11 个风格**待你拍板**）② **下游落地缺口（已修）**：`facs/` 存的是**原始值**、本表是**评估口径** ⇒ 直接用 h5 **绩效与回撤都对不上** ⇒ 精选池顶部加「**下游使用须知**」（必须乘 `sign` / 必须自己剥风格 / **不能用 `values_q.h5`** / 池内未测）|
| **v0.11.0** | 2026-09-14 | `fab2acb` | **「动作有效性」+「参数棘轮」**（§1.15 + §1.1，**同族一起做**）—— 共同点：**都要给动作加"施加前快照"**。① **有效性**：记目标指标基线，一段闭合后**结算**「施加了到底有没有变好」，无效 ⇒ **冷却翻倍**（3→6→12）⇒ 累计 2 次 **永久停用**（治「全A gen61~73 连压 13 代同一叶子且完全无效」）② **棘轮**：记参数旧值，永久停用时**保守回退**（只回退被证伪动作、**且之后没人再动过**的参数）⇒ 治「`depth` 被推到 `[3,4,5]` 后**再也回不去**」。★ 关键设计：**唯一写入口 `_set()` + 动作上下文**（同代多动作会改同一参数，手写旧值必然漏）+ **静态断言防止将来新增动作静默漏记** · ★★ 踩到并修掉「回退顺序导致**停在半路**」（改为**重试收敛**，不需排序）+ 「部分成功不能整体清账」· 回归 **33 项** + 既有 32 项不破 + **两个负向验证** + 真机冒烟 5/5 |
| **v0.10.0** | 2026-09-14 | `2da02b3` | **`sign`（方向）全链路补齐** —— 引擎侧 `_lib_sync` 给新入库因子写 `sign`（**写明细块、不加表格列**：库文件 append-only，加列会让历史行错位 —— 这个坑 2026-09-13 已踩过）+ **`tools/backfill_library_sign.py`** 回填历史（**59 块 · 58 匹配 + 1 未落地 · 幂等**，缺失写「未记录」**绝不臆造**）+ crosspool 视图表A/表B **加 `sign` 列**。★ 先实测回答「下游有没有按列解析」：**一处都没有**（解析的都是明细段/代码块/前两列内容）· ⚠ 修：回填工具幂等前缀太严会**重复插入**（幸好默认 DRY-RUN 当场救了一次）· 回归 **34 项** + 负向验证 |
| **v0.9.1** | 2026-09-14 | `9d16699` | **修 `factor_pool_selected.md` 的表达式被截断**（`[:58]` ⇒ 最长 **58→189**）—— 根因：该文件**只有表格、没有明细段**（对比 `factor_library*.md` 是「总览截断 + 明细全文」两层）⇒ **截了就无处可查** ✗ · ★ **顺带补上 `sign`（方向，必须乘）与 h5 路径** —— 原来 7 个精选因子里 **6 个 `sign=-1`**，不乘它**组合会反向选股** ✗ · 新增 `tools/_test_reports_expr.py`（27 项，编码"没明细段的文件不许省略"）+ **`tools/verify_test_catches.py`**（**负向验证**：把 bug 放回去看测试是否 FAIL —— 已实测抓得到）|
| **v0.9.0** | 2026-09-14 | `b22cea7` | **第二批 ②：亲本选择策略 `--parent_sel`**（`uniform` 默认=不变 / `best` / **`top_percent_plus_random`**）—— 治「**当前 `rng.choice(seeds)` 把排名信息全丢了**：L1 第 1 名和第 30 名被选中概率完全一样」，补上 QuantaAlpha 的**显式探索/利用配比**（我们原来只有「堵」的手段：`fam_quota`/`fam_block_thr`/`--decorr`，**没有「疏」**）· ★ 头号坑：`top_percent_plus_random` 的「否则」分支**必须从全池随机**（写成"只从 rest 随机"会**正好抵消、退化成 uniform**）⇒ 已用**统计检验**守护（比值 **2.42** vs 理论 2.43）· **新增 `tools/smoke_gen_only.py`**（用 `--gen_only` 做**几十秒的真机冒烟** + 断言 state SHA256 未变 —— 把"改引擎要等 50 分钟才知道有没有 `NameError`"降到 **30 秒**）· ⚠ 效果**尚未 A/B 实测** |
| **v0.8.0** | 2026-09-14 | `b1c6333` | **第二批 ①：给全A 轨道注入池库对照集**（`--inject_pools`，`bank_ext`/`bank_ex_ext` **只读注入**，**绝不写回** state/因子库 —— 否则因子库会凭空多出一批不是它挖的因子）⇒ 治「跑全A 会把池库重挖一遍」（实测收益流 \|相关\| 中位 **0.767**、>0.7 占 **82%**）· ★ **只给 `all` 注入**（池轨道的产出是"有效域标签"，注入全A 库会让它无产出）· `run_tracks.py` 默认自动 + `--inject_pools=none` 可关 · ⚠ 顺带修：**argparse help 里的裸 `%` 会让引擎完全起不来** |
| **v0.7.1** | 2026-09-14 | `499c85a` | **PATCH / 纯文档**：落**第二批改造清单**（`loop_todo §1.21`：`§1.15` 动作有效性 · `§1.1` 参数棘轮 · `§1.8` 池库对照集 · `§1.3-C` 亲本策略）+ 分批理由（一次改 5 处无法归因）|
| **v0.7.0** | 2026-09-14 | `6474965` | **第一批改造（判据与口径族）**：`strip_grade` 改**日频** + 新增 **A 档回撤上限 `dd_d > -0.20`**（回撤超限**只降 B 不判 C**）· 新增 `TAG_POOL_FLOOR_CAL=0.15` **修判据不对称**（原来池内只要超额>0 ⇒ `all3` 名不副实）· L2 三处回测开 `with_daily` · `loop_pool_obs` 加 `pools_scope`（§1.5）· **验证：`F10_1000` 标签 `all3`→`csi1000_only`，与外部审查闭环** |
| **v0.6.1** | 2026-09-14 | `59e9ac4` | **PATCH**：全库 50 因子日频实测后**更正 v0.6.0 的过度陈述** —— 「期频**系统性**低估 3.6pp」是 `F10` 极端个案，**全库折算比中位 0.928**（回撤平均只放大 ~1.08×）⇒ 日频的价值在**揪出个别高回撤因子**（`F03_1000` 日 dd **−35%**、`F11` **−23%**）· 新增 `tools/calib_daily_threshold.py`（**阈值敏感度表**：日频 0.25 ⇒ A 档 17 个 **与现状相同**）|
| **v0.6.0** | 2026-09-14 | `0c06780` | **新增「日频 mark-to-market」风险口径**（`evaluate_real(with_daily=True)` ⇒ `dd_d`/`calmar_d`/`sharpe_d`，默认关闭不改变行为）—— 治 §1.19「期频打点导致回撤系统性低估 ~3.6pp」。★ 两个坑写进 docstring：**不能用 `set_fwd(1)`（那是"每天调仓"的另一策略）**、**必须把日超额锚定到期频值**（否则两条净值路径不可比）⇒ `dd_d <= dd_e` **严格成立**。`tools/_test_daily_dd.py` **15 项**（含逐点抽查 418/418 期频点重合）· `build_facs` 落日频列 |
| **v0.5.5** | 2026-09-14 | `b5d975b` | **PATCH / 纯文档**：外部独立审查触发 —— 落 **§1.18**（`all3` 标签判据比入库门槛松：池内只卡"超额>0"、不卡 Calmar ⇒ 误标"真 alpha"）+ **§1.19**（引擎侧风险指标是**期频打点**口径 ⇒ 回撤低估 ~3.6pp、Calmar 高估 ~1.4×；**挖掘侧与组合侧不可直接比**）+ 重跑 gen7/8/9 结果（**收尾 23.7min→2s** ✓ · C 档零新增 ✓）|
| **v0.5.4** | 2026-09-14 | `e89980f` | **PATCH**：`tools/pool_grade_summary.py`（各池档位汇总 + **防回归告警**，`--since-gen` / `--warn-c`，超阈返回码 1）· ★ **§1.17 端到端验证通过**：修复后 gen7 入库 `F10_1000` = **A 档**（C 占比 **0%**），而修复前 8 代出 6 个 C 档（60%）|
| **v0.5.3** | 2026-09-14 | `d8a70c3` | 🔴 **修 `--pool_gate_or_all` 静默绕过「剥风格门槛 + 分段验证」**（`combine_ok()` 纯函数 + `_ok_hard`；`tools/_test_ok_gate.py` 22 项）· `tools/prune_style_only.py`（1000 池 bank 9→3）· 删 3 个垫片 · **产品定位拍板 B. 真中性增强** |
| **v0.5.2** | 2026-09-14 | `b80c93f` | **建 `tools/` 纳入 git**（23 个管线/工具脚本从 `ai_test/` 迁出 ⇒ **回退点完整**）+ 114 处引用更新 + 3 个兼容垫片（**轨道结束后可删**）+ 修 `HERE` 产物路径分裂 + 修 gitignore 误放开 101 个归档文件 |
| **v0.5.1** | 2026-09-14 | `bcea0aa` | **纯文档**：README 版本表修正 v0.5.0 的提交号 + `loop_todo §1.15`（规则1 固定动作且连压 13 代无效 ⇒ 缺"动作有效性"追踪）+ `§1.16`（`ai_test/` 未纳入 git ⇒ **回退点不完整**） |
| **v0.5.0** | 2026-09-14 | `3da89fe` | 池化挖掘闭环（中证1000 池 + 池门槛 OR 语义 + 收益流去重）+ **B角「诊断→决策」链条修复**（门槛对账 / 动作饱和 / **LLM 否决权 + 永久闭嘴**）+ 收尾提速 **23min→4.1s**（`build_facs --only-new`）+ 跨池审查/L3 精选池 + `facs/` 因子值仓（含 `values_q.h5` 快查副本）+ `change_log.md` |
| **v0.4** | 2026-09-12 | `6c60c7f` | 三池并行挖掘 `--mine_pool` + 入库剥风格/池内判据（`--strip_style`/`--pool_obs`）+ `loop_pools.py`（PIT 成分单一事实源）+ **修"整代末尾 `UnboundLocalError`"事故** |
| **v0.3** | 2026-09-12 | `41dbb9d` | `new_n` 判定 ✅ + `--reuse_v` 提速 35% + `--shape_neutral` |
| **v0.2.1** | 2026-09-12 | `3fb5f28` | `shape_pos_n`（风格中性后的第二套形状量，默认仅观测） |
| **v0.2** | 2026-09-12 | `641fe33` | 风格暴露观测 `--style_obs` + 批1 排序分配对实测 |
| **v0.1** | 2026-09-11 | `6ed6b00` | 成本档单一事实源 + L1 指标层（`loop_metrics.py`）+ `--min_mono` / `--score_mode`（默认关闭）+ `software_framework.md`；**引擎行为不变** |
| **v0.0** | `e582921` | 代码基线（改造之前）：gen51 双闸门版 |
